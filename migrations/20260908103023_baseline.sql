-- Техническая начальная миграция. Предметных таблиц она не создаёт: её задача
-- дать проверяемую стартовую версию схемы, чтобы `goose up` работал до первого
-- изменения с данными. Первая предметная схема добавляется отдельным файлом.

-- +goose Up
SELECT 1;

-- +goose Down
SELECT 1;
