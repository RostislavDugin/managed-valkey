# Полные интеграционные тесты оператора

`just test-full` пока сообщает `TODO` и завершается с ошибкой. Тесты нужно
добавить вместе с логикой, которая создаёт Valkey на k3s.

## Восстановление primary

Остановить активный процесс или pod Valkey, увидеть отсутствие primary,
дождаться безопасного выбора и запуска нового primary, затем проверить запись
в инстанс.

## Потеря worker-ноды

Отдельный сценарий: по `SYSTEM.md` promote после потери ноды требует
подтверждённого fencing и не всегда допускается автоматически.
