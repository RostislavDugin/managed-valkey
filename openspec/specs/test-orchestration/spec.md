# test-orchestration Specification

## Purpose

Определяет единый запуск тестов проекта, владение изолированными окружениями и достоверный отчёт о выполненных проверках без воздействия на рабочие dev-данные.

## Requirements

### Requirement: Общий интеграционный набор проверяет совместную работу backend

Общий интеграционный набор SHALL управлять инстансами только через настоящий TCP HTTP API и SHALL запускать рабочие фоновые обработчики API вместе с настоящим оператором. Он SHALL подтверждать результат по сохранённому состоянию HTTP API, объектам Kubernetes и настоящему TLS-подключению к Valkey. Административный клиент Kubernetes SHALL использоваться только для чтения, ожидания, диагностики и очистки ресурсов своего запуска. Тесты MUST NOT создавать или изменять CR напрямую, записывать `status`, снимать `finalizers`, вызывать фоновые обработчики как функции либо менять PostgreSQL для имитации результата.

#### Scenario: Создание single-инстанса

- **WHEN** тест регистрирует отдельный аккаунт и создаёт `single`-инстанс через HTTP API
- **THEN** фоновые обработчики доставляют Namespace, Secret и CR, оператор запускает Valkey, а импорт возвращает в API `status=running`, совпавшие поколения, применённые ресурсы и версию пароля
- **AND** TLS-подключение через публичный адрес принимает `AUTH`, `PING`, `SET` и `GET` с заданным паролем

#### Scenario: Изменение ресурсов single-инстанса

- **WHEN** готовый `single`-инстанс получает новый допустимый размер через HTTP API
- **THEN** оператор заменяет рабочий процесс, Kubernetes показывает запрошенные ресурсы, а API возвращает совпавшие поколения и новые `applied_vcpu` и `applied_ram_gb`
- **AND** Valkey снова принимает TLS-подключение с действующим паролем

#### Scenario: Ротация пароля single-инстанса

- **WHEN** тест записал значение в готовый `single`-инстанс и меняет пароль через HTTP API
- **THEN** API после работы оператора и фонового импорта возвращает совпавшие `password_version` и `applied_password_version`
- **AND** старый пароль отклоняется, новый пароль читает записанное до ротации значение

#### Scenario: Удаление single-инстанса

- **WHEN** тест удаляет готовый `single`-инстанс через HTTP API
- **THEN** оператор завершает удаление, фоновая синхронизация удаляет Namespace, а API исключает инстанс из карточки и списка и освобождает его резерв пользовательской квоты

### Requirement: Общий интеграционный набор проверяет метрики и метаданные владельца

Общий интеграционный набор SHALL подтверждать, что настоящий оператор собирает метрики экземпляра Valkey, созданного через HTTP API, API импортирует их в PostgreSQL и возвращает владельцу через публичный маршрут HTTP API. Набор SHALL также проверять метки идентичности и роли и точную аннотацию с адресом электронной почты владельца на объектах Kubernetes. Тестовый клиент Kubernetes MUST оставаться только читающим, а прямое чтение PostgreSQL MUST NOT использоваться как подтверждение результата.

#### Scenario: Метрики проходят через оператор, PostgreSQL и API

- **WHEN** общий интеграционный сценарий создаёт через HTTP экземпляр в режиме `single` и настоящий оператор переводит его в `running`
- **THEN** `GET /v1/managed/valkey/instances/{id}/metrics` до окончания срока операции возвращает для процесса с `ordinal=0` ряд с ожидаемыми именем и ролью
- **AND** хотя бы одна точка ряда содержит неотрицательные `used_memory_bytes`, `connected_clients`, `ops_per_sec`, `keyspace_hits`, `keyspace_misses`, `evicted_keys` и `cpu_millicores`
- **AND** тест не записывает `ValkeyInstance.status` и не читает таблицу метрик напрямую

#### Scenario: Метаданные владельца присутствуют в Kubernetes

- **WHEN** общий интеграционный сценарий получает готовые `Namespace`, `ValkeyInstance` и `Pod` созданного экземпляра
- **THEN** их управляемые метки содержат ожидаемые идентификаторы экземпляра и пользователя там, где эти метки предусмотрены контрактом ресурса
- **AND** `Pod` содержит подтверждённую роль `primary` в метке `valkey.h3llo-demo.com/role`
- **AND** каждый из трёх объектов содержит адрес электронной почты владельца в аннотации `valkey.h3llo-demo.com/user-email`

#### Scenario: Проверки дополняют существующий сценарий создания

- **WHEN** каталог обязательных общих интеграционных сценариев проверяется перед запуском
- **THEN** проверка метрик и метаданных выполняется внутри существующего сценария создания без нового обязательного сценария и дополнительного экземпляра Valkey

### Requirement: Общий интеграционный набор имеет ориентир длительности для CI

Штатный запуск общего интеграционного набора SHOULD завершать подготовку окружения, выполнение обязательных сценариев и обычную очистку примерно за пять минут. Запуск SHALL измерять и сообщать полную длительность. Превышение ориентира MUST NOT само по себе менять успешный результат тестов. Отсутствие обязательной инфраструктуры или обязательного теста и недостижение требуемого состояния до срока конкретного ожидания MUST завершать набор ошибкой. Сбор диагностики и аварийная очистка SHALL иметь собственные конечные сроки.

Сценарии создания, изменения ресурсов, ротации пароля и удаления SHALL начинаться параллельно и использовать общий стенд. Каждый сценарий SHALL владеть отдельными аккаунтом, Valkey-инстансом и Namespace. Зависимые операции внутри одного сценария SHALL выполняться последовательно. Первая версия набора MUST NOT ограничивать число одновременно работающих сценариев ниже четырёх; невозможность разместить четыре минимальных `single`-инстанса на тестовой ноде SHALL завершать набор ошибкой.

#### Scenario: Штатный запуск в CI

- **WHEN** общий интеграционный набор запускается на поддерживаемом CI runner с доступными зависимостями и образами
- **THEN** отчёт содержит полную длительность подготовки, обязательных сценариев и обычной очистки и позволяет сравнить её с ориентиром в пять минут

#### Scenario: Превышение ориентира длительности

- **WHEN** все обязательные проверки и очистка завершились успешно, но заняли больше пяти минут
- **THEN** набор сообщает фактическую длительность и остаётся успешным

#### Scenario: Параллельные цепочки

- **WHEN** сценарии создания, изменения ресурсов, ротации пароля и удаления выполняются на общем стенде
- **THEN** все четыре сценария запускаются параллельно через один API и один оператор, используя четыре отдельных аккаунта, Valkey-инстанса и Namespace
- **AND** состояние и очистка одного сценария не затрагивают остальные сценарии

#### Scenario: Четыре инстанса не поместились на ноде

- **WHEN** однонодовый стенд не может одновременно разместить четыре минимальных `single`-инстанса
- **THEN** набор завершается ошибкой и не уменьшает параллелизм автоматически

#### Scenario: Отдельное ожидание превысило срок

- **WHEN** API, оператор, Kubernetes или Valkey не достигает требуемого состояния до срока этой операции
- **THEN** набор завершается ошибкой, сохраняет безопасную диагностику и адресно очищает свои ресурсы

### Requirement: Корневая команда запускает все подключённые наборы

Корневой `just test` SHALL параллельно запускать быстрые наборы `api`, `operator` и `web`. Он SHALL завершаться примерно за пять минут на поддерживаемой рабочей машине при установленных зависимостях и MUST NOT запускать k3s-сценарии. Корневой `just test-full` SHALL параллельно запускать полные наборы этих приложений и обязательный общий интеграционный набор. Ни `test`, ни `test-full` MUST NOT запускать Playwright e2e. Отдельная корневая команда `just test-integrations` SHALL запускать только общий интеграционный набор. Каждый подключённый набор SHALL сам готовить своё окружение и выполнять необходимые этапы по порядку. Наборы, использующие общую кластерную квоту в PostgreSQL, MUST NOT конкурентно изменять одну тестовую базу.

#### Scenario: Быстрая проверка

- **WHEN** разработчик запускает `just test` из корня
- **THEN** быстрые наборы API, оператора и web выполняются параллельно без создания k3s

#### Scenario: Полная проверка

- **WHEN** разработчик запускает `just test-full` из корня
- **THEN** полные наборы API, оператора и web и общий интеграционный набор выполняются параллельно

#### Scenario: Только общая интеграция

- **WHEN** разработчик запускает `just test-integrations` из корня
- **THEN** выполняется общий интеграционный набор без запуска полных наборов приложений и Playwright e2e

#### Scenario: E2e запускается отдельно

- **WHEN** разработчик запускает `just test`, `just test-full` или `just test-integrations`
- **THEN** ни один Playwright-сценарий не запускается

### Requirement: Корневые команды форматируют и проверяют приложения

Корневой `just format` SHALL последовательно вызывать форматирование API, оператора и web и добавлять изменения этих приложений и общего Go-кода в индекс Git. Корневой `just lint` SHALL параллельно запускать локальный `lint` API, оператора и web, дождаться всех результатов и вернуть ненулевой код при ошибке любого приложения. E2e MUST NOT входить в эти агрегаты.

#### Scenario: Форматирование нескольких приложений

- **WHEN** разработчик запускает корневой `just format`
- **THEN** форматируются и добавляются в индекс изменения API, оператора, общего Go-кода и web без создания коммита

#### Scenario: Параллельный линтинг

- **WHEN** разработчик запускает корневой `just lint`
- **THEN** линтеры API, оператора и web работают параллельно, команда ждёт каждый набор и сообщает ошибку любого из них

### Requirement: Результат отражает фактически выполненные проверки

Корневые `just test`, `just test-full` и `just test-integrations` SHALL дождаться каждого начатого набора, вывести отдельные результаты и вернуть ненулевой код при ошибке подготовки, тестов или обязательной очистки. Неудача одного набора MUST NOT прерывать независимые наборы. Реализованный обязательный набор MUST NOT пропускаться из-за отсутствия инфраструктуры, переменной окружения или найденных тестов. Общий интеграционный набор SHALL считаться выполненным только после прохождения всех обязательных сценариев. Пустая команда, отсутствие найденных тестов или пропуск из-за недоступного k3s MUST NOT считаться успешной проверкой.

#### Scenario: Ошибка подготовки

- **WHEN** k3s не достиг готовности до срока ожидания во время `test-full` или `test-integrations`
- **THEN** соответствующий набор получает ошибку, остальные начатые наборы продолжают работу, а корневая команда возвращает ненулевой код

#### Scenario: Общий интеграционный сценарий не выполнен

- **WHEN** обязательный сценарий отсутствует в запуске, пропущен или завершился ошибкой
- **THEN** отчёт указывает непроверенный сценарий и общий интеграционный набор завершается неуспешно

#### Scenario: Общий integration-набор ещё не существует

- **WHEN** корневая команда не находит исполняемый общий integration-набор или его обязательные сценарии
- **THEN** запуск завершается ошибкой и не использует прежнюю успешную заглушку

### Requirement: Запуск владеет только своими ресурсами

Каждый запуск SHALL иметь идентификатор и точный перечень созданных процессов, кластеров, баз и сетевых настроек. Тестовые стенды SHALL готовиться независимо от постоянного dev и освобождаться после завершения, ошибки или прерывания. Очистка MUST NOT удалять общую PostgreSQL, dev-данные, посторонние маршруты или ресурсы другого запуска. Повторный запуск одного набора SHALL получать отдельное окружение либо явно отклоняться до изменения занятых ресурсов.

#### Scenario: Прерывание тестов

- **WHEN** пользователь прерывает корневой `just test` или `just test-full`
- **THEN** запуск завершает своих потомков и освобождает принадлежащие ему тестовые ресурсы, сохраняя dev и соседние запуски

#### Scenario: Повторный запуск набора

- **WHEN** два процесса одновременно запускают тесты API
- **THEN** они не используют одну базу и кластер; при невозможности выделить второе окружение второй процесс сообщает конфликт до записи

### Requirement: Диагностика сохраняется отдельно по наборам

Запуск SHALL сохранять отдельные журналы наборов и при ошибке собирать доступную диагностику до удаления стенда. Для Kubernetes диагностика SHALL включать состояния ресурсов и Events без содержимого Secret; для Playwright после его подключения SHALL сохраняться диагностические артефакты браузера с исключением секретов. Итоговый отчёт SHALL указывать пути к журналам.

#### Scenario: Падение медленного теста

- **WHEN** тест оператора завершается ошибкой
- **THEN** пользователь получает путь к его журналу и доступным состояниям Kubernetes после очистки кластера

### Requirement: Границы наборов сохраняются при подключении синхронизации

Быстрые API-тесты SHALL проверять HTTP и PostgreSQL без Kubernetes. Полные API-тесты SHALL дополнительно проверять доставку Namespace/Secret/spec и импорт status через настоящий Kubernetes API без оператора. Набор оператора SHALL начинаться с прямого создания CR без HTTP и PostgreSQL. Общий интеграционный набор SHALL проверять HTTP → PostgreSQL → CR → оператор → status → PostgreSQL → HTTP без подмены фоновых обработчиков. Playwright e2e SHALL выполнять пользовательский путь через браузер и полный стек без подмены backend. Общий интеграционный набор SHALL входить в корневой `test-full` и CI/CD, но MUST NOT входить в быстрый `test` или Playwright e2e. Полные Kubernetes-зависимые наборы API и аварийная матрица оператора SHALL оставаться локальными и MUST NOT включаться в CI/CD.

#### Scenario: Имитация наблюдений в API-тесте

- **WHEN** полный тест API проверяет импорт наблюдений или ожидание удаления CR с finalizer
- **THEN** тестовая обвязка меняет status или снимает finalizer своим административным клиентом, а приложение продолжает работать с ограниченным kubeconfig API

#### Scenario: Настоящее наблюдение в общем интеграционном тесте

- **WHEN** общий интеграционный тест ожидает создание, изменение ресурсов, ротацию пароля или удаление
- **THEN** только настоящий оператор меняет рабочие ресурсы и `status`, а API получает результат через свои фоновые обработчики

#### Scenario: Подключение сквозного сценария

- **WHEN** общий интеграционный набор проверяет создание инстанса через API с настоящим оператором
- **THEN** проверка использует однонодовое integration-окружение и входит в корневой `just test-full` и CI, но не в `just test` и не в Playwright e2e

#### Scenario: Тяжёлая аварийная матрица остаётся локальной

- **WHEN** CI запускает общий интеграционный набор
- **THEN** он проверяет штатные операции на одной ноде и не запускает сценарии HA, потери нод, сетевого разделения или восстановления после аварий

### Requirement: Обязательная матрица оператора на k3s

`operator just test-full` SHALL выполнять unit/envtest и все сценарии k3s из матрицы в design.md изменения add-operator-ha-and-mutations: HA, отказ процессов и worker-нод, сетевое разделение, ресайз, ротацию пароля и восстановление незавершённых операций. Матрица SHALL включать создание полного HA на одной, двух и трёх нодах и отказ ноды при совместном размещении процессов. Поведение Valkey и Envoy SHALL проверяться настоящими процессами и клиентскими подключениями. Unit/envtest SHALL дополнять эти сценарии проверками таймеров, CRD, конфликтов и недоступных ответов Kubernetes, но MUST NOT заменять k3s-проверки отказов. API и PostgreSQL MUST NOT требоваться. Отсутствующая возможность создать обязательный отказ SHALL завершать набор ошибкой, а не skip или успехом.

Подготовка образа оператора для тестового кластера SHALL входить в набор и MUST NOT зависеть от включения публикации релизных образов или развёртывания. Лимиты времени полного локального запуска SHALL учитывать обязательный набор с подготовкой, диагностикой и очисткой. `operator just test` и CI оператора SHALL выполнять unit/envtest без k3s; полная аварийная матрица SHALL оставаться обязательной локально.

#### Scenario: Полный локальный запуск

- **WHEN** запускается локальный `operator just test-full` непосредственно либо через корневой `just test-full`
- **THEN** выполняются все обязательные группы матрицы, результат включает каждый сценарий и ненулевой код при любой неуспешной обязательной проверке

#### Scenario: Невозможно создать отказ

- **WHEN** стенд не может отправить сигнал, остановить agent, установить сетевое ограничение либо запустить оператор внутри кластера
- **THEN** соответствующая проверка падает с причиной; сценарий не считается проверенным по изменённому вручную status

#### Scenario: Ограниченный запуск CI

- **WHEN** CI запускает обязательное задание `test-operator`
- **THEN** `operator just test` выполняет unit/envtest без подготовки k3s; отчёт обозначает ограниченный набор и не утверждает, что аварийная матрица проверена

#### Scenario: Превышение ориентира длительности

- **WHEN** полный локальный набор оператора на Ryzen 9 7950X с 32 потоками и примерно 32 GiB свободной RAM при скачанных образах занимает больше 10 минут с подготовкой и очисткой
- **THEN** превышение само по себе не делает запуск неуспешным; длительности анализируются, но дальнейшее ускорение не требуется при несоразмерных затратах или усложнении сопровождения

#### Scenario: Сборка релизов отключена

- **WHEN** полный локальный набор оператора выполняется без заданий сборки релизов и развёртывания
- **THEN** набор самостоятельно готовит образ и запускает оператор внутри k3s, включая добавленную или восстановленную ноду

#### Scenario: Три варианта размещения HA

- **WHEN** полный набор проверяет создание HA при достаточных ресурсах
- **THEN** отдельные сценарии подтверждают готовые primary и две синхронизированные реплики на одной, двух и трёх нодах

### Requirement: Топология стенда и изоляция аварий

Каждый параллельный сценарий оператора SHALL владеть независимым k3s с минимальной необходимой топологией: одна нода для обычного `single` и совместного размещения HA, две для переноса `single`, разнесённых Envoy или состава HA 2+1, три для предпочтительного разнесения HA и четыре только для сценария, которому нужна запасная нода. Число нод SHALL включать server; размещение и число реплик Envoy SHALL соответствовать проверяемому сценарию. В одном кластере SHALL выполняться не больше одного сценария одновременно. По умолчанию очередь SHALL допускать не более 12 одновременных сценариев и не более шести одновременных bootstrap. По умолчанию общий бюджет SHALL составлять 28672 MiB с минимум 3072 MiB доступной памяти после нового резерва; явные ограничения пользователя MAY снижать эти значения. Отказы нод SHALL затрагивать worker-agent, сохраняя k3s-server для Kubernetes API. Для потери оператора вместе с primary SHALL запускаться единственная копия рабочего оператора внутри k3s с его StatefulSet, ServiceAccount и Lease; хостовая копия SHALL быть остановлена. Все fault-действия SHALL проверять принадлежность ресурсов текущему запуску и сохранять точные идентификаторы целей.

#### Scenario: Общая потеря оператора и primary

- **WHEN** тест разместил оба процесса на выбранном agent и подтвердил размещение
- **THEN** уничтожение этого agent действительно останавливает оба процесса, а восстановление выполняет новый оператор внутри кластера

#### Scenario: Три ноды без запаса

- **WHEN** тест уничтожает одну worker-ноду без запасной, а оставшиеся ноды имеют ресурсы для замены
- **THEN** HA восстанавливает три готовых процесса на двух нодах и достигает `running` без добавления чистой ноды

#### Scenario: После отказа нет ресурсов

- **WHEN** после уничтожения worker-ноды ни одна оставшаяся нода не удовлетворяет жёстким запросам третьего процесса
- **THEN** HA сохраняет доступную запись в `degraded`, а третий Pod остаётся `Pending` до появления ресурсов

#### Scenario: Два независимых запуска

- **WHEN** одновременно работают два набора или постоянный dev
- **THEN** аварийные действия и очистка одного набора не затрагивают чужие контейнеры, правила сети, ноды и данные

### Requirement: Согласованная адресация тестового стенда

Каждый тестовый запуск SHALL сохранять выделенный `K3S_DOCKER_GATEWAY` и явно задавать его при создании Docker-сети. Gateway, адреса нод, включая дополнительный agent, и адреса тестовых клиентов SHALL принадлежать своей подсети и не совпадать. Доступ хостового оператора через `VALKEY_OPERATOR_CIDRS` SHALL соответствовать фактическому gateway. Добавление и восстановление agent MUST NOT менять адрес gateway или нарушать доступ оператора и изоляцию соседнего запуска. Отсутствие обязательного gateway SHALL останавливать подготовку тестового Compose до создания сети.

#### Scenario: Расширение до четырёх нод

- **WHEN** тест добавляет четвёртую ноду в выделенное окружение
- **THEN** её адрес не конфликтует с gateway, другими нодами и клиентами, а хостовый оператор продолжает подключаться к Valkey при действующей NetworkPolicy

#### Scenario: Замена agent после аварии

- **WHEN** уничтоженный agent заменяется чистым
- **THEN** gateway и разрешённый адрес хостового оператора сохраняются; повторная проверка административного подключения проходит до следующего аварийного сценария

#### Scenario: Gateway отсутствует

- **WHEN** тестовый Compose вызван без обязательного K3S_DOCKER_GATEWAY
- **THEN** подготовка завершается ошибкой до создания сети, вместо запуска стенда с неявно выбранным gateway

### Requirement: Проверяемые способы вызвать отказ

Тесты SHALL отдельно вызывать аварийную остановку процесса Valkey, штатное удаление Pod, SIGSTOP, BUSY и уничтожение worker-agent. При имитации уничтожения VM тест SHALL сначала подтвердить остановку agent вместе с вложенными процессами и исключить его автоматический рестарт, затем удалить Node. При сетевом разделении тест SHALL сохранять старую ноду и процессы живыми и подтверждать, какие пути связи нарушены. Тест MUST NOT вручную снимать finalizers, назначать роли или писать доказательства в CR ради завершения автоматического восстановления. Отдельная проверка ручного fencing SHALL сначала физически остановить целевую тестовую ноду.

#### Scenario: Остановка ноды

- **WHEN** выполнено аварийное отключение agent
- **THEN** тест проверяет остановку до удаления Node, а дальнейшую замену Pod и сохранение доказательства делает оператор

#### Scenario: Изолированный живой primary

- **WHEN** тест разорвал связь оператора с primary и сохраняет рабочее старое клиентское соединение
- **THEN** он проверяет отсутствие второго доступного для записи primary в течение окна дольше порогов отказа, NODE_LOST и ожидания пустого primary

#### Scenario: Потеря ответа выполненной команды

- **WHEN** тест проверяет неопределённый результат ACL, CLIENT KILL или REPLICAOF
- **THEN** настоящая команда доходит до Valkey и выполняется, а её ответ теряется контролируемо; проверка не ограничивается подменой результата без изменения реального процесса

### Requirement: Проверки во время отказа и после восстановления

Каждый аварийный сценарий SHALL подтверждать исходное состояние, наступление отказа, ограничения во время ожидания и конечное состояние. Проверки SHALL включать прямые роли и ACL процессов, идентичности, сведения о репликации, старые TCP-соединения и публичный доступ с проверкой TLS/SNI. Сохранность контрольных данных SHALL проверяться только для ключей, чью синхронизацию на необходимых репликах тест подтвердил до отказа. Сценарии допустимой потери кэша SHALL проверять отсутствие старого ключа и возможность новой записи.

#### Scenario: Успешный status без работающего Valkey

- **WHEN** CR показывает running, но клиент не может выполнить ожидаемые SET/GET либо прочитать синхронизированную реплику
- **THEN** сценарий падает и не считает status достаточным доказательством

#### Scenario: Рестарт на промежуточной стадии

- **WHEN** сохранена стадия перед изменяющей командой или команда выполнена до подтверждения в CR
- **THEN** тест останавливает оператор в этой точке, перезапускает его и проверяет продолжение той же операции без нарушения fencing и без преждевременного подтверждения поколения

### Requirement: Сроки ожидания и повторяемость аварийных тестов

Все ожидания SHALL иметь конечный deadline и понятное условие завершения; отрицательные проверки SHALL наблюдать запрет весь заданный интервал. Достижение стадии SHALL синхронизироваться по наблюдаемому состоянию или управляемой тестовой границе, а не случайной задержке. Сценарии, разрушающие общие ноды или сеть одного стенда, SHALL выполняться последовательно. Повтор SHALL создавать чистое состояние и сохранять исходное падение в результате, а не скрывать его удачным повтором. K3s-набор SHALL собирать тестовый процесс и образ оператора с отдельным профилем сокращённых интервалов, включая период и допустимое число неудач проверки готовности, сохраняя рабочие пороги в обычной сборке. Unit/envtest SHALL проверять рабочие пороги с подменяемыми часами. Тестовый профиль MUST NOT включаться в release-образ.

Длительность каждого сценария SHALL быть видна в отчёте `go test -v`. После каждого этапа изменения SHALL выполняться отдельный просмотр самых долгих сценариев и ожиданий. Для обычного переключения Pod или процесса на исправном стенде SHOULD использоваться ориентир 15–30 секунд. Превышение ориентира SHALL приводить к анализу ожиданий, но MUST NOT ослаблять проверку безопасности или скрывать нестабильность повторным успешным запуском.

#### Scenario: Таймаут восстановления

- **WHEN** ожидаемая роль, доказательство остановки или публичная готовность не достигнуты в срок
- **THEN** тест завершается ошибкой с последним наблюдением и собирает диагностику до очистки

#### Scenario: Восстановление agent

- **WHEN** закончился сценарий уничтожения ноды
- **THEN** стенд поднимает чистый agent, не возвращая прежние процессы Valkey, и подтверждает готовность инфраструктуры перед следующим сценарием

#### Scenario: Сценарий дольше ориентира

- **WHEN** обычное переключение на исправном стенде заняло больше 30 секунд
- **THEN** отчёт сохраняет фактическую длительность, а задача этапа проверяет фиксированные паузы, частоту наблюдения, сверку Envoy, повторную подготовку кластера и образа до увеличения общего timeout

### Requirement: Диагностика и очистка после аварий

Запуск SHALL сохранять идентификатор сценария, последовательность аварийных действий с временем, состояния CR/Node/Pod/EndpointSlice, Events и логи оператора и доступной инфраструктуры. Диагностика MUST NOT содержать Secret, хеши ACL, пароли или аргументы чувствительных команд. Очистка SHALL удалять собственные сетевые ограничения и аварийные вспомогательные процессы при успехе, ошибке и прерывании. При невозможности сохранить безопасный снимок SHALL сохраняться причина его отсутствия.

#### Scenario: Прерывание во время сетевого разделения

- **WHEN** тестовый запуск прерван после установки сетевого ограничения
- **THEN** очистка снимает только принадлежащее этому запуску ограничение, завершает его потомков и сохраняет доступные журналы

#### Scenario: Недоступный Kubernetes при падении

- **WHEN** Kubernetes API не отвечает во время сбора диагностики
- **THEN** сбор ограничен deadline, сохраняет доступные журналы контейнеров и причину отсутствия снимка Kubernetes, затем освобождает стенд

### Requirement: Каталог покрывает все сценарии без повторений

Полный запуск SHALL проверять соответствие каталога обнаруженным исполняемым сценариям до подготовки k3s. Каждый сценарий SHALL иметь ровно одно назначение, минимальную топологию и профиль инфраструктуры. Итоговый отчёт SHALL проверять выполнение всех назначенных сценариев и полной обязательной матрицы.

#### Scenario: Тест переименован

- **WHEN** каталог содержит отсутствующее имя или новый тест не назначен сценарию
- **THEN** запуск завершается ошибкой до создания кластеров и перечисляет несовпадения

#### Scenario: Сценарий назначен дважды

- **WHEN** два назначения охватывают один исполняемый сценарий
- **THEN** проверка каталога отклоняет запуск и указывает повтор

### Requirement: Параллелизм учитывает общий бюджет хоста

Запуск SHALL учитывать ресурсы подготовки, тестов и очистки вместе с резервами других активных наборов. Сценарии SHALL выполняться параллельно на независимых кластерах в пределах бюджета. Недостаток ресурсов MUST NOT приводить к пропуску обязательной проверки или запуску за пределами согласованного бюджета.

#### Scenario: Одновременно запущены API и оператор

- **WHEN** оба набора запрашивают ресурсы на одном хосте
- **THEN** их резервы учитываются совместно, а ожидающий сценарий стартует после освобождения достаточного бюджета

#### Scenario: Сценарий не помещается

- **WHEN** требуемый сценарий превышает весь настроенный бюджет даже без соседей
- **THEN** запуск сообщает недостаток ресурсов и завершается ошибкой вместо бесконечного ожидания

### Requirement: Остатки завершённых запусков очищаются адресно

Запуск SHALL обнаруживать оставшиеся тестовые ресурсы по зарегистрированному владельцу. Очистка SHALL проверять отсутствие активного владельца и принадлежность ресурсов, сохранять доступные безопасные журналы и не затрагивать dev или соседние запуски. Повторная очистка SHALL быть безопасной. Неопределённая принадлежность SHALL сообщаться без удаления.

#### Scenario: Владелец аварийно завершился

- **WHEN** обнаружены остатки запуска после потери его процесса и отсутствие владельца подтверждено с учётом повторного использования PID
- **THEN** очищаются только его процессы, контейнеры, тома, сети, маршруты и ограничения; результат перечисляет неустранённые остатки

#### Scenario: Владелец ещё работает

- **WHEN** обнаруженные ресурсы принадлежат активному запуску
- **THEN** очистка сохраняет их и учитывает занятый ими бюджет

### Requirement: Отмена дожидается завершения потомков

Отмена SHALL прекращать назначение новых сценариев, останавливать и дожидаться дочерних процессов перед окончательной очисткой. Ошибка очистки SHALL отражаться ненулевым результатом и перечнем остаточных ресурсов. Диагностика SHALL иметь ограниченный срок сбора и сохраняться после удаления инфраструктуры.

#### Scenario: Отмена во время подготовки

- **WHEN** пользователь отменяет запуск, пока часть кластеров ещё создаётся
- **THEN** после завершения отмены потомки не продолжают создавать ресурсы; каждый зарегистрированный стенд очищен либо указан как ошибка очистки

### Requirement: Playwright проверяет полный пользовательский путь

Обязательный e2e-набор SHALL проверять регистрацию, жизненные циклы `single` и `ha`, личную и общую квоту, потерю primary Pod и потерю worker-ноды. Сценарий полного жизненного цикла `single` SHALL выполняться первым. Локальные `just test-e2e` и `just test-e2e-headed` SHALL выполнять все сценарии `@prod`, `@local` и `@local-k8s` последовательно одним worker на одном стенде. CI SHALL делить тот же каталог на непересекающиеся группы `lifecycle`, `quotas` и `failures`, выполнять группы параллельно на отдельных стендах и сохранять последовательность внутри каждой группы. Каждый самостоятельный сценарий SHALL использовать отдельный аккаунт и MUST NOT зависеть от данных другого сценария. Набор MUST NOT пропускать обязательный сценарий при отсутствии переменной окружения, Kubernetes, браузера или другого элемента стенда.

Пользовательские изменения SHALL выполняться только через личный кабинет. Тест MUST NOT вызывать HTTP API напрямую, менять PostgreSQL, создавать или изменять CR, записывать `status` либо снимать `finalizers`. Административный доступ к Kubernetes SHALL использоваться только для чтения, диагностики и внесения отказов в `@local-k8s`.

#### Scenario: Новый пользователь зарегистрирован

- **WHEN** Playwright открывает страницу авторизации, вводит уникальную почту и дважды вводит `testpassword`
- **THEN** личный кабинет создаёт аккаунт, открывает `/valkey/management` и показывает почту аккаунта в авторизованной оболочке

#### Scenario: Обязательные сценарии выполняются последовательно

- **WHEN** запускается `just test-e2e` или `just test-e2e-headed`
- **THEN** один browser worker сначала выполняет полный жизненный цикл `single`, затем остальные сценарии `@prod`, `@local` и `@local-k8s` по одному на общем стенде
- **AND** два сценария не создают и не изменяют Valkey-инстансы одновременно

#### Scenario: CI выполняет группы параллельно

- **WHEN** GitHub Actions запускает полный Playwright e2e
- **THEN** `lifecycle` выбирает только `@prod`, `quotas` выбирает только `@local`, а `failures` выбирает только `@local-k8s`
- **AND** каждая группа получает отдельный стенд и один browser worker
- **AND** объединение групп содержит каждый обязательный сценарий ровно один раз

#### Scenario: Обязательный сценарий отсутствует

- **WHEN** каталог не находит требуемое название сценария или его метаданные запуска
- **THEN** e2e завершается ошибкой до подготовки тяжёлого стенда

#### Scenario: Неизвестная или пустая группа

- **WHEN** runner получает неизвестное имя группы либо выбранная группа не содержит сценариев
- **THEN** запуск завершается ошибкой до подготовки тяжёлого стенда

#### Scenario: Общие тестовые команды не запускают Playwright

- **WHEN** разработчик запускает корневой `just test`, `just test-full` или `just test-integrations`
- **THEN** e2e-окружение и Chromium не запускаются

#### Scenario: Виртуальная машина CI меньше проверяемого тарифа

- **WHEN** объект `Node` тестового k3s не может разместить `resources.requests` тарифа `4 vCPU / 16 GB`
- **THEN** сборка оператора с тегом `integration` MAY уменьшить только `resources.requests` Pod Valkey до значений e2e-окружения
- **AND** `resources.limits`, `maxmemory`, принятый тариф и применённая конфигурация остаются равны пользовательскому выбору
- **AND** рабочая сборка оператора не принимает такую настройку

### Requirement: E2E проверяет жизненный цикл single

Сценарий `@prod` SHALL зарегистрировать аккаунт, создать через личный кабинет минимальный доступный `single`, проверить применение размера и пароля через настоящий TLS-клиент Valkey, увеличить и уменьшить размер, сменить пароль и удалить инстанс. Размеры SHALL выбираться из показанного серверного каталога без зашитой в тест сетки.

#### Scenario: Single создаётся и принимает команды

- **WHEN** пользователь создаёт минимальный доступный `single` и сохраняет пароль из одноразового окна
- **THEN** во время создания страница показывает незавершённый статус и не разрешает изменение тарифа, пароля или доступа по IP
- **AND** после `running` страница показывает основной адрес и порт без RO-адреса и разрешает действия с конфигурацией
- **AND** TLS-подключение с SNI, пользователем `app` и данными из интерфейса выполняет `SET`, `GET` и `INFO`, а `maxmemory` соответствует применённой RAM

#### Scenario: Single увеличен и уменьшен

- **WHEN** пользователь последовательно выбирает следующий больший размер и исходный размер
- **THEN** после каждой операции выбранные и применённые ресурсы совпадают в интерфейсе
- **AND** `INFO` возвращает соответствующий `maxmemory`, а новое значение можно записать и прочитать
- **AND** тест не требует сохранения ключа после замены single при изменении размера

#### Scenario: Пароль single изменён

- **WHEN** пользователь меняет пароль и сохраняет новое одноразовое значение
- **THEN** прежнее соединение закрывается, новый вход со старым паролем получает отказ, а новый пароль разрешает `SET`, `GET` и `INFO`

#### Scenario: Single удалён

- **WHEN** пользователь подтверждает удаление значением slug
- **THEN** инстанс исчезает из списка, занятая личная квота становится нулевой, а новое соединение по прежнему адресу не устанавливается

### Requirement: E2E проверяет жизненный цикл HA

Сценарий `@prod` SHALL зарегистрировать аккаунт, создать минимальный допустимый `ha`, проверить основной и RO-маршруты, ресурсы трёх процессов, изменение размера, смену пароля и удаление. Размеры SHALL выбираться из показанного серверного каталога и текущей квоты.

#### Scenario: HA создаётся с двумя маршрутами

- **WHEN** пользователь создаёт минимальный допустимый `ha` и дожидается `running`
- **THEN** интерфейс показывает основной и отдельный RO-адрес с портом, размер одного процесса и суммарные ресурсы трёх процессов
- **AND** запись через основной адрес читается через оба адреса, запись через RO получает `READONLY`, а `INFO` через оба адреса показывает ожидаемый `maxmemory`

#### Scenario: HA увеличен и уменьшен

- **WHEN** пользователь увеличивает размер до следующего допустимого в текущей квоте, а затем возвращает исходный размер
- **THEN** после каждой перекатки интерфейс показывает совпавшие выбранный и применённый размеры
- **AND** основной и RO-адреса принимают допустимые команды, а `maxmemory` соответствует размеру одного процесса
- **AND** после уменьшения RAM тест не требует сохранения прежних данных

#### Scenario: Пароль HA изменён

- **WHEN** пользователь меняет пароль готового HA-инстанса
- **THEN** прежние соединения закрываются, старый пароль отклоняется, а новый пароль работает на основном и RO-адресах с учётом запрета записи через RO

#### Scenario: HA удалён

- **WHEN** пользователь удаляет HA-инстанс через личный кабинет
- **THEN** инстанс исчезает, личная квота освобождается, а основной и RO-адреса перестают принимать новые подключения

### Requirement: E2E проверяет личную и общую квоту

Сценарии `@local` SHALL выбирать размеры из показанного каталога и проверять предварительный запрет интерфейса как по личной квоте 4 vCPU / 12 GB, так и по суммарной учётной ёмкости. Они MUST NOT считать кластерный лимит гарантией физической раскладки, использовать занятость окружений разработки или эксплуатации либо подготавливать состояние прямой записью в API или PostgreSQL.

#### Scenario: HA не помещается в личную квоту

- **WHEN** новый аккаунт выбирает HA-размер, суммарный расход которого превышает свободные 4 vCPU или 12 GB
- **THEN** интерфейс показывает недостающие ресурсы, оставляет размер видимым и не разрешает отправить запрос создания
- **AND** HTTP-запрос создания не отправляется

#### Scenario: Изменение не помещается в личную квоту

- **WHEN** пользователь создаёт `single` 1 vCPU / 2 GB и `ha` 3 x 1 vCPU / 2 GB, затем выбирает для `single` размер, который вместе с HA превышает личную квоту
- **THEN** интерфейс показывает причину и не отправляет изменение
- **AND** после удаления HA тот же допустимый размер можно применить к `single`
- **AND** удаление последнего инстанса возвращает использование квоты к нулю

#### Scenario: Исчерпана общая ёмкость

- **WHEN** тест последовательно создаёт минимальные `single` у разных аккаунтов до исчерпания суммарного бюджета, затем открывает форму ещё одного аккаунта
- **THEN** интерфейс сообщает о нехватке ресурсов Managed Kubernetes и не разрешает отправить создание
- **AND** HTTP-запрос создания не отправляется, отклонённая карточка не появляется, а ранее созданные инстансы продолжают выполнять `SET` и `GET`

#### Scenario: Резерв уменьшает ёмкость пустого кластера

- **WHEN** `/capacity` для пустого кластера с полной ёмкостью 4 vCPU / 16 GiB возвращает лимит 3 vCPU / 14 GiB, личной квоты пользователя хватает и он выбирает `single` 4 vCPU / 16 GiB
- **THEN** размер остаётся видимым в каталоге, но интерфейс сообщает о нехватке ресурсов Managed Kubernetes и не разрешает отправить создание
- **AND** отдельный HTTP-тест с достаточной личной квотой подтверждает отказ API с `reason=cluster_quota`

#### Scenario: Общая ёмкость заканчивается при открытой форме

- **WHEN** один аккаунт держит открытую форму с доступным размером, а другой занимает последний суммарный ресурс
- **THEN** первый аккаунт после следующего опроса видит ошибку ресурсов Managed Kubernetes и недоступную отправку
- **AND** опросы `/v1/managed/valkey/capacity` не перекрываются

#### Scenario: Личная и общая квоты закончились одновременно

- **WHEN** выбранный размер не помещается ни в личную квоту, ни в общий бюджет
- **THEN** интерфейс показывает причину личной квоты и не показывает причиной Managed Kubernetes
- **AND** HTTP-запрос мутации не отправляется

#### Scenario: Бюджет нельзя исчерпать допустимым числом инстансов

- **WHEN** настроенный бюджет нельзя исчерпать минимальными инстансами раньше предела 32 инстанса
- **THEN** подготовка сценария завершается понятной ошибкой без создания пользовательских ресурсов

### Requirement: E2E проверяет восстановление после отказов Kubernetes

Сценарии `@local-k8s` SHALL запускать независимые пробы записи через основной адрес и чтения контрольного ключа через RO с интервалом 100 мс. В каждом сценарии тест SHALL наблюдать хотя бы один временный отказ записи и восстановление записи через прежний публичный адрес не позднее двух минут после внесения отказа. Максимальный промежуток без успешного RO-чтения MUST NOT превышать пяти секунд.

#### Scenario: Потерян primary Pod

- **WHEN** административный клиент определяет primary по свежему `status` CR и удаляет только его Pod
- **THEN** прежнее соединение закрывается, проба записи наблюдает временный отказ, а RO-чтение контрольного ключа не прерывается дольше пяти секунд
- **AND** после переподключения прежний основной адрес снова принимает `SET`, а контрольный ключ читается через основной и RO-адреса

#### Scenario: Потеряна worker-нода с primary

- **WHEN** primary работает на принадлежащем e2e-стенду k3s-agent, тест останавливает контейнер этого agent и только затем удаляет соответствующий объект Node
- **THEN** k3s-server и публичный адрес остаются доступны, RO-чтение не прерывается дольше пяти секунд, а запись временно отказывает и восстанавливается через прежний адрес
- **AND** при достаточных ресурсах HA достигает `running` с тремя готовыми процессами на двух нодах до возврата или замены agent

#### Scenario: Отказ прервал сценарий

- **WHEN** проверка завершается после остановки agent или запуска проб
- **THEN** teardown останавливает пробы, пытается вернуть только принадлежащий запуску agent и затем выполняет пользовательскую очистку

### Requirement: Headed e2e показывает пользовательские действия с интервалом

E2e SHALL выдерживать не менее 1000 мс между двумя последовательными пользовательскими действиями в headed-запуске. Headless- и production-запуски SHALL использовать интервал 0 мс. Пользовательскими действиями считаются переходы, ввод, выбор, переключение и нажатия в браузере. Перед вводом, выбором, переключением или нажатием помощник SHALL прокрутить страницу до целевого элемента. В headed-запуске прокрутка SHALL быть плавной, помещать элемент в центр видимой области и завершаться паузой 500 мс перед действием. Headless- и production-запуски SHALL прокручивать без этой паузы. Ожидания интерфейса, проверки, HTTP-наблюдение, команды Valkey и административные действия Kubernetes MUST NOT получать эту задержку.

#### Scenario: Два действия в headed

- **WHEN** пользовательское действие следует за другим пользовательским действием в headed-запуске
- **THEN** второе начинается не раньше чем через 1000 мс после первого

#### Scenario: Действие относится к элементу за пределами видимой области

- **WHEN** e2e собирается ввести значение, выбрать вариант, переключить поле или нажать элемент
- **THEN** помощник сначала прокручивает страницу до целевого элемента и затем выполняет действие

#### Scenario: Элемент выбран в headed-запуске

- **WHEN** headed-сценарий готовится взаимодействовать с элементом
- **THEN** помощник плавно прокручивает элемент в центр видимой области, ждёт 500 мс и выполняет действие

#### Scenario: Проверка следует за действием

- **WHEN** после пользовательского действия выполняется ожидание состояния или проверка backend
- **THEN** проверка начинается без искусственной трёхсекундной задержки

#### Scenario: Headless-запуск

- **WHEN** тот же сценарий выполняется в headless Chromium
- **THEN** помощник пользовательских действий не добавляет ожидание

#### Scenario: Production-запуск

- **WHEN** сценарий выполняется командой production-проверки
- **THEN** помощник пользовательских действий не добавляет ожидание

### Requirement: E2E очищает пользовательские ресурсы через интерфейс

Каждый сценарий SHALL регистрировать созданные аккаунты и после основного тела входить в них через новый контекст браузера, удалять оставшиеся инстансы через личный кабинет и ждать исчезновения карточек и освобождения квоты. Очистка SHALL выполняться после успеха, ошибки и прерывания. Прямые DELETE-запросы, SQL, удаление CR или Namespace и снятие `finalizers` для пользовательской очистки запрещены. Ошибка очистки SHALL завершать весь запуск ненулевым кодом и сообщать только почту, имя и slug оставшихся ресурсов.

#### Scenario: Сценарий завершился до удаления

- **WHEN** основной сценарий упал после создания одного или нескольких инстансов
- **THEN** новый контекст браузера входит в каждый зарегистрированный аккаунт и пытается удалить все видимые инстансы через интерфейс

#### Scenario: Очистка не завершилась

- **WHEN** инстанс не исчез или квота не освободилась до срока очистки
- **THEN** e2e завершается ошибкой и не печатает пароль аккаунта, пароль Valkey, JWT или содержимое Secret

### Requirement: Диагностика e2e не раскрывает секреты

E2e SHALL записывать пароль аккаунта и каждый полученный одноразовый пароль Valkey в закрытый список контрольных секретов. До публикации отчёт Playwright SHALL удалять известные секретные значения, а диагностическая проверка SHALL искать их в обычных файлах, ZIP и встроенных данных HTML-отчёта. Трассировка SHALL оставаться выключенной. Снимок страницы при ошибке SHALL маскировать видимые поля одноразового пароля. Кubeconfig, JWT, тела запросов с паролями и содержимое Kubernetes Secret MUST NOT публиковаться.

#### Scenario: Ошибка при открытом окне пароля

- **WHEN** сценарий падает, пока на странице показан одноразовый пароль Valkey
- **THEN** сохранённый снимок маскирует значение, а опубликованный отчёт и диагностика не содержат исходный пароль

#### Scenario: Проверка нашла секрет

- **WHEN** после очистки отчёта известное секретное значение остаётся в любом публикуемом файле
- **THEN** публикация диагностического результата запрещается и job завершается ошибкой

### Requirement: Регистрацию можно безопасно проверить на рабочем сайте

Корневая команда `just test-e2e-prod` SHALL вручную запускать сценарии `@prod` в видимом Chromium по точному адресу `https://app.h3llo-demo.com`. Команда SHALL проверять регистрацию и полные жизненные циклы `single` и `ha`, MUST NOT запускать сценарии личной или общей квоты и отказы Kubernetes и MUST NOT запускаться из CI или после deploy. После успешного запуска в production не должно оставаться Valkey-инстансов; созданные аккаунты могут оставаться, пока личный кабинет не предоставляет их удаление.

#### Scenario: Ручная production-проверка

- **WHEN** разработчик запускает `just test-e2e-prod`
- **THEN** Playwright выполняет регистрацию и жизненные циклы `single` и `ha` на `https://app.h3llo-demo.com`
- **AND** пользовательская очистка удаляет созданные Valkey-инстансы до успешного завершения команды

#### Scenario: Production-проверка не вносит отказ

- **WHEN** запускается `just test-e2e-prod`
- **THEN** Playwright выбирает только сценарии `@prod` и не обращается к административному Kubernetes или Docker

### Requirement: Имя теста описывает действие, условия и результат

Каждый именованный тест SHALL по возможности сообщать проверяемое действие, существенные условия и ожидаемый результат без чтения тела теста. Go-функции SHALL называться по форме `Test_<WhatWeDo>_<WhatWeExpect>` или `Test_<WhatWeDo>_<WhichConditions>_<WhatWeExpect>` с английскими сегментами в `PascalCase`. Идентификаторы обязательной матрицы MAY занимать отдельный сегмент сразу после `Test_`.

Когда тестовый фреймворк принимает произвольную строку, самостоятельный сценарий SHALL получать длинное понятное название по-русски. Группы сценариев и варианты одного табличного случая MAY использовать короткие названия без префикса `Test_`, если они лишь уточняют значение параметра или общий контекст.

#### Scenario: Go-тест имеет имя идентификатора

- **WHEN** разработчик добавляет или переименовывает самостоятельную Go-функцию теста
- **THEN** её имя начинается с `Test_` и явно называет действие, существенное условие при его наличии и ожидаемый результат

#### Scenario: Фреймворк принимает строковое название

- **WHEN** самостоятельный сценарий объявляется через `t.Run`, Vitest или Playwright и его имя не обязано быть идентификатором кода
- **THEN** сценарий получает самодостаточное русское название, по которому понятны действие, условия и ожидаемый результат

#### Scenario: Имя обозначает только группу или вариант

- **WHEN** название относится к группе сценариев, версии движка или отдельному значению параметра таблицы
- **THEN** оно остаётся короткой понятной меткой и не изображает самостоятельный тест

#### Scenario: Бывшая тестовая функция запускается как подтест

- **WHEN** запускатель вызывает бывшую самостоятельную Go-функцию как подтест
- **THEN** строка `t.Run` дословно совпадает с полным именем этой функции

### Requirement: Ссылки на имена тестов остаются согласованными

Переименование теста SHALL одновременно обновлять каждый активный каталог, фильтр запуска, проверку отчёта и тестовые данные, которые ссылаются на его точное имя. Исторические архивные документы и сохранённые журналы MUST NOT переписываться ради соответствия текущим именам.

#### Scenario: Integration-тест оператора переименован

- **WHEN** меняется имя сценария, назначенного в каталоге оператора
- **THEN** каталог выбирает новое точное имя, а проверка полноты подтверждает единственное назначение до подготовки k3s

#### Scenario: Изменился общий префикс набора

- **WHEN** переименование меняет префикс, которым скрипт выбирает envtest или другой набор
- **THEN** фильтр запуска и проверка его отчёта используют новый префикс

### Requirement: Проверки подтверждают два адреса каждого режима

Изменённые unit-, operator k3s, общие интеграционные и Playwright e2e-наборы SHALL проверять постоянное наличие основного адреса и адреса чтения у `single` и `ha`. Проверки SHALL различать семантику режимов: в `single` оба адреса ведут на один primary и допускают одинаковые команды, в `ha` адрес чтения выбирает готовую реплику и отклоняет запись.

#### Scenario: Unit-проверки контракта API

- **WHEN** API создаёт, читает или возвращает credentials инстанса любого режима
- **THEN** тесты подтверждают непустые `host` и `host_ro`, постоянный порт и сохранение адресов после изменения настроек сервера

#### Scenario: Оператор проверяет single с двумя маршрутами

- **WHEN** полный набор оператора создаёт настоящий `single` в k3s
- **THEN** основной SNI и SNI чтения проходят TLS, whitelist и маршрутизацию к тому же процессу
- **AND** запись через основной адрес читается через адрес чтения

#### Scenario: Общая интеграция проверяет контракт через HTTP

- **WHEN** общий интеграционный сценарий создаёт `single` через настоящий HTTP API
- **THEN** API возвращает оба адреса, оператор подтверждает оба маршрута, а настоящий TLS-клиент записывает через основной адрес и читает через адрес чтения

#### Scenario: E2E single использует оба адреса

- **WHEN** Playwright проходит полный жизненный цикл `single`
- **THEN** интерфейс показывает строки «Адрес для записи и чтения» и «Адрес только для чтения» со значениями `rediss://<host>:<port>` по HTTPS или `redis://<host>:<port>` по HTTP, а клиент разбирает оба варианта и выполняет допустимые команды через оба адреса
- **AND** удаление прекращает новые подключения по обоим адресам

### Requirement: Проверки покрывают окно обслуживания и подписи

Тесты API SHALL проверять создание, изменение, очистку и валидацию окна обслуживания. Тесты web SHALL проверять обязательное местное окно, преобразование в UTC при отправке и обратное преобразование ответа. Playwright SHALL задавать окно через форму создания, менять его через действие в строке карточки и проверять согласованные пользовательские подписи без прямых обращений к HTTP API.

#### Scenario: API создаёт инстанс с окном

- **WHEN** HTTP-тест отправляет допустимое окно обслуживания в POST создания
- **THEN** ответ и повторное чтение содержат то же окно, а идемпотентный повтор не создаёт вторую запись или событие

#### Scenario: Web проверяет форму и карточку

- **WHEN** компонентные тесты создают базу с обязательным местным окном и затем меняют его
- **THEN** они подтверждают тело POST, отдельный PATCH, строки «Белый список» и «Создано» и отсутствие устаревших подписей

#### Scenario: E2E меняет окно через личный кабинет

- **WHEN** Playwright создаёт базу с окном обслуживания и открывает действие изменения в строке карточки
- **THEN** сохранённые значения видны после создания, изменения и перезагрузки страницы
- **AND** сценарий не использует прямой HTTP API или PostgreSQL для подготовки и проверки состояния

### Requirement: Проверка соответствует характеру изменения

После добавления или изменения поведения теста исполнитель SHALL запустить затронутый тест и убедиться в его успешном результате. Во время реализации и перед завершением изменения исполнитель SHALL выбирать минимальный набор проверок, который покрывает изменённые компоненты и контракты: конкретные unit/envtest, отдельные k3s-сценарии оператора, именованные сценарии общей интеграции, выбранные файлы Vitest и Playwright. Агрегатные `test-full` и полные наборы неизменённых компонентов MUST NOT запускаться без отдельного запроса пользователя.

Линтер, проверка типов, сборка или быстрая компонентная команда SHALL запускаться только для затронутого приложения. Межкомпонентное изменение SHALL проверяться точечными командами каждого затронутого приложения и соответствующими сквозными сценариями, а не всеми сценариями этих наборов. Каждый сбой SHALL быть исследован и исправлен в рабочем коде, тесте или локальной тестовой обвязке. После исправления SHALL успешно завершиться затронутая проверка. Тест MUST NOT отключаться, исключаться из обязательного каталога или ослабляться только ради успешного результата. Итоговый отчёт SHALL перечислять фактически выполненные команды и не утверждать, что полный набор прошёл, если он не запускался.

#### Scenario: Изменено поведение теста

- **WHEN** вместе с именем изменились подготовка, действие или проверки тестового сценария
- **THEN** затронутый тест запускается и успешно завершается

#### Scenario: Межкомпонентное изменение имеет целевые сценарии

- **WHEN** изменение затрагивает API, оператор, web и тестовую обвязку, а новый контракт покрыт выбранными unit/envtest, k3s, общими интеграционными и e2e-сценариями
- **THEN** исполнитель запускает только эти проверки и применимые проверки качества затронутых приложений
- **AND** не запускает `just test-full`, компонентный `test-full` или полный каталог Playwright

#### Scenario: Точечная или отдельно запрошенная полная проверка обнаружила ошибку

- **WHEN** выбранная точечная проверка либо отдельно запрошенный полный набор обнаруживает ошибку
- **THEN** исполнитель исправляет причину и успешно повторяет затронутую проверку
- **AND** полный набор повторяется только тогда, когда его запуск был отдельно запрошен

#### Scenario: Изменены только имена

- **WHEN** изменение ограничено именами тестов и связанными точными ссылками
- **THEN** достаточно компиляции, проверки типов, синтаксиса и согласованности каталога без выполнения тестовых сценариев

#### Scenario: Все локальные наборы прошли

- **WHEN** код, тесты и документация изменения завершены
- **THEN** итоговый отчёт перечисляет затронутые компоненты, выполненные точечные команды и их результаты
- **AND** отдельно сообщает, что полные `test-full` не запускались
