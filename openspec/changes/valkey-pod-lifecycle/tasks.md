## 1. Сохранённая конфигурация запуска

- [ ] 1.1 Добавить сохранённый образ в `ValkeyInstance.status`, записывать его до первого процесса и использовать после потери всех `Pod`; обновить CRD и сгенерированное копирование. Проверить первоначальную запись, перезапуск после неё, смену `VALKEY_IMAGE` и отсутствие сохранённого образа у инстанса с историей тестами в `operator/`.
- [ ] 1.2 Вычислять конфигурацию каждого создаваемого процесса из принятого и применённого размера и сохранённой операции; проверить тестами смешанный состав HA во время изменения размера, неизменность старой `ConfigMap` и восстановление после отсутствия всех процессов.

## 2. Прямое управление `Pod`

- [ ] 2.1 Заменить формирование пользовательского `StatefulSet` отдельными `Pod` с устойчивыми именами, `hostname`/`subdomain`, прямым владельцем CR, `spec.restartPolicy: Never` и блокировкой удаления с момента создания. Проверить тестами отсутствие контейнерных правил перезапуска, сохранность ресурсов, метаданных, сетевой изоляции, размещения и стартового `app off`.
- [ ] 2.2 Перевести события и RBAC на создание принадлежащих CR `Pod`; оставить для пользовательских `StatefulSet` только чтение для диагностики прежних ресурсов. Проверить сгенерированный RBAC и обработку событий `Pod`, не меняя `StatefulSet` самого оператора.
- [ ] 2.3 Добавить создание недостающих процессов с учётом сохранённой стадии, истории остановки и UID владельца. Проверить потерянный ответ создания, `AlreadyExists`, занятое чужим объектом имя, повторные проходы и потерю `Lease`; ни один сценарий не должен создавать лишний процесс или менять чужой объект.
- [ ] 2.4 Обнаруживать прежний пользовательский `StatefulSet` и сообщать `RECOVERY_REQUIRED` без присвоения его `Pod` или удаления контроллера. Проверить отдельными тестами блокировку пользовательского ресурса и отсутствие такой блокировки из-за `StatefulSet` самого оператора.

## 3. Остановка, восстановление и операции

- [ ] 3.1 Связать создание замены с сохранённым доказательством остановки и исчезновением прежнего `Pod`; сохранить ограничения UID/`resourceVersion` при удалении. Проверить повтор после каждой границы записи доказательства, удаления и создания, отсутствие `Pod` при живой ноде и завершение чужой инкарнации.
- [ ] 3.2 Перенести полную остановку `single` и уменьшение RAM HA на прямое удаление `Pod`; проверить продолжение после перезапуска без преждевременных замен, запуск пустого кэша только после доказательств и ожидание ресурсов без подтверждения нового `applied`.
- [ ] 3.3 Перенести последовательное изменение размера HA на прямую замену реплик и основного процесса; проверить синхронизацию после каждой замены, безопасное переключение, отказ основного процесса посреди операции и отсутствие дополнительных процессов сверх трёх.
- [ ] 3.4 Перенести удаление инстанса на прямые `Pod`; проверить приоритет удаления над восстановлением и ротацией, сохранность `Secret` до остановки всех известных процессов и продолжение очистки после перезапуска оператора.

## 4. Проверки оператора и окружения

- [ ] 4.1 Перевести существующие проверки и подготовку ресурсов оператора с пользовательских `StatefulSet` на `Pod`, сохранив каталог сценариев и точки искусственных отказов. По мере реализации запускать только непосредственно затронутые тесты или их небольшие группы после законченного фрагмента работы; полный набор на промежуточных шагах не запускать. Проверить точные ссылки на имена в командах и отчётах.
- [ ] 4.2 Настроить отдельные кластеры `k3s` оператора с явно выключенным `ContainerRestartRules` на сервере и рабочих нодах; убрать обязательность этой возможности из общей подготовки локальных и тестовых кластеров. Проверить фактическую конфигурацию стенда и запуск `Pod`; при изменении общего кода выполнить быстрые проверки API и оператора. Новые сценарии добавлять только в `operator/`.
- [ ] 4.3 На настоящем Kubernetes проверить завершение контейнера с нулевым и ненулевым кодом при остановленном операторе, принудительное завершение, отсутствие повторного запуска и успешную замену после возвращения оператора. Проверить сохранение точной идентичности завершённого процесса до удаления объекта.
- [ ] 4.4 Сохранить и выполнить проверки потери истории, отсутствия `Pod` при живой ноде, уничтожения ноды и возврата ноды после сетевого разделения. Проверить отсутствие двух доступных для записи основных процессов и отказ от `lastState.terminated`, принадлежащего другой инкарнации.
- [ ] 4.5 На прямых `Pod` проверить запрет `Eviction` ниже двух готовых копий HA, разнесение по нодам, ожидание третьей ноды и восстановление полного состава. Выполнить существующие проверки клиентских адресов, TLS и ротации пароля.

## 5. Документация и передача к выпуску

- [ ] 5.1 Обновить `SYSTEM.md`, `SYSTEM-DETAIL.md` и документы оператора: описать прямые `Pod`, сохранение образа, правила остановки и переход с прежней версии с потерей кэша. Проверить, что документация отличает пользовательские процессы от `StatefulSet` оператора и не объявляет ещё не выполненный рабочий выпуск успешным.
- [ ] 5.2 После завершения кода и документации один раз выполнить форматирование затронутого кода без добавления чужих файлов в индекс, проверку исходного кода, сборку, быстрые и полные тесты оператора. Независимые наборы запустить одновременно с максимально допустимым по доступным ресурсам и изоляции стендов параллелизмом. До, во время и после тяжёлых проверок контролировать ресурсы и адресно очищать только собственные завершённые стенды; записать результаты и ограничения проверок.
- [ ] 5.3 Выполнить строгую проверку OpenSpec, сверить реализацию с требованиями, проверить отсутствие секретов и новых общих интеграционных или сквозных сценариев. Синхронизировать основные спецификации после завершения реализации и отметить готовность зависимости в `production-kubernetes-delivery`; рабочую проверку нод, DNS и полный CI/CD с выпуском оставить задачам того изменения.
