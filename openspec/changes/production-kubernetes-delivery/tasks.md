## 1. Разделение изменений OpenSpec

- [x] 1.1 Оставить в `cluster-capacity-placement` только завершённый расчёт вместимости и настройки API, проверить синхронизацию его основных спецификаций, выполнить строгую проверку и перенести изменение в архив без незавершённого раздела `ci-cd`.

## 2. Условия первого выпуска

- [x] 2.1 Проверить доступ к среде выполнения контейнеров рабочих нод и восстановление после замены ноды; зафиксировать общедоступный пакет `ghcr.io/rostislavdugin/managed-valkey-operator` как способ доставки для v1 и отсутствие `imagePullSecrets`.
- [x] 2.2 Рассчитать инфраструктурный резерв 1000m CPU / 2048 MiB на ноду по объявленным запросам системных компонентов и проверить настройки 3 × 4 vCPU / 16 GiB в `Docker Compose`.
- [x] 2.3 Проверить сохранённые намерения в рабочей PostgreSQL без вывода секретов; зафиксировать 0 активных инстансов, 0 незавершённых удалений и нулевой резерв на 11 сентября 2026 года.
- [x] 2.4 Добавить `CLOUDFLARE_API_TOKEN` в окружение GitHub `production`, проверить наличие обязательных секретов без вывода значений и описать их замену в эксплуатационной документации.
- [ ] 2.5 Добиться работы `ContainerRestartRules` на всех рабочих нодах либо согласовать отдельную переработку остановки Valkey; проверить каждую ноду служебным `Pod` и убедиться, что проверочные ресурсы удалены.

## 3. Сборка и подготовка Kubernetes

- [x] 3.1 Завершить сборку образа оператора для `linux/amd64`, публикацию с тегом SHA и обязательную проверку результата; подтвердить метаданные образа и скачивание из общедоступного пакета без учётных данных.
- [x] 3.2 Подготовить CRD, RBAC, сетевые ресурсы, TLS и закреплённые версии `Gateway API`, `Envoy Gateway` и `cert-manager`; проверить результат обработки `Kustomize` и отсутствие секретов в нём.
- [ ] 3.3 Проверить на рабочем кластере порядок применения компонентов и управляемую замену `Pod` при `OnDelete`; подтвердить готовность оператора и точный SHA его образа, не создавая пользовательский `ValkeyInstance`.
- [ ] 3.4 Создать отдельный файл подключения Kubernetes для API, проверить ограниченные права, режим `0600`, монтирование только для чтения и удаление временных файлов при успешном и прерванном выпуске.

## 4. Включение рабочего сервиса

- [ ] 4.1 Получить внешний адрес Envoy, направить `*.valkey.h3llo-demo.com` на него в режиме `DNS only` и повторить выпуск; проверить совпадение DNS, готовность TLS-сертификата и состояние шлюза.
- [ ] 4.2 Передать выпуск на сервер, применить миграции и запустить API с `KUBERNETES_SYNC_ENABLED=true`; проверить доступ API к Kubernetes и внешний `/readyz` до записи `/opt/managed-valkey/.deployed-sha`.
- [x] 4.3 Повторить выпуск после искусственного отказа между подготовкой Kubernetes и обновлением API; подтвердить идемпотентность и сохранность пользовательских ресурсов средствами сценарных тестов выпуска, не расширяя общую интеграцию или сквозные тесты.

## 5. Документация и итоговая проверка

- [x] 5.1 Обновить `SYSTEM.md` и руководство по рабочему окружению: описать секреты, порядок первого выпуска, ручную настройку DNS, замену учётных данных и откат; сверить текст с фактическими командами.
- [ ] 5.2 Запустить проверки сценариев выпуска и синтаксиса командных файлов, строгую проверку OpenSpec и просмотр итоговых изменений; подтвердить отсутствие секретов, тестовых пользовательских CR и новых проверок в общей интеграции или сквозных тестах.
