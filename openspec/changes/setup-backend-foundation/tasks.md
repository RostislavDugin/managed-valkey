## 1. Go-модуль и инструменты

- [ ] 1.1 Создать корневой модуль `github.com/RostislavDugin/managed-valkey`, закрепить Go 1.27.1 и прямые runtime-зависимости из дизайна; проверить `go mod tidy`, `go mod verify` и отсутствие вложенных `go.mod`.
- [ ] 1.2 Добавить через `go get -tool` закреплённые Air, `goose`, golangci-lint, controller-gen и совместимый `setup-envtest`; проверить запуск версии каждого инструмента через `go tool` без глобальной установки.
- [ ] 1.3 Создать `api/.golangci.yml` и `operator/.golangci.yml` на основе Databasus: `standard` плюс `funcorder`, `bodyclose`, `errorlint`, `gocritic`, `unconvert`, `misspell`, `errname`, `noctx`, `modernize`; включить проверку type assertions и форматтеры `gofumpt`, `golines` на 120 символов и `gci`; проверить оба файла golangci-lint v2 и общим module path проекта.
- [ ] 1.4 Проверить, что в корне нет `Justfile`, а `just --list` в `api` и `operator` показывает только согласованные публичные команды после их создания.

## 2. Общая основа и API

- [ ] 2.1 Реализовать `internal/logging` с уровнем из env, text stdout на dev, JSON stdout на prod и безопасным fan-out в OTLP; покрыть тестами атрибуты `service.name`/`env`, уровни и продолжение работы без `VL_OTLP_URL`.
- [ ] 2.2 Добавить разбор обязательной конфигурации API, подключение GORM к PostgreSQL и явное закрытие пула; проверить тестами ошибку пустого `DATABASE_URL` и недоступной базы.
- [ ] 2.3 Создать `api/cmd/api` на `gin.New()` с recovery, `request_id`, журналом метаданных запроса, `/livez` и `/readyz`; проверить HTTP-тестами успешную liveness, зависимость readiness только от PostgreSQL и отсутствие тел и секретных заголовков в логах.
- [ ] 2.4 Реализовать завершение API через `signal.NotifyContext`, `http.Server.Shutdown`, закрытие базы и flush логов с таймаутами; проверить тестом, что сервер перестаёт принимать новые соединения и завершает начатый запрос.
- [ ] 2.5 Добавить `.air.api.toml`, ограничив наблюдение каталогами `api` и общим `internal`; проверить, что изменение API вызывает пересборку, а изменение файла в `web` не перезапускает процесс.
- [ ] 2.6 Создать `api/Justfile` с публичными `migration-up`, `migration-down`, `migration-generate name`, `test`, `lint`, `run` и `build`; `lint` должен форматировать и проверять API и используемый им общий код, а закрытый `_lint-check` только проверять; сверить список через `just --list`, выполнить `just lint`, `just build` и `just run`.
- [ ] 2.7 Настроить `just test` в `api` как единый набор unit- и PostgreSQL-тестов без деления на fast/full; проверить успешный прогон на мигрированной `managed_valkey_test` без полного provisioning Kubernetes.

## 3. Каркас оператора Kubebuilder

- [ ] 3.1 Сгенерировать эталон Kubebuilder v4.13.1 во временном каталоге с группой `valkey.h3llo-demo.com/v1alpha1`, сохранить сведения о версии и проверить, что эталон использует `controller-runtime v0.23.3`.
- [ ] 3.2 Адаптировать manager, тип `ValkeyInstance`, DeepCopy, начальный reconciler, CRD и RBAC к `operator/cmd/operator`, `operator/api/v1alpha1`, `operator/internal/operator` и `operator/config`; проверить отсутствие вложенного модуля, Kubebuilder Makefile и демонстрационных ресурсов.
- [ ] 3.3 Сделать начальный reconcile безопасной пустой операцией без записи `status` и создания объектов; проверить через `envtest`, что создание `ValkeyInstance` не меняет ресурс и не создаёт зависимостей.
- [ ] 3.4 Настроить manager с leader election, `/healthz`, `/readyz`, общим logger и завершением по контексту; проверить `envtest` запуска, готовности и остановки manager.
- [ ] 3.5 Добавить закрытые рецепты генерации DeepCopy, CRD и RBAC закреплённым `controller-gen`; дважды выполнить их через публичные команды и проверить отсутствие diff после второго запуска.
- [ ] 3.6 Добавить `.air.operator.toml`, ограничив наблюдение каталогами `operator` и общим `internal`; проверить независимую пересборку оператора при изменении его исходников.
- [ ] 3.7 Создать `operator/Justfile` с публичными `test-fast`, `test-full`, `lint`, `build` и `run`; `lint` должен форматировать и проверять оператор и используемый им общий код, а закрытый `_lint-check` только проверять; сверить список через `just --list` и отсутствие миграционных команд.
- [ ] 3.8 Настроить `just test-fast` в `operator` для unit-тестов и `envtest` без настоящих подов Valkey; проверить, что команда не требует работающего dev compose и использует Kubernetes API ветки 1.35.
- [ ] 3.9 Настроить `just run` в `operator`: собрать dev-образ, загрузить его в ноды k3s, применить CRD и манифесты и дождаться готовности StatefulSet; повторить команду и проверить обновление только оператора.
- [ ] 3.10 Настроить `just test-full` в `operator` для установки текущей сборки, тестов с dev k3s, реальных изменений Kubernetes и ожидания provisioning с явными таймаутами; проверить каркас набора без реализации будущих failover-сценариев.

## 4. Миграции PostgreSQL

- [ ] 4.1 Создать корневой `migrations` с технической транзакционной миграцией `baseline`, которая не добавляет предметные таблицы; применить её к чистой PostgreSQL 18 и проверить запись версии `goose`.
- [ ] 4.2 Реализовать `just migration-generate <name>` в `api` на закреплённом `goose` и корневом `../migrations`; проверить создание timestamp-файла с переданным именем во временной копии каталога.
- [ ] 4.3 Реализовать `just migration-up` и `just migration-down` в `api`; проверить применение с нуля, безопасный повтор `up` и откат ровно одной последней миграции.
- [ ] 4.4 Добавить проверку формата и соглашений SQL для новых миграций, включая UUIDv7, `TIMESTAMPTZ`, имена булевых колонок и раздельные ограничения; проверить её на корректном файле и временном нарушении.
- [ ] 4.5 Настроить `managed_valkey_test` и применение миграций перед `api just test`; проверить тестом реальной базы откат ошибочной транзакционной миграции без записи её версии.

## 5. Образы приложений

- [ ] 5.1 Создать многоступенчатый `api/Dockerfile` с целями `dev`, `build`, `runtime` и `migrate`, корневым контекстом и закреплёнными базовыми образами; собрать все цели и проверить запуск runtime от non-root и отсутствие Go toolchain в нём.
- [ ] 5.2 Создать многоступенчатый `operator/Dockerfile` с целями `dev`, `build` и `runtime`; собрать все цели и проверить запуск runtime от non-root и наличие CA-сертификатов.
- [ ] 5.3 Создать `caddy/Dockerfile` с закреплённым `mholt/caddy-ratelimit` и `caddy/Caddyfile` по `SYSTEM.md`; собрать образ и проверить валидность конфигурации командой Caddy.
- [ ] 5.4 Проверить контексты и `.dockerignore`, чтобы сборки получали общий Go-модуль, но не копировали `.git`, локальные секреты, временные бинарники и кэши; подтвердить состав образов через `docker history` и запуск контейнеров.

## 6. Dev-окружение

- [ ] 6.1 Создать `.env.example` со всеми переменными из `SYSTEM.md`, обязательными пустыми секретами и безопасными dev-значениями; проверить, что `.env` игнорируется Git и пример не содержит рабочего секрета или закрытого ключа.
- [ ] 6.2 Создать `docker-compose.dev.yml` для PostgreSQL 18, VictoriaLogs, `migrate`, API и оператора с healthcheck, volumes и зависимостями запуска; проверить разрешённую конфигурацию через `docker compose config`.
- [ ] 6.3 Добавить k3s-server и два k3s-agent с закреплённой версией, выключенными Traefik и servicelb, общим token и нужными портами; запустить их и проверить три готовые ноды через `kubectl get nodes`.
- [ ] 6.4 Создать идемпотентный k3s-bootstrap с закреплёнными Gateway API, Envoy Gateway и исходными манифестами `deploy/dev`; дважды запустить bootstrap и проверить одинаковый успешный результат без удаления ресурсов.
- [ ] 6.5 Создать отдельные RBAC и kubeconfig API и оператора в Docker volumes; командами `kubectl auth can-i` проверить разрешённые действия и запрет API на `status` и поды.
- [ ] 6.6 Настроить dev-сертификат и NodePort `41379` по `SYSTEM.md`, не сохраняя закрытый ключ в Git; проверить наличие TLS Secret и доступность опубликованного порта.
- [ ] 6.7 Подключить API и оператор к Air, kubeconfig и VictoriaLogs по схеме dev; проверить `/livez`, `/readyz`, probes оператора и появление записей обоих `service.name` в vmui.
- [ ] 6.8 Запустить и остановить окружение прямыми командами `docker compose -f docker-compose.dev.yml`; проверить отсутствие корневой обёртки `just` и сохранение именованных volumes при обычной остановке.

## 7. Production-окружение

- [ ] 7.1 Создать `docker-compose.prod.yml` для PostgreSQL, `migrate`, API, VictoriaLogs и Caddy с портами и порядком запуска из `SYSTEM.md`; проверить `docker compose config` с тестовым env-файлом.
- [ ] 7.2 Проверить остановку запуска API при ошибке `migration-up` и запуск после исправления миграции; подтвердить статусами контейнеров и ответом `/readyz`.
- [ ] 7.3 Создать `deploy/prod` с Kustomize, StatefulSet оператора, ServiceAccount, RBAC, leader election, probes и ссылками на сгенерированный CRD; выполнить `kustomize build` и проверить отсутствие `DATABASE_URL` и адреса API в конфигурации оператора.
- [ ] 7.4 Запустить и остановить окружение прямыми командами `docker compose -f docker-compose.prod.yml`; проверить, что dev-проект и его volumes не затронуты.
- [ ] 7.5 Проверить, что production compose, Dockerfile и манифесты не используют `latest`, а внешние образы имеют конкретные версии и допускают фиксацию digest; сохранить итоговую проверку в CI-пригодном скрипте.

## 8. TODO, инструкции и итоговая проверка

- [ ] 8.1 Создать короткий `operator/TODO.md` со сценариями остановки primary pod, ожидания нового primary и отдельной проверки потери worker-ноды с fencing; проверить, что файл не подменяет реализацию теста подробным планом.
- [ ] 8.2 Создать короткий `e2e/TODO.md` со сценариями регистрации пользователя, создания Valkey через API, подключения и запросов каждые 100 мс во время остановки primary pod до восстановления; проверить наличие критериев временной ошибки и возврата успешных запросов.
- [ ] 8.3 Создать `api/AGENTS.md` со ссылками на корневые инструкции и `SYSTEM.md`, адаптировать правила spacing, comments, UTC, `slog`, GORM, `goose` и современного Go; проверить отсутствие чужих имён, обязательного Swagger и неподходящей DI-схемы из исходного гайда.
- [ ] 8.4 Создать `operator/AGENTS.md` с теми же общими правилами Go и отдельными правилами `controller-runtime`, идемпотентного reconcile и запрета PostgreSQL/API; проверить ссылки и отсутствие инструкций по миграциям как обязанности оператора.
- [ ] 8.5 Создать относительные символические ссылки `api/CLAUDE.md -> AGENTS.md` и `operator/CLAUDE.md -> AGENTS.md`; проверить тип каждой ссылки, её цель и чтение итогового файла через оба пути.
- [ ] 8.6 Расширить `.githooks/pre-commit`: для staged Go-файлов запускать форматирование и возвращать только их в индекс, затем выполнять `_lint-check`, `build` и `go mod tidy -diff` для затронутых приложений; общие Go-файлы и модульные файлы должны проверять оба приложения, а любая ошибка или замечание линтера должны отменять коммит.
- [ ] 8.7 Проверить hook временным неформатированным Go-файлом, который после успешных проверок попадает в индекс уже исправленным; отдельно проверить, что ошибка линтера, расхождение `go mod tidy -diff` и ошибка сборки отменяют коммит без добавления посторонних незакоммиченных файлов.
- [ ] 8.8 Обновить корневой `AGENTS.md` с описанием проверок frontend и Go, а разделы 14 и 16 `SYSTEM.md` с локальными командами `lint`, остальными командами приложений и корневым каталогом `e2e`; проверить согласованность всех упоминаний команд поиском.
- [ ] 8.9 Запустить `go mod verify`, `just lint` и тесты из обоих приложений, сборку обоих приложений, сборку трёх production-образов и проверку обоих compose-файлов; устранить все ошибки без расширения продуктового поведения.
- [ ] 8.10 Запустить полный dev compose, `just run` и каркас `just test-full` из `operator`; проверить PostgreSQL, baseline `goose`, три ноды k3s, bootstrap, API, оператор и VictoriaLogs, затем штатно остановить окружение и убедиться, что в логах нет секретов.
- [ ] 8.11 Просмотреть итоговый diff, проверить отсутствие корневого `Justfile` и выполнить `openspec validate setup-backend-foundation --strict`.
