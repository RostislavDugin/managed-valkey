## 1. Конфигурация API

- [x] 1.1 Ввести явную топологию и инфраструктурный резерв, целочисленные единицы и проверки переполнения; проверить тестами конфигурации в `api/` случай 3 × 4 / 16, дробный доступный CPU, нулевой остаток, пропущенные и устаревшие переменные окружения.
- [x] 1.2 Обновить передачу вычисленного бюджета в сервис и все настройки запуска API; проверить поиск старых переменных окружения, разбор `Docker Compose` и сохранение прежних тестовых бюджетов. В общей интеграции и сквозных тестах менять только обязательную конфигурацию запуска, не добавлять сценарии.

## 2. Проверка размещения

- [x] 2.1 Реализовать точный расчёт существования распределения CPU/RAM с разнесением HA и отменой поиска; проверить модульными тестами в `api/` точные границы, фрагментацию, две ноды для HA, смешанные `single`/HA и случай, требующий возврата после неудачной жадной раскладки.
- [x] 2.2 Проверить корректность поиска сравнением с независимым полным перебором на небольших наборах и ограничить время на наборах до 32 инстансов; сохранить выбранный срок и результаты измерений в `design.md`, проверить различие невозможности и таймаута.
- [x] 2.3 Включить расчёт в создание и изменение размера под общей транзакционной блокировкой после существующих проверок; проверить HTTP-тестами в `api/` приоритет личной/общей квоты и атомарность отказа по размещению.
- [x] 2.4 Согласовать чтение резервов с подтверждениями применённых размеров и `deleted_at` в импорте, сохранить максимум желаемых и применённых размеров и возможность уменьшения превышенного бюджета; проверить тестами в `api/` освобождение только после подтверждения и отсутствие двойного резерва при изменении размера.
- [x] 2.5 Добавить контракт `placement_capacity` и `placement_check_timeout`; проверить HTTP-ответы в `api/`, единицы измерения, нулевой общий `missing` при фрагментации и отсутствие сведений о чужих инстансах.

## 3. Регрессии API

- [x] 3.1 Дополнить HTTP-тесты конкурентным созданием через два API-процесса и гонкой создания и изменения размера за последнее размещаемое место; проверить ровно один успех, отсутствие частичных строк при отказе и корректный идемпотентный повтор.
- [x] 3.2 Дополнить тесты API удержанием резервов при `error`, устаревшем наблюдении, уменьшении и DELETE, а также последующим освобождением; новые пользовательские операции выполнять через TCP HTTP, состояние подтверждений готовить средствами тестов API.
- [x] 3.3 Выполнить форматирование только своих файлов, проверку средством статического анализа и быстрый набор API; для затронутого импорта выполнить соответствующие существующие API-тесты синхронизации. Проверить, что новые тесты находятся только в `api/`, без расширения общей интеграции, сквозных тестов или тестов оператора.

## 4. Подготовка решений для рабочего окружения

- [x] 4.1 Проверить доступ к среде выполнения контейнеров нод и восстановление после замены ноды; зафиксировать общедоступный пакет `ghcr.io/rostislavdugin/managed-valkey-operator` как способ доставки для v1. Оператор остаётся внутри Kubernetes, кластер скачивает образ без `imagePullSecrets`.
- [ ] 4.2 Определить внешний адрес Envoy, DNS, выпуск и продление TLS, учётные данные API и инфраструктурный резерв; подтвердить `ContainerRestartRules` на каждой рабочей ноде, проверить доступность необходимых параметров без вывода секретов и записать выбранные значения/процедуры в эксплуатационную документацию.
- [x] 4.3 Проверить существующие намерения в рабочем окружении перед включением синхронизации и их расчётную вместимость; результатом должен быть отчёт без секретов и согласованный порядок действий при превышении, без удаления чужих данных.

## 5. Доставка Kubernetes-компонентов

- [ ] 5.1 Добавить сборку оператора для `linux/amd64`, публикацию в `ghcr.io/rostislavdugin/managed-valkey-operator` с тегом SHA выпуска и обязательный результат в итоговую проверку; ограничить `GITHUB_TOKEN` правом `packages: write`, проверить метаданные образа и общедоступное скачивание без учётных данных.
- [x] 5.2 Исправить сборку манифестов рабочего окружения, включить CRD, API RBAC и закреплённые Gateway API/Envoy зависимости; проверить результат обработки `Kustomize` и отсутствие учётных данных в нём.
- [ ] 5.3 Реализовать скачивание образа оператора из общедоступного пакета `GHCR`, применение компонентов в правильном порядке и обновление оператора с учётом `OnDelete`; проверить SHA запущенного образа и его готовность, не создавая тестовые пользовательские CR.
- [ ] 5.4 Подключить `PRODUCTION_KUBECONFIG` и отдельный файл подключения к Kubernetes для API, включить синхронизацию в `Docker Compose`; проверить режим файлов, монтирование только для чтения, права API и очистку временных учётных данных при успехе и ошибке.
- [ ] 5.5 Дополнить установку выпуска проверками Kubernetes и подключения API до записи успешного SHA; проверить синтаксис сценария, разобрать путь частичного отказа и повторного запуска, подтвердить готовность при согласованной выкладке в рабочее окружение без добавления сценариев общей интеграции или сквозных тестов.

## 6. Документация и завершение

- [ ] 6.1 Обновить `SYSTEM.md` и документацию развёртывания: новые переменные окружения, расчётный характер размещения, ошибки, секреты, порядок выпуска и отката; проверить согласованность с реализованным контрактом и закрытие решений раздела 4.
- [ ] 6.2 Проверить итоговые изменения и файлы OpenSpec; подтвердить отсутствие несогласованных изменений тестовых наборов, секретов и изменений пользовательских ресурсов.
