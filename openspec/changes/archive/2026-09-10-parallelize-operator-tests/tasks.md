## 1. Очистка и разделение работы

- [x] 1.1 Первым действием реализации проверить остатки прошлых запусков, сохранить полезные журналы, установить отсутствие владельцев и выполнить адресную очистку; записать список очищенных ресурсов, остатки и доступную RAM до и после, подтвердить сохранность dev и активных запусков.
- [x] 1.2 Согласовать формат каталога, входы/выходы сценария, бюджет и владение файлами; сохранить договорённости и запустить параллельно агентов по направлениям 2, 3 и 4, оставив ведущему направление 5. Каждый агент подтверждает чтение AGENTS.md и humanizer. Цель реализации с проверками около часа; зафиксировать время начала и сообщать о причинах превышения, не пропуская обязательную работу.

Результат 1.1 от 2026-09-10: проверено 12 каталогов `tmp/k3s` со статусом
`cleaned`; записанные PID завершены. Тестовых контейнеров, сетей и маршрутов нет.
Каталоги с журналами сохранены. `managed-valkey-dev` работает. Доступная RAM до
и после проверки составляла около 15 GiB; ресурсы тестов очищать не потребовалось.

## 2. Топологии и сценарии, агент A

- [x] 2.1 Классифицировать сценарии по минимальным топологиям и зависимости от Envoy, передать таблицу агенту B; проверить каждое назначение чтением теста и отметить независимые части составных тестов.
- [x] 2.2 Поддержать bootstrap и проверку стендов на 1–4 нодах, профиль Envoy с одной или двумя репликами; целевыми прогонами подтвердить single, перенос single, HA и HA с запасом, записать время подготовки и пиковую память.
- [x] 2.3 Устранить искусственные требования лишних нод и разделить независимые длинные сценарии; проверить сохранение исходных утверждений, размещения и межрежимных проверок, выполнить затронутые сценарии.

Результат 2.1 и 2.2 от 2026-09-10: каталог содержит 62 сценария с проверенными
минимальными топологиями. Составные тесты с независимыми режимами и стадиями
разделены на отдельные верхнеуровневые тесты.

| Сценарий | Ноды / Envoy | Подготовка до `ready` | Сам тест | Весь запуск | Пик памяти контейнеров |
| --- | ---: | ---: | ---: | ---: | ---: |
| `pw01-rz01-single-mutation` | 1 / 1 | 178 с с ожиданием слота | 46,67 с | 234 с | 874 MiB |
| `nd03-single-worker-loss` | 2 / 1 | 104 с | 134,02 с | 255 с | 1214 MiB |
| `ha05-pdb` | 3 / 1 | 95 с | 59,51 с | 180 с | 1200 MiB |
| `ct14-ha03-capacity` | 4 / 2 | 116 с | 49,26 с | 195 с | 1384 MiB |

Все четыре сценария прошли параллельно. Минимальный `MemAvailable` общего
запуска составил 30067 MiB. После каждого стенда осталось по нулю контейнеров,
сетей, томов, маршрутов и процессов; общий резерв памяти освобождён.

Результат 2.3 от 2026-09-10: разделённые сценарии SIGKILL, SIGSTOP/SIGCONT,
OP-05 и DL-01 прошли как отдельные тесты. Сценарии ND-03, PW-07, DL-02 и DL-03
прошли на уменьшенных топологиях. Повтор ND-02 подтвердил ожидание готовности
containerd после возврата чистой ноды. Повтор шести затронутых сценариев занял
от 163 до 327 секунд на сценарий при параллельном запуске; после очистки их
ресурсов не осталось. CT-09 допускает штатное создание нового Pod объектом
`StatefulSet`, но проверяет, что без Secret процесс Valkey в нём не запускается.
Целевой повтор CT-09 прошёл за 109,35 секунды, весь запуск занял 228 секунд.

## 3. Каталог и отчёт, агент B

- [x] 3.1 Создать единый каталог с точными именами, топологиями и оценками ресурсов; устранить расхождение имени ND-07/08/09 и проверить соответствие всем обнаруженным тестам и исходной матрице.
- [x] 3.2 Реализовать отказ до bootstrap при пропущенном, неизвестном или повторном назначении; проверить эти случаи, включая независимые подтесты и пустой выбор.
- [x] 3.3 Объединять результаты сценариев без смешивания журналов, показывать режим, длительности и полноту; проверить падение, skip, отсутствующий результат и сохранение исходного сбоя после повтора.

## 4. CI и документация, агент C

- [x] 4.1 Реализовать явный ограниченный режим unit/envtest и подключить его к test-operator; проверить отсутствие вызовов bootstrap/сборки аварийного образа, успешный режим и ненулевой результат при отказе подготовки или теста.
- [x] 4.2 Проверить зависимости build/deploy и сохранение диагностики; подтвердить, что ошибка обязательного теста блокирует последующие этапы, а ограниченный отчёт не заявляет полную матрицу.
- [x] 4.3 Сверить SYSTEM.md, SYSTEM-DETAIL.md, AGENTS.md, operator/AGENTS.md и основные спеки с реализацией; обновить описания топологий и команд, убрать отметки о нереализованности только для проверенного поведения, выполнить поиск противоречащих требований CI.

Результат 4.1 и 4.2 от 2026-09-10: последний контрольный `operator just test-ci`
выполнил unit за 5 секунд и envtest за 75 секунд без k3s; эти длительности
попали в общий отчёт. Отдельно проверены ошибки подготовки, unit и envtest:
команда вернула ненулевой код, записала `status=failed` и сохранила журналы.
Workflow прошёл `actionlint`; `build-api` и `build-web` зависят от
`test-operator`, а `deploy` зависит от обеих сборок.

Результат 4.3 от 2026-09-10: целевые прогоны подтвердили кластеры
из 1, 2, 3 и 4 нод с одной или двумя репликами Envoy. Каждый стенд
после завершения оставил ноль контейнеров, сетей, томов, маршрутов и
процессов. Описания команд, топологий, порядка подготовки, CI и значений
по умолчанию сверены с кодом; противоречащих требований CI не найдено.

## 5. Запускатель и бюджет, ведущий агент

- [x] 5.1 Реализовать очередь независимых сценариев, однократную сборку бинарника/образа и ограниченную подготовку кластеров; проверить одновременное выполнение разных стендов и отсутствие повторной сборки на каждый сценарий.
- [x] 5.2 Реализовать общий учёт резервов на хосте с другими активными наборами и явные ограничения пользователя; проверить конкурирующие запросы, ожидание освобождения и отказ для сценария, который не помещается даже один.
- [x] 5.3 Регистрировать владельцев и ресурсы до подготовки, реализовать отмену с ожиданием потомков и адресную повторную очистку; проверить отмену во время bootstrap и сетевой аварии, отсутствие новых ресурсов после завершения отмены и ненулевой результат при ошибке очистки.
- [x] 5.4 Реализовать обнаружение осиротевших запусков с защитой от повторного использования PID; проверить очистку доказанных остатков, сохранение активного владельца и отказ от удаления при неопределённой принадлежности.

Результат 5.2 и 5.4 от 2026-09-10: общий реестр резервов проверен на
одновременных запросах operator и API, ожидании освобождения, превышении всего
бюджета и устаревшей записи. Адресная очистка проверена на активном владельце,
завершённом процессе, повторном использовании PID, неопределённой принадлежности
и ошибке штатной очистки. Старые каталоги со статусом `cleaned` сохранены.

Результат 5.1 от 2026-09-10: быстрый тест запускателя подтвердил очередь с
ограничением параллелизма, одновременную работу разных сценариев и единственный
вызов каждого этапа подготовки. Четыре настоящих стенда 1–4 ноды также прошли
одновременно после одной сборки бинарника и образов.

Результат 5.3 от 2026-09-10: отмена во время bootstrap завершила всю группу
процессов с кодом 143 и не оставила контейнеров, сетей, томов, маршрутов или
процессов сразу после завершения и через пять секунд. CT-12 за 331 секунду
подтвердил сохранение диагностики прерванного запуска, адресное удаление
временной четвёртой ноды и снятие сетевых ограничений. Отдельный тест ошибки
штатной очистки вернул ненулевой код и сохранил окружение для диагностики.

## 6. Объединение и проверка

- [x] 6.1 Объединить результаты параллельных направлений и провести небольшой совместный прогон, включая соседний API; записать RAM/CPU, время подготовки и очистки, выбрать значения бюджета и предела bootstrap без превышения ресурсов машины.
- [ ] 6.2 Выполнить один полный локальный operator just test после целевых проверок агентов; подтвердить полную матрицу, отсутствие пропусков и остаточных ресурсов, сохранить время запуска и самые долгие сценарии. Повторять полный набор только после влияющих изменений, ошибок или нерешённых сомнений.
- [x] 6.3 Сопоставить длительность с ориентиром около 10 минут; при превышении перечислить основные затраты и применить только соразмерные улучшения, зафиксировать итог без ослабления проверок.
- [ ] 6.4 Провести итоговый review кода и согласованности документации, выполнить требуемые проверки затронутых частей и строгую валидацию OpenSpec; сообщить фактическое время реализации, результаты и оставшиеся ограничения. Не отмечать непроверенные задачи выполненными ради часового ориентира.

Результат 6.1 от 2026-09-10: `api just test` и однонодовый сценарий оператора
прошли одновременно за 168 секунд. Минимальный `MemAvailable` составил
33837 MiB, пик занятого общего резерва 5120 MiB, наблюдаемый пик загрузки CPU
18,5%. Оба окружения получили статус `cleaned`; резерв, контейнеры и маршруты
запуска не остались. По замерам выбран бюджет 28672 MiB с сохранением не менее
3072 MiB доступной памяти, до 12 сценариев и до 6 одновременных bootstrap.

Результат 6.3 от 2026-09-10: первая попытка полного запуска с
`MV_TEST_MAX_PARALLEL_BOOTSTRAPS=6` заняла 34:05 и выявила четыре ошибки кода
или тестов. Повтор с `MV_TEST_MAX_PARALLEL_BOOTSTRAPS=10` занял 37:33: прошли
60 из 62 сценариев, два стенда завершились ошибкой загрузки образа. Минимальный
наблюдаемый `MemAvailable` составил около 19 GiB, пик занятого резерва достиг
28160 MiB. Предел в десять одновременных подготовок не ускорил запуск; в этом
же запуске два стенда не загрузили образ. Поэтому значение по умолчанию остаётся
равным шести. Дальнейшее увеличение параллелизма ради ориентира в 10 минут
признано несоразмерным.

Финальный полный запуск остановлен по решению пользователя после 7 из 62
сценариев; все семь завершённых сценариев прошли. Поэтому задачи 6.2 и 6.4
остаются незавершёнными при архивировании change.
