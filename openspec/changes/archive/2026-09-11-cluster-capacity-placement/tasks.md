## 1. Конфигурация API

- [x] 1.1 Ввести явную топологию и инфраструктурный резерв, целочисленные единицы и проверки переполнения; проверить тестами конфигурации в `api/` случай 3 × 4 / 16, дробный доступный CPU, нулевой остаток, пропущенные и устаревшие переменные окружения.
- [x] 1.2 Обновить передачу вычисленного бюджета в сервис и все настройки запуска API; проверить поиск старых переменных окружения, разбор `Docker Compose` и сохранение прежних тестовых бюджетов. В общей интеграции и сквозных тестах менять только обязательную конфигурацию запуска, не добавлять сценарии.

## 2. Проверка размещения

- [x] 2.1 Реализовать точный расчёт существования распределения CPU/RAM с разнесением HA и отменой поиска; проверить модульными тестами в `api/` точные границы, фрагментацию, две ноды для HA, смешанные `single`/HA и случай, требующий возврата после неудачной жадной раскладки.
- [x] 2.2 Проверить корректность поиска сравнением с независимым полным перебором на небольших наборах и ограничить время на наборах до 32 инстансов; сохранить выбранный срок и результаты измерений в `design.md`, проверить различие невозможности и истечения срока.
- [x] 2.3 Включить расчёт в создание и изменение размера под общей транзакционной блокировкой после существующих проверок; проверить HTTP-тестами в `api/` приоритет личной/общей квоты и атомарность отказа по размещению.
- [x] 2.4 Согласовать чтение резервов с подтверждениями применённых размеров и `deleted_at` в импорте, сохранить максимум желаемых и применённых размеров и возможность уменьшения превышенного бюджета; проверить тестами в `api/` освобождение только после подтверждения и отсутствие двойного резерва при изменении размера.
- [x] 2.5 Добавить контракт `placement_capacity` и `placement_check_timeout`; проверить HTTP-ответы в `api/`, единицы измерения, нулевой общий `missing` при фрагментации и отсутствие сведений о чужих инстансах.

## 3. Регрессии API

- [x] 3.1 Дополнить HTTP-тесты конкурентным созданием через два API-процесса и гонкой создания и изменения размера за последнее размещаемое место; проверить ровно один успех, отсутствие частичных строк при отказе и корректный идемпотентный повтор.
- [x] 3.2 Дополнить тесты API удержанием резервов при `error`, устаревшем наблюдении, уменьшении и DELETE, а также последующим освобождением; новые пользовательские операции выполнять через TCP HTTP, состояние подтверждений готовить средствами тестов API.
- [x] 3.3 Выполнить форматирование только своих файлов, проверку средством статического анализа и быстрый набор API; для затронутого импорта выполнить соответствующие существующие API-тесты синхронизации. Проверить, что новые тесты находятся только в `api/`, без расширения общей интеграции, сквозных тестов или тестов оператора.
- [x] 3.4 Обновить `.env.example`, настройки `Docker Compose` и документацию API для новой топологии и ошибок размещения; проверить отсутствие прежних переменных общей ёмкости и согласованность с основными спецификациями.
