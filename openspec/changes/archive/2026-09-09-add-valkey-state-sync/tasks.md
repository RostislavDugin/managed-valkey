## 1. Схема и общий контракт

- [x] 1.1 Добавить миграцию `goose`, которая расширяет схему с UID `Namespace`/CR, этапом удаления, отдельными причинами восстановления, таблицами `valkey_instance_nodes` и `valkey_instance_phase_events`; проверить порядок таблиц, внешних ключей и индексов, а применение с `-allow-missing` проверить штатным запуском API-тестов.
- [x] 1.2 Добавить GORM-модели по одной на таблицу и служебные поля инстанса; проверить явные теги колонок и сохранение существующих значений, включая `is_recovery_required`, на интеграционных сценариях.
- [x] 1.3 Расширить фазы CRD значениями `updating` и `degraded`, вынести необходимые общие константы защитной отметки удаления и меток в публичный контракт и обновить генерацию; проверить принятие фаз настоящим Kubernetes API и отсутствие расхождения сгенерированных файлов.

## 2. Репозитории и запуск циклов

- [x] 2.1 Добавить выборку всех неудалённых инстансов и свежее чтение одного намерения, запись UID и этапов с проверкой прежних значений; проверить конфликт идентичности и неизменность `updated_at` через реальные PostgreSQL-транзакции в сценариях синхронизации.
- [x] 2.2 Реализовать атомарную запись наблюдений, нод и истории с явными колонками и согласованным порядком блокировок; проверить откат транзакции при отказе записи события и отсутствие перезаписи нового HTTP-намерения.
- [x] 2.3 Реализовать два независимых цикла в `api/internal/sync`, первый проход сразу и `SyncInterval=time.Second`, ограниченное число работников и отменяемые запросы; проверить управляемыми часами интервал, отсутствие перекрытия/накопления проходов и продолжение обработки после ошибки одного инстанса.
- [x] 2.4 Подключить Kubernetes-клиент и циклы в точке входа API, завершать их до закрытия PostgreSQL; проверить запуск с недоступным Kubernetes, сохранение готовности HTTP API и остановку активных обращений по контексту.

## 3. Доставка намерений

- [x] 3.1 Реализовать проверки имён, ID, меток и UID, создание `Namespace` → `Secret` → CR с защитной отметкой удаления и сохранение идентичности; проверить через HTTP и k3s первоначальную доставку и повтор после потерянного ответа `Create`.
- [x] 3.2 Доставлять размер, `whitelist`, порт, режим и поколение с проверкой `resourceVersion` и свежим чтением БД при конфликте; проверить увеличение/уменьшение размера, пустой `whitelist`, `false`, сохранение служебных полей и отсутствие записей при совпадающем состоянии.
- [x] 3.3 Реализовать доставку версионного хеша до изменения CR с сохранением прежнего хеша и служебных ключей; проверить прерывание между `Secret` и CR, конфликт одной версии с другим хешем и отсутствие возврата очищенного старого хеша.
- [x] 3.4 Обрабатывать удаляемый CR, потерю `Secret`/CR, неполные служебные поля и чужие объекты через восстановление без пересоздания; проверить неизменность объектов и корректное сохранение/снятие причин восстановления.
- [x] 3.5 Добавить сверку принадлежащих сервису `Namespace` без строки БД и сообщение для ручного разбора; проверить, что такая сверка не удаляет объекты и не принимает ошибку PostgreSQL за отсутствие строк.

## 4. Импорт наблюдений

- [x] 4.1 Валидировать наблюдения относительно идентичности и поколений CR/БД, импортировать фазы, `reason`, поля применённой конфигурации и исходный `observedAt`; проверить старые, пустые и невозможные снимки, одинаковое время наблюдения, `false`/`null` и неизменность пользовательских полей.
- [x] 4.2 Импортировать `network` и `RecoveryRequired`, объединять причину оператора с причиной доставки под строковой блокировкой; проверить, что импорт не снимает ещё действующую причину потери объектов и не освежает прежний `network_verified_at`.
- [x] 4.3 Сохранять состав нод и переходы фаз атомарно; проверить замену процесса того же `ordinal`, удаление исчезнувшей ноды, пустой список, исходное время события и отсутствие дубликатов при повторном импорте.
- [x] 4.4 Проверить итоговый HTTP-контракт после создания, изменения размера, настройки `whitelist` и смены пароля: поля применённой конфигурации меняются только после подготовленного `status`, свежесть наблюдений определяется по `observedAt`, квота освобождается после подтверждения меньшего размера, биллинг и пользовательский аудит не дублируются.

## 5. Удаление и продолжение после рестарта

- [x] 5.1 Реализовать сохранение `cr_delete_prepared` с проверенными UID и защитной отметкой удаления до `DELETE` CR, повторное чтение и проверку условий удаления; проверить прерывания до отправки, потерянный ответ и ожидание снятия защиты удаления с сохранением `Namespace`/`Secret`.
- [x] 5.2 Реализовать `namespace_delete_prepared`, удаление исходного `Namespace` и атомарную запись `deleted_at`/события `deleted` после `NotFound`; проверить рестарт на каждой границе, повторный проход, освобождение квоты и неизменность закрытого HTTP-запросом биллингового периода.
- [x] 5.3 Обработать отменённое создание до первого CR и неожиданные исчезновения/замены объектов; проверить очистку только принадлежащих остатков и отсутствие автоматического удаления `Namespace` при потере CR с историей.
- [x] 5.4 Проверить конкурентные `DELETE`/доставку и `DELETE`/импорт с управляемыми барьерами; убедиться, что позднее наблюдение не возвращает удалённую запись, а начатая запись Kubernetes остаётся под защитой отметки удаления и обрабатывается удалением.

## 6. Тестовое окружение и проверки отказов

- [x] 6.1 Подключить к HTTP-тестовой обвязке рабочие циклы с файлом подключения к Kubernetes с ограниченными правами и отдельный административный клиент для изменения `status` и снятия защиты удаления; проверить изоляцию аккаунтов/`Namespace`, остановку циклов до очистки и выполнение нового набора через существующую команду `just test` в каталоге `api`.
- [x] 6.2 Проверить RBAC настоящим k3s: разрешённые операции `Namespace`/`Secret`/CR выполняются, изменение `status` и управление подами рабочим клиентом запрещены; тестовый клиент не передаётся приложению.
- [x] 6.3 Добавить управляемые отказы транспорта конкретного тестового клиента, ошибки PostgreSQL и конфликты записи; проверить продолжение после восстановления, откат транзакции, отсутствие ложного `NotFound` и работоспособность HTTP при недоступности Kubernetes.
- [x] 6.4 Проверить отсутствие секретов в HTTP, аудитах, логах и диагностике новых сценариев; ошибки сравнений `Secret` не должны печатать исходные значения или хеши.
- [x] 6.5 Выполнить `just test` в `api` и `operator`, проверку генерации, применимые линтинг/сборку и `go mod tidy -diff`; зафиксировать фактические результаты, отдельно указав, что выполнение изменения размера и смены пароля настоящим оператором этим набором не проверяется.
- [x] 6.6 Использовать выделенные окружению адреса, файлы подключения и `K3S_DOCKER_GATEWAY` из штатной подготовки; при полном запуске API проверить совпадение шлюза в `environment.env` и Docker-сети, а также прохождение новых сценариев через существующую команду задания CI `test-api` без отдельной подготовки стенда.
- [x] 6.7 Проверить, что асинхронные сценарии ожидают требуемые поля и удаление объектов повторным чтением с ограниченным сроком, а не однократной проверкой сразу после HTTP-ответа; полный набор должен укладываться в существующее ограничение `test-api` в 20 минут, включая подготовку окружения.

## 7. Документация и завершение

- [x] 7.1 Обновить относящиеся к этому изменению фрагменты `SYSTEM.md` и `SYSTEM-DETAIL.md`: интервал API 1 секунда, первый проход сразу, протокол удаления, служебные поля и реализованные границы; проверить отсутствие прежних утверждений о пятисекундном интервале API и нереализованной синхронизации.
- [x] 7.2 Проверить согласованность реализации со сценариями обеих спецификаций изменений и выполнить `openspec validate add-valkey-state-sync --strict`; в отчёте перечислить выполненные проверки и оставшиеся этапы оператора.
