## 1. Go-модуль и инструменты

- [x] 1.1 Создать корневой модуль `github.com/RostislavDugin/managed-valkey`, закрепить Go 1.27.1 и прямые runtime-зависимости из дизайна; проверить `go mod tidy`, `go mod verify` и отсутствие вложенных `go.mod`.
- [x] 1.2 Добавить через `go get -tool` закреплённые `goose`, golangci-lint, controller-gen и совместимый `setup-envtest`; проверить запуск версии каждого инструмента через `go tool` без глобальной установки.
- [x] 1.3 Создать корневой `.golangci.yml` на основе Databasus: `standard` плюс `funcorder`, `bodyclose`, `errorlint`, `gocritic`, `unconvert`, `misspell`, `errname`, `noctx`, `modernize`; включить проверку type assertions и форматтеры `gofumpt`, `golines` на 120 символов и `gci`; проверить файл golangci-lint v2 и общий module path проекта.
- [x] 1.4 Проверить, что в корне нет `Justfile`, а `just --list` в `api` и `operator` показывает только согласованные публичные команды после их создания.

## 2. Общая основа и API

- [x] 2.1 Реализовать `internal/logging` с уровнем из env, text stdout на dev, JSON stdout на prod и безопасным fan-out в OTLP; покрыть тестами атрибуты `service.name`/`env`, уровни и продолжение работы без `VL_OTLP_URL`.
- [x] 2.2 Добавить разбор обязательной конфигурации API, подключение GORM к PostgreSQL и явное закрытие пула; проверить тестами ошибку пустого `DATABASE_URL` и недоступной базы.
- [x] 2.3 Создать `api/cmd/api` на `gin.New()` с recovery, `request_id`, журналом метаданных запроса, `/livez` и `/readyz`; игнорировать адрес клиента из `X-Forwarded-For`; проверить HTTP-тестами успешную liveness, зависимость readiness только от PostgreSQL, корректный адрес клиента и отсутствие тел и секретных заголовков в логах.
- [x] 2.4 Реализовать завершение API через `signal.NotifyContext`, `http.Server.Shutdown`, закрытие базы и flush логов с таймаутами; проверить тестом, что сервер перестаёт принимать новые соединения и завершает начатый запрос.
- [x] 2.5 Настроить запуск API на хосте через `api just run` с корневым `.env`, отдельным kubeconfig и применением ожидающих миграций.
- [x] 2.6 Создать `api/Justfile` только с публичными `migration-up`, `migration-down`, `migration-generate name`, `test` и `run`; сверить список через `just --list` и выполнить `just run`.
- [x] 2.7 Настроить `just test` в `api` как единый набор unit- и PostgreSQL-тестов без деления на fast/full; проверить успешный прогон на мигрированной `managed_valkey_test` без развёртывания Valkey в Kubernetes.

## 3. Каркас оператора Kubebuilder

- [x] 3.1 Сгенерировать эталон Kubebuilder v4.13.1 во временном каталоге с группой `valkey.h3llo-demo.com/v1alpha1`, сохранить сведения о версии и проверить, что эталон использует `controller-runtime v0.23.3`.
- [x] 3.2 Адаптировать manager, тип `ValkeyInstance`, DeepCopy, начальный reconciler, CRD и RBAC к `operator/cmd/operator`, `operator/api/v1alpha1`, `operator/internal/operator` и `operator/config`; проверить отсутствие вложенного модуля, Kubebuilder Makefile и демонстрационных ресурсов.
- [x] 3.3 Сделать начальный reconcile безопасной пустой операцией без записи `status` и создания объектов; проверить через `envtest`, что создание `ValkeyInstance` не меняет ресурс и не создаёт зависимостей.
- [x] 3.4 Настроить manager с leader election, `/healthz`, `/readyz`, общим logger и завершением по контексту; проверить `envtest` запуска, готовности и остановки manager.
- [x] 3.5 Добавить один закрытый рецепт генерации DeepCopy, CRD и RBAC закреплённым `controller-gen`; выполнить его через публичные команды и проверить результаты отдельным скриптом во временной копии.
- [x] 3.6 Настроить запуск оператора на хосте через `operator just run` с отдельным kubeconfig из `tmp/k3s`.
- [x] 3.7 Создать `operator/Justfile` с публичными `test-fast`, `test-full` и `run`; до появления интеграционных тестов на k3s команда `test-full` должна печатать `TODO` и завершаться с ошибкой; сверить список через `just --list` и отсутствие миграционных команд.
- [x] 3.8 Настроить `just test-fast` в `operator` для unit-тестов и `envtest` без настоящих подов Valkey; проверить, что команда не требует работающего dev compose и использует Kubernetes API ветки 1.35.
- [x] 3.9 Настроить `just run` в `operator`: сгенерировать артефакты, проверить наличие kubeconfig и запустить manager через `go run`; проверить `/healthz`, `/readyz`, получение Lease и штатное завершение.

## 4. Миграции PostgreSQL

- [x] 4.1 Создать корневой `migrations` с технической транзакционной миграцией `baseline`, которая не добавляет предметные таблицы; применить её к чистой PostgreSQL 18 и проверить запись версии `goose`.
- [x] 4.2 Реализовать `just migration-generate <name>` в `api` на закреплённом `goose` и корневом `../migrations`; проверить создание timestamp-файла с переданным именем во временной копии каталога.
- [x] 4.3 Реализовать `just migration-up` и `just migration-down` в `api`; проверить применение с нуля, безопасный повтор `up` и откат ровно одной последней миграции.
- [x] 4.4 Записать соглашения SQL для новых миграций в `api/AGENTS.md`, включая UUIDv7, `TIMESTAMPTZ`, имена булевых колонок и раздельные ограничения.
- [x] 4.5 Настроить создание `managed_valkey_test` по требованию и применение миграций перед `api just test`; проверить тестом реальной базы откат ошибочной транзакционной миграции без записи её версии.

## 5. Образы приложений

- [x] 5.1 Создать многоступенчатый `api/Dockerfile` с целями `build`, `runtime` и `migrate`, корневым контекстом и закреплёнными базовыми образами; собрать все цели и проверить запуск runtime от non-root и отсутствие Go toolchain в нём.
- [x] 5.2 Создать многоступенчатый `operator/Dockerfile` с целями `build` и `runtime`; собрать все цели и проверить запуск runtime от non-root и наличие CA-сертификатов.
- [x] 5.3 Создать `caddy/Dockerfile` с закреплённым `mholt/caddy-ratelimit` и `caddy/Caddyfile` по `SYSTEM.md`; собрать образ и проверить валидность конфигурации командой Caddy.
- [x] 5.4 Проверить контексты и `.dockerignore`, чтобы сборки получали общий Go-модуль, но не копировали `.git`, локальные секреты, временные бинарники и кэши; подтвердить состав образов через `docker history` и запуск контейнеров.

## 6. Dev-окружение

- [x] 6.1 Создать `.env.example` со всеми переменными из `SYSTEM.md`, значениями `Q1234567` для секретов и безопасными dev-значениями; проверить, что `.env` игнорируется Git и пример не содержит рабочего секрета или закрытого ключа.
- [x] 6.2 Создать `docker-compose.dev.yml` только для PostgreSQL 18, k3s-server и двух k3s-agent; проверить четыре разрешённых сервиса через `docker compose config`.
- [x] 6.3 Добавить k3s-server и два k3s-agent с закреплённой версией, выключенными Traefik и servicelb, общим token и нужными портами; запустить их и проверить три готовые ноды через `kubectl get nodes`.
- [x] 6.4 Создать идемпотентный host-bootstrap с системными `kubectl`, `helm` и `mkcert`, закреплёнными Gateway API, Envoy Gateway и манифестами `deploy/dev`; дважды запустить и проверить восстановление зависшего Helm release и повторной регистрации agent-нод.
- [x] 6.5 Создать отдельные RBAC и kubeconfig API и оператора в `tmp/k3s`; командами `kubectl auth can-i` проверить разрешённые действия и запрет API на `status` и поды.
- [x] 6.6 Настроить dev-сертификат и NodePort `41379` по `SYSTEM.md`, не сохраняя закрытый ключ в Git; проверить наличие TLS Secret и доступность опубликованного порта.
- [x] 6.7 Подключить API и оператор к раздельным kubeconfig и запускать на хосте с логами в stdout; проверить `/livez`, `/readyz` и probes оператора.
- [x] 6.8 Запустить dev-инфраструктуру командой `docker compose -f docker-compose.dev.yml up -d --remove-orphans`; проверить отсутствие корневой обёртки `just`, удаление прежних сервисов и сохранение именованных volumes при обычной остановке.

## 7. Production-окружение

- [x] 7.1 Создать `docker-compose.prod.yml` для PostgreSQL, `migrate`, API, VictoriaLogs и Caddy с портами, healthcheck API и порядком запуска из `SYSTEM.md`; проверить `docker compose config` с тестовым env-файлом.
- [x] 7.2 Проверить остановку запуска API при ошибке `migration-up` и запуск после исправления миграции; подтвердить статусами контейнеров и ответом `/readyz`.
- [x] 7.3 Создать `deploy/prod` с Kustomize, StatefulSet оператора, ServiceAccount, RBAC, leader election, probes и ссылками на сгенерированный CRD; выполнить `kustomize build --load-restrictor LoadRestrictionsNone deploy/prod` и проверить отсутствие `DATABASE_URL` и адреса API в конфигурации оператора.
- [x] 7.4 Запустить и остановить окружение прямыми командами `docker compose -f docker-compose.prod.yml`; проверить, что dev-проект и его volumes не затронуты.
- [x] 7.5 Проверить, что production compose, Dockerfile и манифесты не используют `latest`, а внешние образы имеют конкретные версии и допускают фиксацию digest; записать это правило в корневой `AGENTS.md`.

## 8. TODO, инструкции и итоговая проверка

- [x] 8.1 Создать короткий `operator/TODO.md` со сценариями остановки primary pod, ожидания нового primary и отдельной проверки потери worker-ноды с fencing; проверить, что файл не подменяет реализацию теста подробным планом.
- [x] 8.2 Создать короткий `e2e/TODO.md` со сценариями регистрации пользователя, создания Valkey через API, подключения и запросов каждые 100 мс во время остановки primary pod до восстановления; проверить наличие критериев временной ошибки и возврата успешных запросов.
- [x] 8.3 Создать `api/AGENTS.md` со ссылками на корневые инструкции и `SYSTEM.md`, адаптировать правила spacing, comments, UTC, `slog`, GORM, `goose` и современного Go; проверить отсутствие чужих имён, обязательного Swagger и неподходящей DI-схемы из исходного гайда.
- [x] 8.4 Создать `operator/AGENTS.md` с теми же общими правилами Go и отдельными правилами `controller-runtime`, идемпотентного reconcile и запрета PostgreSQL/API; проверить ссылки и отсутствие инструкций по миграциям как обязанности оператора.
- [x] 8.5 Создать относительные символические ссылки `api/CLAUDE.md -> AGENTS.md` и `operator/CLAUDE.md -> AGENTS.md`; проверить тип каждой ссылки, её цель и чтение итогового файла через оба пути.
- [x] 8.6 Расширить `.githooks/pre-commit`: для staged Go-файлов без незакоммиченных строк запускать форматирование и возвращать только их в индекс, затем напрямую выполнять линтинг, проверку генерации, сборку и `go mod tidy -diff` для затронутых приложений; общий Go-код, модульные файлы и корневой `.golangci.yml` должны проверять оба приложения, а удаление затронутого файла, любая ошибка или замечание линтера должны отменять коммит.
- [x] 8.7 Проверить hook временным неформатированным Go-файлом, который после успешных проверок попадает в индекс уже исправленным; отдельно проверить частично staged-файл, удаление, изменение только `go.mod`, ошибку линтера, расхождение `go mod tidy -diff` и ошибку сборки без добавления посторонних незакоммиченных файлов.
- [x] 8.8 Обновить корневой `AGENTS.md` с описанием проверок frontend и Go, а разделы 14 и 16 `SYSTEM.md` с короткими списками команд приложений, разделением интеграционных тестов оператора и корневого `e2e`, а также обязательной production-конфигурацией; проверить согласованность всех упоминаний команд поиском.
- [x] 8.9 Запустить `go mod verify`, прямой линтинг и сборку обоих приложений, `api just test`, `operator just test-fast`, сборку трёх production-образов и проверку обоих compose-файлов; устранить все ошибки без расширения продуктового поведения.
- [x] 8.10 Запустить dev compose, дважды выполнить host-bootstrap, затем `just run` из `api` и `operator`; проверить PostgreSQL, baseline `goose`, три ноды k3s, API и оператор, затем штатно остановить процессы и убедиться, что в логах нет секретов.
- [x] 8.11 Просмотреть итоговый diff, проверить отсутствие корневого `Justfile` и выполнить `openspec validate setup-backend-foundation --strict`.
