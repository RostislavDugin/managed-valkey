## 1. Квоты аккаунтов

- [x] 1.1 Добавить Goose-миграцию, которая меняет `DEFAULT` таблицы `user_quotas` на 4 vCPU / 12 GB и обновляет все существующие строки; проверить применение к новой базе и к базе с аккаунтом 4/16 через штатный запуск миграций API.
- [x] 1.2 Вынести стандартную квоту регистрации в константы 4 vCPU / 12 GB, обновить HTTP-тесты регистрации и `/v1/me`; запустить затронутые Go-тесты.
- [x] 1.3 Проверить сценарий существующего расхода выше 12 GB: миграция не меняет инстансы, уменьшение или удаление разрешено, увеличение превышения отклоняется; запустить соответствующий HTTP-тест.

## 2. API доступной ёмкости

- [x] 2.1 Добавить в хранилище запрос одного согласованного снимка PostgreSQL для личной квоты, личного и общего резерва и числа активных инстансов; покрыть `single`, `ha`, `desired_*`, `applied_*` и неподтверждённое удаление тестами.
- [x] 2.2 Добавить в Valkey-сервис модель снимка ресурсов с лимитами из конфигурации и пределом 32 инстанса, не получая общую блокировку и не обращаясь к Kubernetes; проверить тесты сервиса.
- [x] 2.3 Добавить защищённый `GET /v1/managed/valkey/capacity` и HTTP-тесты формата ответа, изоляции личного расхода, общего расхода всех владельцев и `401` без JWT.
- [x] 2.4 Сохранить окончательную проверку мутаций и порядок `QUOTA_EXCEEDED` перед `NOT_ENOUGH_RESOURCES`; запустить тесты конкурентной борьбы за последний ресурс и отсутствия частичных записей.

## 3. Клиентская модель ограничений

- [x] 3.1 Добавить DTO и клиент `getValkeyCapacity`, обновить тестовые API-ответы и проверить преобразование `ram_gb`, личного бюджета, общего бюджета и предела инстансов.
- [x] 3.2 Обобщить расчёт кандидата для личного и общего бюджета с учётом текущего резерва `resize` и правила неувеличения превышенного лимита; покрыть причины `available`, `user_quota`, `cluster_resources` и `instance_limit` модульными тестами.
- [x] 3.3 Передавать в `SizePlans` причину недоступности вместо одного признака и выводить отдельные подписи для личной квоты, ресурсов Managed Kubernetes и предела баз; проверить доступность выбора и отображение цены компонентными тестами.

## 4. Опрос в формах

- [x] 4.1 На странице создания загрузить снимок ресурсов сразу при открытии и обновлять примерно раз в 5 секунд только после завершения предыдущего запроса; проверить блокировку до первого ответа, обновление занятого другим аккаунтом ресурса и отмену при уходе или смене сессии.
- [x] 4.2 В окне изменения тарифа запустить отдельный опрос ресурсов только пока окно открыто, сохранив независимое обновление карточки базы; проверить вычитание текущего резерва, отмену при закрытии и отсутствие перекрывающихся запросов.
- [x] 4.3 Реализовать приоритет личной квоты над общим бюджетом, отдельное сообщение «Недостаточно ресурсов Managed Kubernetes» и запрет мутации при достигнутом пределе баз; проверить все три причины и одновременное нарушение компонентными тестами.
- [x] 4.4 После принятой мутации или ресурсного `422` немедленно обновлять снимок без параллельного запроса, сохранять заполненные значения и оставлять серверный текст окончательной ошибки; запустить тесты создания и `resize`.

## 5. Сквозные проверки и документация

- [x] 5.1 Обновить общие тестовые ответы `web` и E2E-константы с 4/16 на 4/12, убрав предположения, из-за которых тест изменения тарифа не может подобрать запрещённый кандидат; выполнить проверку TypeScript и каталога сценариев.
- [x] 5.2 Переделать E2E личной квоты на совместные `single` 1/2 и `ha` 3 x 1/2, отказ `resize` и успех после удаления HA.
- [ ] 5.2a Проверить сценарий личной квоты командой группы `quotas`. Пропущено по указанию пользователя.
- [x] 5.3 Переделать E2E общей ёмкости на предварительную блокировку без POST, добавить занятие последнего ресурса другим аккаунтом при открытой форме и приоритет личной квоты.
- [ ] 5.3a Проверить работающие ранее созданные базы через `SET` и `GET` в E2E. Пропущено по указанию пользователя.
- [x] 5.4 Обновить `SYSTEM.md`, `SYSTEM-DETAIL.md` и E2E-документацию: стандартная квота 4/12, контракт `/v1/managed/valkey/capacity`, последовательный опрос и отличие настроенного резерва от фактического `allocatable`; проверить поиском отсутствие устаревших утверждений 4/16 вне исторических примеров и тарифов.
- [x] 5.5 Выполнить форматирование затронутых областей, затем локальные lint и быстрые тесты API и web; проверить `git status`, чтобы форматирование не добавило чужие изменения в индекс.
- [x] 5.6 Перед тяжёлой проверкой оценить RAM, CPU, процессы и контейнеры.
- [ ] 5.6a Запустить `just test-e2e-group quotas`, во время и после запуска повторить проверку ресурсов и адресно очистить только остатки этого стенда. Пропущено по указанию пользователя.
