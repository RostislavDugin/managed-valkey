## MODIFIED Requirements

### Requirement: E2E проверяет личную и общую квоту

Сценарии `@local` SHALL выбирать размеры из показанного каталога и проверять предварительный запрет интерфейса как по личной квоте 4 vCPU / 12 GB, так и по общей ёмкости. Они MUST NOT использовать занятость окружений разработки или эксплуатации и MUST NOT подготавливать состояние прямой записью в API или PostgreSQL.

#### Scenario: HA не помещается в личную квоту

- **WHEN** новый аккаунт выбирает HA-размер, суммарный расход которого превышает свободные 4 vCPU или 12 GB
- **THEN** интерфейс показывает недостающие ресурсы, оставляет размер видимым и не разрешает отправить запрос создания
- **AND** HTTP-запрос создания не отправляется

#### Scenario: Изменение не помещается в личную квоту

- **WHEN** пользователь создаёт `single` 1 vCPU / 2 GB и `ha` 3 x 1 vCPU / 2 GB, затем выбирает для `single` размер, который вместе с HA превышает личную квоту
- **THEN** интерфейс показывает причину и не отправляет изменение
- **AND** после удаления HA тот же допустимый размер можно применить к `single`
- **AND** удаление последнего инстанса возвращает использование квоты к нулю

#### Scenario: Исчерпана общая ёмкость

- **WHEN** тест последовательно создаёт минимальные `single` у разных аккаунтов до исчерпания доступных после инфраструктурного резерва ресурсов настроенной топологии, затем открывает форму ещё одного аккаунта
- **THEN** интерфейс сообщает о нехватке ресурсов Managed Kubernetes и не разрешает отправить создание
- **AND** HTTP-запрос создания не отправляется, отклонённая карточка не появляется, а ранее созданные инстансы продолжают выполнять `SET` и `GET`

#### Scenario: Общая ёмкость заканчивается при открытой форме

- **WHEN** один аккаунт держит открытую форму с доступным размером, а другой занимает последний подходящий ресурс
- **THEN** первый аккаунт после следующего опроса видит ошибку ресурсов Managed Kubernetes и недоступную отправку
- **AND** опросы `/v1/managed/valkey/capacity` не перекрываются

#### Scenario: Личная и общая квоты закончились одновременно

- **WHEN** выбранный размер не помещается ни в личную квоту, ни в общий бюджет
- **THEN** интерфейс показывает причину личной квоты и не показывает причиной Managed Kubernetes
- **AND** HTTP-запрос мутации не отправляется

#### Scenario: Бюджет нельзя исчерпать допустимым числом инстансов

- **WHEN** настроенный бюджет нельзя исчерпать минимальными инстансами раньше предела 32 инстанса
- **THEN** подготовка сценария завершается понятной ошибкой без создания пользовательских ресурсов
