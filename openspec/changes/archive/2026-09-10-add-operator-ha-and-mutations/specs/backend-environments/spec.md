## MODIFIED Requirements

### Requirement: Пять независимых окружений Kubernetes

Проект SHALL поддерживать постоянный трёхнодовый dev-кластер и отдельные кластеры для API-тестов (одна нода), оператора (три ноды с добавлением четвёртой для сценариев с запасным хостом), API + operator (три ноды) и Playwright e2e (три ноды). Трёхнодовый кластер SHALL состоять из одного server и двух agents; четвёртая нода оператора SHALL быть дополнительным agent того же тестового запуска. Набор оператора SHALL проверять восстановление как без запасного хоста, так и с ним. Расширение тестового окружения оператора MUST NOT добавлять ноду в постоянный dev или соседние кластеры.

Подготовка SHALL использовать общий параметризованный шаблон. Окружения SHALL иметь независимые Compose projects, тома, токены и сетевые диапазоны; маршруты к Pod IP MUST NOT пересекаться при одновременной работе на одном хосте. Дополнительный agent SHALL участвовать в выделении адресов, bootstrap, подготовке образов, маршрутах, диагностике и очистке своего запуска. Явный gateway тестовой Docker-сети SHALL сохраняться при добавлении и замене agent; его адрес MUST NOT совпадать с адресами нод или тестовых клиентов.

#### Scenario: Полный локальный запуск

- **WHEN** dev и четыре тестовых окружения подняты одновременно с исходным трёхнодовым стендом оператора
- **THEN** работают пять независимых Kubernetes API и 13 нод, а оператор каждого полного стенда обрабатывает только CR своего кластера

#### Scenario: Дополнительная нода оператора

- **WHEN** к одновременно работающим окружениям добавляется запасной agent оператора
- **THEN** в исходно полном составе работают 14 нод и те же пять Kubernetes API, dev сохраняет три ноды, дополнительный agent использует сеть и ресурсы только своего тестового запуска

#### Scenario: Восстановление без запасного хоста

- **WHEN** оператор проверяет потерю worker-ноды на исходном трёхнодовом стенде
- **THEN** остаются две ноды, HA восстанавливает запись в degraded с третьим Pod в Pending; добавление чистого agent позволяет восстановить три копии

#### Scenario: Очистка тестового кластера

- **WHEN** тестовый запуск удаляет свой кластер и тома
- **THEN** он освобождает также дополнительный и заменяющие agents, их маршруты и сетевые ограничения, а dev-инстансы, dev-база и соседние тестовые окружения сохраняются
