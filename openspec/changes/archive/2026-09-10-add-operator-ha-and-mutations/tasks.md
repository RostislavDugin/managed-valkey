## 1. Контракт и сохранённые операции

- [x] 1.1 Добавить типы failover, rollout, credentialRotation и фазы updating/degraded в публичный CR; проверить генерацию DeepCopy/CRD/RBAC и сохранение новых полей через envtest `CT-03`.
- [x] 1.2 Добавить сведения о репликации и историю неподтверждённых прежних процессов, сохранив nodes по ordinal; проверить A/B/C, отсутствие чужого доказательства и смену run ID в `CT-06`, `CT-07`.
- [x] 1.3 Реализовать принятие полного снимка нового поколения без подмены незавершённой цели, с ожиданием валидного хеша; проверить устаревшие поколения, неподдерживаемые смешанные поля и конфликты в `CT-04`.
- [x] 1.4 Разделить подтверждения размера, пароля и всего поколения; проверить различие desiredGeneration/metadata.generation, совместное изменение и сохранение старого applied до завершения в envtest `CT-03`, `CT-04`.
- [x] 1.5 Обеспечить чтение ранее созданного single без новых полей операций и без замены существующих селекторов StatefulSet; проверить сохранение Pod UID, служебных паролей и публичного доступа текущим lifecycle и регрессией `CT-11`.
- [x] 1.6 Измерить проверки контракта и сохранённых операций; заменить ожидание реального времени подменяемыми часами или наблюдаемым состоянием, сохранив envtest там, где нужен Kubernetes API.

## 2. Секундное наблюдение

- [x] 2.1 Установить период наблюдения и ожидания операций 1 секунда, сохранить немедленную обработку watch; проверить планирование без накопления тиков и перекрытий в `CT-01` с подменяемыми часами.
- [x] 2.2 Разделить результаты PING/ROLE/INFO на транспортный сбой, BUSY, LOADING и AUTH; проверить сбросы счётчиков и независимость порогов 10/30/60/120 секунд в `CT-01`, `CT-02`.
- [x] 2.3 Обеспечить наблюдение остальных процессов и ограниченный параллелизм независимых инстансов при зависшем вызове; проверить таймауты и отсутствие блокировки исправного инстанса в `CT-10`.
- [x] 2.4 Отделить обновление снимков Envoy от секундного наблюдения Valkey, сохранив период проверки сети 10 секунд; проверить отсутствие ложного failover и продолжение heartbeat в `NT-06`.
- [x] 2.5 Сохранять observedAt только по свежим наблюдениям, не дублировать Events и изменения неизменных ресурсов; проверить работу после восстановления Kubernetes и запрет действий по старому снимку в `NT-04`.
- [x] 2.6 Измерить тесты секундного наблюдения; убрать фиксированные паузы и проверить рабочие пороги подменяемыми часами, а сокращённые интервалы k3s-профиля отдельной сборкой.

## 3. Обвязка k3s и управляемые аварии

- [x] 3.1 Разделить существующий harness на общие средства для single/HA, создания входного CR/Secret и проверки TLS/ролей/репликации; проверить прежний single lifecycle на той же публичной команде.
- [x] 3.2 Добавить режим с дополнительным чистым agent текущего запуска и его bootstrap/маршруты/очистку; выделять K3S_AGENT_3_IP из текущей подсети с сохранением явного K3S_DOCKER_GATEWAY и проверкой отсутствия совпадений с нодами и клиентами; проверить трёх- и четырёхнодовый стенд в `CT-14` и отсутствие чужих ресурсов в перечне очистки.
- [x] 3.3 Добавить самостоятельную сборку рабочего Dockerfile оператора с тегом запуска и загрузку образа во все тестовые ноды, включая новые/восстановленные, затем запуск внутри k3s с StatefulSet, ServiceAccount и Lease без одновременной хостовой копии; проверить доступность образа при выключенных заданиях сборки релизов и `OP-01` через SIGKILL без graceful cleanup.
- [x] 3.4 Добавить отправку сигналов Valkey, DELETE Pod и аварийную остановку agent с проверкой вложенных процессов; проверить, что недоступный механизм воздействия завершает тест ошибкой и не приводит к skip.
- [x] 3.5 Добавить адресные сетевые разрывы operator/Valkey, agent/control-plane и operator/Kubernetes с проверкой заданной связности; проверить снятие собственных ограничений при ошибке и прерывании в `CT-12`.
- [x] 3.6 Добавить управляемые точки рестарта до/после внешнего действия и потерю ответа настоящей команды Valkey; проверить, что команда фактически выполнена, а роль/ACL читаются у реального процесса для `OP-04`.
- [x] 3.7 Обеспечить сохраняемый публичный вход через живой Envoy при уничтожении agent и возможность проверить оба Envoy отдельно; подтвердить исходный и восстановленный TLS/SNI-доступ для `ND-06`.
- [x] 3.8 Добавить журналы событий отказа с ID матрицы, конечные deadlines, безопасные снимки и очистку после каждого сценария; проверить падение и прерывание при недоступном Kubernetes в `CT-12`.
- [x] 3.9 Сохранить согласованность gateway в распределителе, Compose IPAM и operator.env; проверить обязательность значения для тестового Compose и путь Docker inspect в bootstrap через `CT-13`, административный доступ хостового оператора при NetworkPolicy до/после добавления и замены agent через `CT-14`.
- [x] 3.10 Измерить подготовку k3s, сборку и загрузку образа; добавить отдельный build tag сокращённых интервалов для k3s-тестов и их образа, сохранив рабочие пороги в обычной сборке и unit/envtest; измерить переключение в `FP-04`, переиспользовать готовый стенд внутри запуска и проверить, что release-образ не получает тестовый профиль.

## 4. Первоначальный HA

- [x] 4.1 Создавать три пода HA с жёстким anti-affinity и PDB minAvailable 2, сохраняя OnDelete/Never; проверить размещение, нехватку хостов и Eviction в `HA-01`, `HA-03`, `HA-05`.
- [x] 4.2 Добавить старт HA в роли реплики с app off и первоначальное назначение ordinal 0; проверить отсутствие второго primary при рестарте между назначением и подтверждением в `HA-02`.
- [x] 4.3 Подключать реплики к текущему primary и подтверждать синхронизацию конкретного процесса; проверить данные через primary/-ro и отбрасывание пустой или чужой истории в `HA-01`, `CT-07`.
- [x] 4.4 Добавить Services, listener, TCPRoute и первоначальный whitelist для -ro, расширить проверку EndpointSlice и обоих Envoy; проверить `HA-04`, `HA-06` с неверным SNI, паролем и изоляцией соседей.
- [x] 4.5 Рассчитывать running/degraded/updating/unavailable с учётом первоначальной готовности и незавершённых операций; проверить degraded при Pending и переход в running в `HA-03`.
- [x] 4.6 Расширить удаление на весь HA и оба маршрута, сохранив Secret до доказательств остановки; проверить `HA-07` с открытыми клиентскими соединениями.
- [x] 4.7 Измерить создание, восстановление состава и удаление HA; заменить лишние паузы ожиданием Pod UID, роли, репликации, EndpointSlice и публичной готовности.

## 5. Fencing и переключение primary

- [x] 5.1 Добавить операции Valkey для чтения истории/upstream/offset и управляемого изменения роли без автоматического повтора; проверить ответы реального Valkey и выбор кандидата в `FP-12`, сравнение несопоставимых историй в `CT-07`.
- [x] 5.2 Реализовать сохраняемый fencing источника и кандидата с ACL off и закрытием app-соединений; проверить старые primary/-ro-соединения в `HA-08`, фактическую потерю ответов в `OP-04`.
- [x] 5.3 Реализовать choosing с сохранённым контрольным offset и пятисекундным deadline, затем promoting и проверку фактической роли; проверить рестарт каждой стадии и срока ожидания в `OP-03`, `OP-08`.
- [x] 5.4 Обрабатывать кандидата с неизвестным результатом как потенциальный primary, запрещая выбор другой цели без доказательства; проверить убийство и изоляцию кандидата в `OP-05`, `OP-06`.
- [x] 5.5 Переподключать все доступные процессы непосредственно к новому primary и проверять роль/ACL/синхронизацию перед допуском; проверить `FP-01`, `RZ-10` и отсутствие доступа к прежнему primary до его проверки.
- [x] 5.6 Реализовать приём управления и очистку прежних административных соединений перед наблюдением неизвестного результата; проверить потерю Lease и задержанные команды в `OP-02`, `OP-07`.
- [x] 5.7 Проверить SIGKILL и DELETE primary, каждой реплики и single с настоящими соединениями; закрыть все варианты `FP-01`, `FP-02`, `FP-03`, `FP-04`, `FP-05`.
- [x] 5.8 Реализовать восстановление без пригодной копии с условиями пустого старта и событием потери кэша; проверить потерю обеих реплик и всего состава в `FP-10`, `FP-11`, запрет по одному таймауту в `CT-07`.
- [x] 5.9 Измерить каждый вариант failover; удерживать обычное переключение исправного стенда в ориентире 15–30 секунд тестового профиля и сохранять полные отрицательные окна для неизвестного primary и кандидата.

## 6. Ноды, сетевые разделения и зависшие процессы

- [x] 6.1 Обобщить сохранение доказательств остановки для всех ролей и node_deleted с чтением Node без кэша; проверить NotFound/Forbidden/timeout, старый UID и удаление только Pod в `CT-05`.
- [x] 6.2 Принимать ручное fencing только для точной ожидающей идентичности после выключения ноды; проверить чужие и устаревшие аннотации в `CT-06` и восстановление после реальной остановки тестовой ноды.
- [x] 6.3 Реализовать остановку процесса без ответов после порога с UID/resourceVersion preconditions, сохранением стадии и доказательства; проверить SIGSTOP/SIGCONT для всех ролей и рестарты в `FP-06`, `FP-07`, `OP-08`.
- [x] 6.4 Реализовать BUSY recovery для Lua/Function, включая AUTH, KILL, UNKILLABLE и остановку; проверить короткий, read-only и пишущий скрипты в `FP-08`, `FP-09`.
- [x] 6.5 Проверить отсутствие продвижения при живом изолированном primary/agent и при отказе только сети оператора; закрыть длительные отрицательные сценарии `NT-01`, `NT-02`, `NT-03` с подтверждённо работающим старым процессом.
- [x] 6.6 Проверить остановку операций при недоступном Kubernetes и восстановление репликации после разрыва; закрыть `NT-04`, `NT-05` без ручного назначения ролей.
- [x] 6.7 Уничтожить agent с primary, репликой и single при запасной ноде; проверить `ND-01`, `ND-02`, `ND-03` и восстановление через публичный адрес без правки Pod finalizers.
- [x] 6.8 Проверить общую потерю оператора и primary, восстановление без запасной ноды и потерю Envoy; закрыть `ND-04`, `ND-05`, `ND-06` с подтверждением размещения перед отказом.
- [x] 6.9 Проверить новую ноду с прежним именем, отказ ноды при остановке процесса и рестарт после node_deleted; закрыть `ND-07`, `ND-08`, `ND-09` с чистым состоянием agent.
- [x] 6.10 Измерить сценарии нод, сети, SIGSTOP и BUSY; ускорять восстановление agent, проверку связности и диагностические deadlines без сокращения отрицательного наблюдения ниже тестовых порогов.

## 7. Ресайз

- [x] 7.1 Сохранять rollout и целевую ConfigMap до изменения StatefulSet, удерживать старые конфигурации и образ; проверить рестарт старого контейнера и соответствие maxmemory его размеру в `RZ-09`.
- [x] 7.2 Реализовать ресайз single через доказанную остановку и пустой запуск; проверить рост, уменьшение и изменение только CPU в `RZ-01`.
- [x] 7.3 Реализовать последовательную замену реплик HA без уменьшения RAM, ожидая синхронизации; проверить рост RAM и оба направления CPU в `RZ-02`, отказ обновляемой реплики в `RZ-06`.
- [x] 7.4 Подключить плановое переключение к общему failover с кандидатом целевого размера и проверкой прямого upstream всех реплик; проверить сохранение ключа и задержку замены бывшего primary в `RZ-02`, `RZ-10`.
- [x] 7.5 Реализовать HA shrink через ноль реплик и доказательство остановки всего состава; проверить пустой кэш и отсутствие преждевременного запуска при рестарте в `RZ-03`, `RZ-08`.
- [x] 7.6 Сохранять цель rollout при аварии и Pending, подтверждать applied только после полной перекатки; проверить нехватку ресурсов и убийство primary в `RZ-04`, `RZ-05`.
- [x] 7.7 Проверить потерю agent и рестарты на границах изменения шаблона/DELETE/replicas/applied; закрыть `RZ-07`, `RZ-08` для последовательной перекатки и полной остановки.
- [x] 7.8 Проверить раннее следующее поколение и удаление во время rollout; закрыть `RZ-11`, подтверждая сохранение принятой цели и отсутствие возрождения удаляемого workload.
- [x] 7.9 Измерить каждый вариант ресайза; переиспользовать готовый инстанс там, где это не смешивает состояния, и ждать конкретные стадии rollout, Pod UID, ресурсы и синхронизацию без фиксированных пауз.

## 8. Ротация пароля

- [x] 8.1 Обновлять users.acl по принятой версии с сохранением app off и служебных паролей, не выбирая максимум ключей Secret; проверить оба порядка доставки хеша/spec в `PW-02`.
- [x] 8.2 Добавить смену только хеша с сохранением on/off и прав, проверку ACL и закрытие app-соединений; проверить single и HA с TCP/pub-sub/блокирующими клиентами в `PW-01`, закрытый процесс в `PW-08`.
- [x] 8.3 Сохранять подтверждения версии по конкретным процессам и обрабатывать повтор после неизвестного ответа; проверить рестарты каждой стадии и гибель реплики в `PW-03`, `PW-05`.
- [x] 8.4 Завершать ротацию по всем работающим и доказанно остановленным процессам, не ожидая ещё не запущенную реплику; проверить живую изолированную реплику и уничтожение agent без места для замены в `PW-06`, `PW-07`.
- [x] 8.5 Реализовать отдельную стадию очистки предыдущего хеша с сохранением будущих версий и служебных данных; проверить конфликт и рестарт после записи Secret в `CT-08`, `PW-03`.
- [x] 8.6 Применять целевой пароль до допуска нового primary при аварии посреди ротации; проверить `PW-04`, не допуская временного возврата старого пароля на новом primary.
- [x] 8.7 Проверить отсутствие повторных разрывов после подтверждения и актуальный пароль у будущего процесса; закрыть `PW-09` с проверкой неизменности Pod UID/container ID при самой ротации.
- [x] 8.8 Применять ротацию перед размером при общем поколении и отдавать приоритет удалению; проверить `PW-10` с отдельными appliedPasswordVersion/applied и общим observedGeneration.
- [x] 8.9 Измерить варианты ротации; переиспользовать безопасно открытые соединения и готовые single/HA, ожидая стадии и подтверждения процессов вместо временных пауз.

## 9. Совмещённые отказы и регрессии

- [x] 9.1 Проверить удаление во время failover, shrink и ротации с учётом неизвестного кандидата; закрыть `DL-01` и проверить доказательства остановки каждого известного процесса.
- [x] 9.2 Проверить уничтожение agent и живую изолированную ноду при удалении; закрыть `DL-02`, `DL-03` без ручного снятия finalizers и преждевременного удаления Secret.
- [x] 9.3 Перезапустить оператор на каждой стадии удаления HA до и после внешнего действия; закрыть `DL-04`, сохранив namespace за вызывающей стороной.
- [x] 9.4 Проверить потерю и восстановление исходного Secret/служебного поля, отсутствие регенерации и утечек; закрыть `CT-09` и проверить безопасную диагностику.
- [x] 9.5 Проверить секундное наблюдение исправного соседа, неизменность образа и служебных паролей, прежний single и публичную изоляцию после аварий; закрыть `CT-10`, `CT-11`.
- [x] 9.6 Измерить совмещённые отказы и регрессии; отделять состояние сценариев namespace и явной очисткой, не пересоздавая весь k3s без необходимости.

## 10. Полный запуск и документация

- [x] 10.1 Включить все ID матрицы в operator just test и существующий CI job test-operator; согласовать go test -timeout и timeout-minutes job по фактической длительности с запасом на сборку/загрузку образа, подготовку, диагностику и очистку; проверить полный запуск без skip обязательных сценариев и отчёт по каждому ID, сохранив отключёнными публикацию релизов и развёртывание.
- [x] 10.2 Проверить прерывание полного запуска во время сетевого отказа и очистку четвёртого agent; подтвердить сохранение диагностики, dev и ресурсов соседнего запуска по `CT-12`.
- [x] 10.3 Обновить SYSTEM.md, SYSTEM-DETAIL.md, operator/TODO.md и инструкции тестового запуска по реализованным возможностям и секундному периоду; проверить отсутствие заявлений о подключённом API, метриках или подтверждённом поведении h3llo.
- [x] 10.4 Выполнить полный operator just test, генерационные проверки и обязательные проверки Go, включая сборку потребителей публичного CR; сохранить результаты и убедиться, что все обязательные ID матрицы выполнены.
- [x] 10.5 Сверить реализованное поведение со всеми delta specs и выполнить openspec validate add-operator-ha-and-mutations --strict; закрывать задачи только при наличии указанного в них результата проверки.
- [x] 10.6 После реализации синхронизировать delta specs с основными спецификациями, включая ci-cd, backend-environments и backend-foundation; проверить согласованность вариантов оператора на три/четыре ноды с test-orchestration, сохранение трёхнодового dev и сценариев 13/14 нод, затем выполнить строгую проверку изменения и затронутых основных спецификаций.
- [x] 10.7 Сохранить длительности полного `operator just test`, вывести самые долгие сценарии и сверить бюджеты Go/CI с фактическим запуском; сначала устранить лишние ожидания и повторную подготовку, затем увеличивать timeout.

## 11. Исправления после ревью реализации

- [x] 11.1 Исправить ротацию при недоступной реплике: продолжать обновление доступных реплик и primary, но не подтверждать версию до возврата или доказанной остановки недоступного процесса; усилить `PW-06` и unit-тесты порядка обхода.
- [x] 11.2 Разрешить аварийное восстановление primary во время всех стадий rollout, включая полную остановку, не допуская пустой процесс поверх пригодных копий; проверить сочетание уменьшения RAM, ротации и отказа primary.
- [x] 11.3 Убрать блокировку failover незавершённым восстановлением другой реплики и дать `reconfiguring` безопасный путь повторного выбора после отказа нового primary; проверить оба сценария с сохранёнными стадиями.
- [x] 11.4 После рестарта накапливать новую серию транспортных ошибок перед удалением, а любой ответ Valkey отменять ещё не принятый DELETE; проверить `BUSY`, `LOADING`, `WRONGPASS` и сохранённое ожидание.
- [x] 11.5 Исправить время жизни клиентов `LeaseScope`, ошибки свежего чтения Pod и Node, отсчёт пустого primary и сохранение истории прежних процессов; добавить unit/envtest на освобождение ресурсов и запрет действий по устаревшим данным.
- [x] 11.6 Не переводить исправный инстанс в `unavailable` из-за пустого кэша Envoy или одной транспортной ошибки; убрать повторные записи неизменных ресурсов и тяжёлые действия каждого секундного прохода, сохранив немедленную реакцию на подтверждённую потерю.
- [x] 11.7 Сохранить fencing при допуске процессов во время ротации и failover, исправить остановку после `killingBusy`, ошибку чтения Secret при удалении и единое владение `ProcessReady`; добавить Events для потери доказательства, `node_deleted` и ожидания ручного fencing.
- [x] 11.8 Согласовать `SYSTEM.md`, `SYSTEM-DETAIL.md`, `operator/AGENTS.md` и design с фактическим контрактом ручного fencing, кодами причин, тестовым профилем длительностей и запуском оператора внутри k3s.
- [x] 11.9 Исправить обвязку: проверять журналы без учёта `.gitignore`, явно проверять наличие `rg`, формировать матричный отчёт и диагностику при раннем падении, сохранять длительности в CI и проверять отсутствие build tag `integration` в release-сборке.
- [x] 11.10 Усилить отмеченные ревью сценарии `CT-01`, `CT-02`, `CT-06`, `CT-07`, `CT-13`, `CT-14`, `HA-03`, `HA-06`, `NT-05`, `OP-03`, `OP-04`, `OP-06`, `ND-08`, `DL-03` и отрицательные окна так, чтобы отчёт различал unit, envtest, обвязку и k3s и не засчитывал ручную подмену состояния как целевое воздействие.
- [x] 11.11 Измерить повторный полный запуск, устранить лишний bootstrap и загрузку образов при добавлении или восстановлении agent, затем сверить групповые таймауты с бюджетом CI без ослабления обязательных проверок.
- [x] 11.12 Выполнить генерацию, форматирование, линтинг, сборку `operator` и `api`, unit/envtest, полный `operator just test`, строгую проверку OpenSpec и обновить `verification.md` по новому запуску без skip.
