## 1. Параметры и владение окружениями

- [x] 1.1 Ввести общий шаблон dev/api/operator/integration/e2e с топологиями 3/1/3/3/3 и независимыми тестовыми Compose projects; проверить разрешённые Compose-конфигурации и сохранение имени существующего dev-project и его томов.
- [x] 1.2 Ввести run ID, отдельные каталоги состояния и перечень принадлежащих запуску ресурсов; проверить, что два параллельных запуска не перезаписывают состояние и повторная очистка использует только записанные цели.
- [x] 1.3 Добавить выделение непересекающихся Docker/Pod/Service CIDR и согласованного cluster-dns с блокировкой; проверить конкурентное выделение, конфликт с существующим маршрутом и отказ до изменения сети при исчерпании диапазонов.
- [x] 1.4 Назначать тестам свободные localhost-порты и передавать фактические адреса, сохранив стабильные dev-порты; проверить одновременное подключение ко всем пяти Kubernetes API.

## 2. Подготовка k3s и PostgreSQL

- [x] 2.1 Параметризовать bootstrap и получение kubeconfig по project и окружению; проверить повторный bootstrap двух кластеров и отсутствие изменений CRD, Helm-релиза и маршрутов соседа.
- [x] 2.2 Добавить облегчённую подготовку API-кластера с CRD и RBAC и полную подготовку остальных профилей; проверить готовность 1/3 нод, отсутствие оператора и Envoy в API и готовность Gateway в полном профиле.
- [x] 2.3 Выдавать отдельные K3S_TOKEN и kubeconfig API/operator/admin с режимом 0600; проверить фактические адреса и RBAC: API создаёт CR, но не меняет status и не читает поды.
- [x] 2.4 Ограничить настройку и очистку host-маршрутов выбранным кластером; проверить прямой доступ к Pod IP двух полных стендов и сохранение чужого маршрута после cleanup одного из них.
- [x] 2.5 Подготовить общий локальный PostgreSQL и отдельные базы запусков api/integration/e2e с `goose -allow-missing`; проверить первый запуск без базы, одновременную подготовку, приоритет тестового DATABASE_URL и отказ очистки dev-базы.
- [x] 2.6 Реализовать очистку собственных процессов, тестовых баз, кластеров и томов после успеха, ошибки и сигнала; проверить повторный cleanup и сохранение общего PostgreSQL, dev и соседнего тестового запуска.

## 3. Полные команды подсистем

- [x] 3.1 Заменить генерацию в тестовых командах проверкой во временном каталоге через существующую обвязку; проверить ошибку при устаревших артефактах и отсутствие перезаписи исходников при параллельной компиляции.
- [x] 3.2 Перевести operator lifecycle на выделенный кластер и параметры сети/портов, включая адреса whitelist-клиентов; проверить полный существующий TestSingleLifecycle при одновременно работающем dev-операторе.
- [x] 3.3 Объединить unit/envtest/k3s этапы в `operator/just test`, убрать публичные test-fast/test-full и отключить кэш результатов инфраструктурных Go-тестов; проверить список команд, полный успешный прогон и ненулевой код при недоступном стенде.
- [x] 3.4 Обновить `api/just test` для собственной базы и однонодового k3s, сохранив настоящие HTTP-тесты и последовательность проверок глобальной квоты; проверить все существующие тесты и отсутствие доступа к dev-данным. Подключение тестов синхронизации остаётся следующему change.
- [x] 3.5 Добавить `web/just test` как вход в существующий полный pnpm test; проверить, что набор выполняется без Kubernetes и сообщает исходный код ошибки тестов.

## 4. Корневые команды

- [x] 4.1 Расширить `just run`: подготовка dev-сервисов, readiness, bootstrap, миграции и запуск оператора, API и фронта; проверить запуск без ручного bootstrap и сохранение данных после повторного запуска.
- [x] 4.2 Добавить управление группами процессов, защиту от двух dev-run и `just down` без удаления данных; проверить Ctrl+C, неожиданный выход приложения, конфликт повторного run и независимость тестовых процессов.
- [x] 4.3 Добавить корневой `just test` с параллельным запуском существующих наборов и ожиданием всех результатов; проверить пересечение времени выполнения наборов, продолжение соседей после ошибки и итоговый ненулевой код.
- [x] 4.4 Добавить отдельные журналы, диагностику Kubernetes до cleanup и итоговую сводку с путями; проверить доступность диагностики после удаления стенда и отсутствие Secret, паролей и kubeconfig в публикуемых файлах.
- [x] 4.5 Подготовить точки подключения integration/e2e и явный отчёт об ещё не реализованных наборах без успешных заглушек; проверить, что отсутствующий будущий набор не отображается как прошедший, а пустой либо пропущенный подключённый набор даёт ошибку.

## 5. CI и документация

- [x] 5.1 Перевести jobs API, operator и web на единые команды с собственными окружениями; проверить workflow, обязательность медленных тестов, сбор диагностики при ошибке и отсутствие автоматической отмены соседних jobs. Зафиксировать способ подключения integration/e2e jobs вместе с будущими сценариями.
- [x] 5.2 Кратко объяснить в `SYSTEM.md` пять окружений, разделение баз, назначение корневых `just run` / `just test` и единые тестовые команды подсистем; проверить соответствие таблицы топологиям 3/1/3/3/3 и отсутствие обещания ещё не реализованных сквозных проверок.
- [x] 5.3 Кратко описать в `SYSTEM-DETAIL.md` подготовку, сетевую изоляцию, передачу kubeconfig, сохранение dev-данных и очистку тестовых запусков; проверить, что описание соответствует реализованным скриптам и не дублирует подробный перечень задач.
- [x] 5.4 Обновить корневой и локальные AGENTS.md, примеры env и применимые инструкции тестов, включая e2e/TODO.md; поиском проверить отсутствие действующих указаний запускать test-fast/test-full или использовать dev-кластер для тестов. Исторические архивы OpenSpec не переписывать.

## 6. Общая проверка

- [x] 6.1 Одновременно подготовить dev и четыре тестовых окружения, проверить 13 нод и независимость объектов каждого кластера; затем очистить тестовые окружения и проверить сохранность контрольного объекта и данных dev.
- [x] 6.2 Выполнить корневой `just test` при работающем `just run`, проверить все существующие наборы и отсутствие записей тестов в dev-базе и dev-кластере.
- [x] 6.3 Проверить прерывание общего запуска и ошибку bootstrap одного набора: остальные результаты собраны, собственные ресурсы очищены либо точно перечислены, соседние окружения доступны.
- [ ] 6.4 Измерить пиковые ресурсы и время подготовки/тестов локально и в GitHub Actions, проверить размещение подов с их requests; сохранить краткий результат проверки и устранить обнаруженные ограничения без отключения тестов.
- [x] 6.5 Проверить согласованность изменённых спецов, документации и команд, выполнить строгую валидацию OpenSpec и применимые проверки форматирования, линтинга и сборки; завершать реализацию только при успешных обязательных проверках.
