## 1. Суммарная ёмкость API

- [x] 1.1 Заменить понодовые параметры конфигурации на `MANAGED_K8S_CLUSTER_VCPU` и `MANAGED_K8S_CLUSTER_RAM_GB`, добавить целочисленный расчёт 85% CPU / 90% RAM и явный отказ для семи старых переменных; проверить затронутые тесты `api/internal/config` для 12/48, 4/16, округления, отсутствующих, неверных и переполняющих значений.
- [x] 1.2 Заменить `ClusterTopology` суммарной ёмкостью, использовать внутренние бюджеты 10200m / 44236 MiB для 12/48 и 3400m / 14745 MiB для 4/16, возвращать публичные лимиты 10/43 и 3/14 без изменения DTO `/capacity`; проверить затронутые unit- и HTTP-тесты ёмкости.
- [x] 1.3 Сохранить множитель процессов 1 для `single` и 3 для `ha`, переименовать вводящее в заблуждение внутреннее понятие `NodeCount` в число процессов и проверить тесты каталога, цены и резерва.
- [x] 1.4 Удалить placement-поиск, `placementCandidate`, deadline, `ValkeyReservations` и ответы `placement_capacity`/`placement_check_timeout`; заменить placement-тесты сценариями принятия физически неразмещаемого, но проходящего по сумме запроса и проверить затронутые API-тесты через подготовленную тестовую PostgreSQL.
- [x] 1.5 Проверить отказ `single` 4/16 на пустом физическом кластере 4/16 с достаточной личной квотой, а также атомарную суммарную квоту для конкурентных create/resize, идемпотентного повтора, неподтверждённого уменьшения и удаления; во время реализации запустить соответствующие тесты квот и конкуренции до финального полного прогона.

## 2. Конфигурация окружений и каталог

- [x] 2.1 Перевести `docker-compose.prod.yml` на `MANAGED_K8S_CLUSTER_VCPU=12` и `MANAGED_K8S_CLUSTER_RAM_GB=48`, сохранить `VALKEY_INSTANCE_MAX_VCPU=4` и `VALKEY_INSTANCE_MAX_RAM_GB=16`; проверить итоговый `docker compose config` без вывода секретов.
- [x] 2.2 Обновить `.env.example`, локальные и тестовые значения двумя суммарными параметрами, удалить передачу старой топологии из `scripts/test_e2e.sh` и `scripts/test_integrations.sh`; проверить синтаксис изменённых shell-файлов и их точечные runner-тесты.
- [x] 2.3 Пересчитать E2E-подготовку исчерпания общего бюджета от целого суммарного лимита `/capacity`, а не от числа и размера нод; добавить точечную проверку интерфейса, что при достаточной личной квоте и кластерном лимите 3/14 размер `single` 4/16 виден, но недоступен из-за ресурсов Managed Kubernetes; во время реализации проверить TypeScript-типы и целевой сценарий `cluster-capacity` до финального полного Playwright-набора.

## 3. Мягкое размещение HA

- [x] 3.1 Заменить обязательный `PodAntiAffinity` HA на `TopologySpreadConstraint` с `maxSkew=1`, `kubernetes.io/hostname`, `ScheduleAnyway` и selector одного инстанса; проверить затронутые unit- и envtest-тесты workload, отсутствие ограничения у `single` и сохранение PDB `minAvailable=2`.
- [x] 3.2 Разделить общий integration-helper на проверку полного состава и отдельную проверку распределения по hostname; проверить компиляцию всех вызывающих тестов и отсутствие неявного требования трёх уникальных нод.
- [x] 3.3 Добавить целевые k3s-сценарии создания HA на одной, двух и трёх нодах с проверкой primary, двух синхронизированных реплик, `running`, TLS-записи и RO-чтения; запустить только эти новые сценарии через каталог оператора.
- [x] 3.4 Переписать сценарий потери одной из трёх нод так, чтобы при достаточных ресурсах полный состав восстанавливался на двух оставшихся до возврата agent; отдельно проверить потерю ноды с двумя процессами для выжившего primary и выжившей replica, запустив только изменённые fault-сценарии.
- [x] 3.5 Изменить тест настоящей нехватки replacement capacity так, чтобы `Pending` создавался cordon или недостаточными `resources.requests`, а не anti-affinity; запустить этот сценарий и подтвердить `degraded` до возврата жёсткой ёмкости.
- [x] 3.6 Проверить resize и ротацию пароля на совместно размещённом HA, исправить предположения о разных нодах и запустить только изменённые mutation-сценарии.

## 4. E2E и тестовая матрица

- [x] 4.1 Изменить `worker-node-loss` и Kubernetes helper: после потери primary worker ждать `running` с тремя процессами на двух нодах до восстановления agent; запустить только группу или точный Playwright-сценарий отказа worker-ноды.
- [x] 4.2 Обновить `scripts/operator_test_scenarios.tsv`, проверки каталога, node count и оценки ресурсов для новых одно- и двухнодовых HA-сценариев и переименованных fault/mutation-тестов; запустить проверку согласованности каталога до подготовки k3s.
- [x] 4.3 Сохранить полную аварийную матрицу в `operator just test-full`; во время реализации использовать целевые сценарии, а в финальном прогоне выполнить всю матрицу и считать любой пропущенный или неуспешный обязательный сценарий ошибкой.

## 5. Документация и выпуск

- [x] 5.1 Обновить `SYSTEM.md` и `SYSTEM-DETAIL.md`: описать суммарную физическую конфигурацию 12/48, резерв 15%/10%, учётный лимит 10/43, отказ размера 4/16 на физическом кластере 4/16, отсутствие гарантии раскладки и мягкое HA-размещение; проверить поиском отсутствие действующих описаний старых переменных и обязательного разнесения Valkey.

## 6. Полная проверка

- [x] 6.1 Отформатировать только изменённые Go-, TypeScript- и Markdown-файлы, выполнить линтеры затронутых приложений и проверить, что форматирование не добавило в индекс чужие изменения.
- [x] 6.2 После завершения реализации и документации проверить ресурсы хоста, затем выполнить `just lint`, `just test`, `just test-full`, `just test-integrations` и `just test-e2e`; контролировать ресурсы во время тяжёлых наборов и после них проверить адресную очистку принадлежащих запуску процессов и контейнеров.
- [x] 6.3 При падении любой команды исправить причину в коде, тесте или локальной тестовой обвязке без отключения сценария и ослабления проверки; повторить сначала затронутый тест, затем упавшую полную команду и не завершать задачу, пока все пять команд не пройдут.
- [x] 6.4 Выполнить `openspec validate aggregate-cluster-capacity-and-soft-ha-placement --strict`, просмотреть итоговый diff и перечислить результаты всех обязательных локальных команд.
