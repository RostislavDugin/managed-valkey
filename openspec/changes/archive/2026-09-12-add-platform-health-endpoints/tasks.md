## 1. Сводное состояние платформы

- [x] 1.1 Добавить чтение полей активных `valkey_instances`, нужных для мониторинга, одним запросом без миграции; интеграционным тестом хранилища проверить выбор строк с `deleted_at IS NULL` и отмену по контексту.
- [x] 1.2 Реализовать чистую классификацию операций, фаз, признаков восстановления и свежести наблюдений; unit-тестами проверить границы 60 и 300 секунд, создание, изменение пароля, удаление, `degraded`, `unavailable` и отсутствие двойной ошибки при удалении.
- [x] 1.3 Собрать параллельные проверки PostgreSQL и Kubernetes под общим сроком 5 секунд; тестами управляемых зависимостей проверить порядок итоговых состояний, независимость результатов, `disabled`, `unknown`, отмену и длительность параллельного запроса.

## 2. Проверка Valkey

- [x] 2.1 Реализовать разбор `X-Valkey-Primary` и необязательного `X-Valkey-Read`; табличными unit-тестами подтвердить допустимый `rediss://app:<password>@host:port` и отклонение IP, другого домена, порта, схемы, пользователя, path, query, fragment, повторного заголовка и несовпадающего read label до сетевого обращения.
- [x] 2.2 Реализовать параллельные TLS/AUTH/PING подключения через `valkey-go` без повторов и команд с данными; тестовым TLS-сервером проверить PONG, неверный сертификат, пароль, зависание, сохранение успешного соседнего результата и закрытие соединений.
- [x] 2.3 Свести ошибки Valkey к безопасным машинным кодам и проверить захватом ответа и логов, что URI, host, пароль и исходные ошибки не сохраняются и не возвращаются.

## 3. HTTP API и запуск приложения

- [x] 3.1 Зарегистрировать публичные `GET /health` и `GET /valkey-health`, добавить типизированные ответы, HTTP-статусы и `Cache-Control: no-store`; тестами через `httptest.Server` проверить `200`, `400`, `503`, предупреждения и наличие результатов всех завершившихся проверок.
- [x] 3.2 Передать сервису состояния PostgreSQL, Kubernetes client, флаг синхронизации, домен и порт из существующей конфигурации; тестами запуска маршрутизатора подтвердить состояние `disabled` и неизменную семантику `/livez` и `/readyz`.

## 4. Рабочий маршрут и инструкция мониторинга

- [x] 4.1 Добавить `/health` и `/valkey-health` в matcher API Caddy и общую зону на 30 запросов в минуту с одного IP; собрать образ Caddy, выполнить `caddy validate` и HTTP-проверкой контейнера подтвердить передачу маршрутов и ответ `429` после исчерпания лимита.
- [x] 4.2 Обновить `SYSTEM.md` и `SYSTEM-DETAIL.md`: описать контракт, безопасные заголовки, требование whitelist для исходящего адреса API и исходные настройки Uptime Kuma 30 секунд и шесть повторов; поиском по документации проверить, что прежнее утверждение об отсутствии автоматического мониторинга удалено и секреты не приведены как значения.

## 5. Сквозная проверка

- [x] 5.1 Дополнить один общий интеграционный сценарий запросами `/health` и `/valkey-health` к готовому инстансу через настоящий API, Kubernetes, PostgreSQL, Envoy и TLS; запустить этот сценарий через `MV_INTEGRATION_TEST` и подтвердить успешный primary, а для HA также read.
- [x] 5.2 Отформатировать изменённый Go-код, выполнить `api just lint`, `api just test`, проверку Caddy и `openspec validate add-platform-health-endpoints --strict`; убедиться, что проверки проходят и не меняют файлы других приложений.
