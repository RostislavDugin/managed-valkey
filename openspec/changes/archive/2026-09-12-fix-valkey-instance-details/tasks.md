## 1. Контракт API и данные

- [x] 1.1 Добавить Goose-миграцию, которая заполняет `host_ro` существующих `single` на основе сохранённых `slug` и `host`, закрепляет непустое значение для активных строк и корректно откатывается; проверить применение через `goose -allow-missing` на новой схеме и на схеме с уже применённой более поздней миграцией.
- [x] 1.2 Создавать и возвращать `<slug>-ro.<domain>` для `single` и `ha`, сохранить адрес в DTO и credentials как постоянную часть записи; запустить точечные тесты создания, чтения credentials, коллизии slug и смены серверного домена.
- [x] 1.3 Расширить POST создания необязательным `maintenance`, применить проверку границ PATCH, нормализовать отсутствие и `null` для идемпотентного отпечатка и сохранить окно в транзакции создания; запустить точечные HTTP-тесты допустимых, ошибочных, повторных и откатываемых запросов.
- [x] 1.4 Обновить клиентские DTO общей интеграции и тестовые данные API для обязательного `host_ro` и начального окна обслуживания; скомпилировать затронутые Go-пакеты и запустить только изменённые тестовые функции.

## 2. Два маршрута single в операторе

- [x] 2.1 Добавить для `single` SNI-маршрут `<slug>-ro.<base-domain>` к тому же primary Service с общими TLS, whitelist и idle timeout, включая обновление и удаление ресурсов; проверить проекцию объектов точечными unit/envtest.
- [x] 2.2 Включить основной маршрут и маршрут чтения `single` в отпечаток и сверку обеих реплик Envoy, не подтверждать первоначальный `running` при ошибке любого маршрута; запустить затронутые unit/envtest по именам.
- [x] 2.3 Расширить `Test_RunSingleLifecycle_EnforcesAccessIsolationAndSafeRecovery` проверкой двух SNI, чтения записанного значения через `-ro`, одинакового поведения белого списка и закрытия обоих адресов при удалении; обновить строку `single-lifecycle` в каталоге, если имя теста изменится.
- [x] 2.4 До и после точечного k3s-сценария `single-lifecycle` проверить RAM, CPU, процессы и контейнеры, запустить только эту строку каталога через временный выбор сценария и убедиться, что штатная очистка освободила принадлежащие запуску ресурсы.

## 3. Форма и карточка web

- [x] 3.1 Добавить `maintenance` в модель формы создания и тело POST без дополнительного PATCH; обновить тест API-клиента и проверить точный JSON для включённого, выключенного и неуказанного окна.
- [x] 3.2 Добавить в форму создания необязательные поля дня недели, часа UTC и длительности с понятными подписями и проверкой границ; проверить значения по умолчанию, ошибки и успешную отправку в `CreateValkeyPage.test.tsx`.
- [x] 3.3 Отделить изменение окна обслуживания от переименования, добавить действие в строку «Окно обслуживания» и поддержать задание, замену и очистку через PATCH только с `maintenance`; проверить доступность действия во время операции и блокировку после начала удаления.
- [x] 3.4 Всегда показывать строки «Primary» и «Для чтения», форматировать отображаемые и копируемые значения как `redis://<host>:<port>` и включить обе роли в примеры подключения; проверить `single`, `ha`, загрузку credentials и копирование полного значения.
- [x] 3.5 Заменить видимые варианты «Доступ по IP» и «Белый список адресов» на «Белый список», изменить аудит на «Изменён белый список» и подпись свойства на «Создано», не меняя «Создана база»; проверить тексты, доступные имена элементов и отсутствие устаревших надписей в затронутых Vitest-файлах.

## 4. Точечные сквозные проверки

- [x] 4.1 Добавить безопасный способ выбрать одну Go-функцию общей интеграции, одну строку каталога оператора и один файл Playwright без запуска остальных сценариев; обновить тесты скриптов и проверить, что пустой или неизвестный выбор завершается ошибкой до подготовки стенда.
- [x] 4.2 Расширить `Test_CreateSingleValkey_WithRealApiAndOperator_BecomesReachableAndRunning`: создать окно обслуживания через HTTP, получить оба адреса, записать через primary и прочитать через `-ro`; запустить только эту функцию в отдельном integration-окружении.
- [x] 4.3 Обновить `00-single-lifecycle.spec.ts` и помощники Playwright: создать базу с окном обслуживания, проверить строки «Primary», «Для чтения», `redis://`, «Белый список» и «Создано», изменить окно из карточки и подключиться по обоим адресам; запустить только этот файл.
- [x] 4.4 Перед каждым точечным k3s или Playwright-запуском, во время него и после завершения проверить RAM, CPU, процессы и контейнеры; использовать штатную очистку только своего стенда и подтвердить освобождение ресурсов.

## 5. Очистка выполненных планов

- [x] 5.1 Сопоставить каждый раздел `operator/TODO.md` с текущими именованными operator k3s, общими интеграционными, Playwright e2e и production-сценариями; сохранить найденные соответствия в итоговом отчёте и не удалять несопоставленный пункт без отдельного согласования.
- [x] 5.2 Удалить выполненные пункты и пустой `operator/TODO.md`, убрать действующие ссылки и утверждения о будущих сценариях из `operator/AGENTS.md`, `SYSTEM-DETAIL.md` и других активных документов; поиском проверить отсутствие ссылок вне `openspec/changes/archive`.
- [x] 5.3 Обновить описания локальных проверок так, чтобы они предписывали затронутые тесты и точечные сценарии без обязательного `test-full`; проверить согласованность корневых и компонентных инструкций поиском по активным документам.

## 6. Проверка изменения

- [x] 6.1 Отформатировать только изменённые файлы штатными форматтерами соответствующих приложений, проверить рабочее дерево и убедиться, что посторонние изменения не добавлены в индекс и не перезаписаны.
- [x] 6.2 Запустить линтер, проверку типов, сборку и `go mod tidy -diff` только для затронутых API, оператора и web; исправить найденные ошибки и повторить упавшую проверку.
- [x] 6.3 Повторить только перечисленные в задачах точечные API, operator unit/envtest, operator k3s, Vitest, общую интеграцию и Playwright-проверки; не запускать корневой или компонентный `test-full` и не выдавать точечный результат за полный набор.
- [x] 6.4 Выполнить `openspec validate fix-valkey-instance-details --strict`, проверить отсутствие незапланированных файлов в diff и составить отчёт с точными командами, результатами и явно указанными границами проверки.
