## 1. Сервис аудита и хранилище

- [x] 1.1 Добавить в `api/internal/audit` модели публичного события, страницы и курсора, отдельные контракты чтения и записи, проверку параметров и base64url-кодирование; проверить допустимые границы, неверные курсоры и сохранение микросекунд тестами пакета.
- [x] 1.2 Реализовать в `api/internal/store` чтение `limit + 1` событий по `service`, `resource_id` и составной позиции `(created_at, id)`; проверить сортировку и отсутствие пропусков на одинаковом времени интеграционными тестами.
- [x] 1.3 Сохранить порядок создания инстанса: получить ID, записать аудит в той же транзакции и выполнить фиксацию только после обеих операций; проверить существующие тесты атомарности без изменения схемы базы.

## 2. HTTP API

- [x] 2.1 Передать отдельный сервис аудита в маршрутизатор, зарегистрировать `GET /v1/managed/valkey/instances/{id}/audit` внутри группы с общей проверкой владельца и не добавлять изменяющие методы; проверить регистрацию маршрута тестом через HTTP.
- [x] 2.2 Разобрать `limit` и `before`, отклонить неизвестные, повторяющиеся и неверные параметры и вернуть только `id`, `action`, `user_email`, `created_at` и `next_cursor`; проверить контракт и ошибки через `httptest.Server` и настоящий маршрутизатор.
- [x] 2.3 Покрыть HTTP-тестами изоляцию владельцев, страницы с одинаковым временем, пустой журнал и чтение после принятого удаления; проверить, что чужой ID возвращает `404 NOT_FOUND`, а курсор не расширяет доступ.

## 3. Подключение фронтенда

- [x] 3.1 Установить зависимости командой `pnpm install`, добавить в `pages/valkey/model/valkey-audit.ts` типы, группировку местных дат и подписи шести действий; проверить «Сегодня», «Вчера», другой год и `instance.whitelist.update` модульными тестами.
- [x] 3.2 Добавить в `pages/valkey/api/valkey-audit.ts` запрос серверного журнала и преобразование полей ответа без `ownerId` и локального хранилища; проверить URL, `limit`, передачу непрозрачного `before`, DTO и `AbortSignal` тестами клиента.
- [x] 3.3 Заменить заглушку `ValkeyAuditPage` журналом и стилями для широкой и мобильной раскладки; проверить загрузку, данные, пустой ответ, ошибку, повтор, кнопку «Показать ещё», защиту от двойного нажатия и отменённого ответа компонентными тестами.

## 4. Общая проверка

- [x] 4.1 Запустить `just test` в `api` и устранить ошибки интеграционных тестов, миграций и сборки.
- [x] 4.2 Запустить `pnpm check` в `web` и устранить ошибки форматирования, FSD-границ, типов, тестов и производственной сборки.
- [x] 4.3 Запустить `openspec validate connect-valkey-audit --strict` и проверить, что реализация соответствует обоим изменённым требованиям.
