## 1. Дочерние команды

- [x] 1.1 Разделить API-тесты по зависимости от Kubernetes: пометить k3s-зависимые проверки явным build tag, добавить режим окружения только с PostgreSQL и проверить, что `api just test` не создаёт k3s, а `api just test-full` выбирает весь набор
- [x] 1.2 Перестроить `operator/Justfile`: направить `test` на unit/envtest, `test-full` на полную k3s-матрицу, убрать публичный `test-ci` и проверить список рецептов без запуска полного набора
- [x] 1.3 Добавить в `api` и `operator` команды `format` и `lint`, включая общий Go-код, сборку, `go mod tidy -diff` и проверку сгенерированных ресурсов оператора; проверить область добавленных в индекс файлов на временных изменениях
- [x] 1.4 Добавить в `web/Justfile` команды `run`, `format`, `lint` и `test-full`; проверить делегирование существующим pnpm-командам и добавление в индекс только файлов `web`

## 2. Корневая маршрутизация

- [x] 2.1 Переделать общий сценарий тестов для выбора `test` или `test-full`, параллельного запуска API, оператора и web, раздельных журналов и корректной отмены; проверить оба маршрута подменёнными быстрыми командами без k3s
- [x] 2.2 Исключить e2e из обоих агрегатов и вывести integration как не реализованный только в отчёте `test-full`; проверить, что заглушка не считается пройденным набором и не делает успешные реализованные тесты ошибочными
- [x] 2.3 Добавить корневые `format` и `lint`: форматирование выполнять последовательно с добавлением областей приложений в индекс, линтинг выполнять параллельно; проверить ненулевой итог при ошибке любого дочернего линтера
- [x] 2.4 Сохранить единый корневой `run`, перевести запуск web на его дочерний рецепт и проверить по маршрутизации, что стартуют инфраструктура, API, оператор и web
- [x] 2.5 Сохранить отдельные `test-e2e` и `test-e2e-headed`, переименовать `test-prod` в `test-e2e-prod` во всех `Justfile` и проверить отсутствие старого публичного рецепта

## 3. CI и инструкции

- [x] 3.1 Перевести задания CI API, оператора и web на дочерний `just test`, убрать k3s-подготовку API и сохранить e2e отдельным заданием; проверить структуру workflow без его удалённого запуска
- [x] 3.2 Обновить корневой и дочерние `AGENTS.md`: описать локальные `format`, `lint`, `test`, `test-full`, правила для общего Go-кода и запрет параллельного форматирования одной области; проверить согласованность команд с `just --list`
- [x] 3.3 Обновить `SYSTEM.md`, `SYSTEM-DETAIL.md` и документацию e2e: разделить быстрый и полный наборы, убрать e2e из агрегатов и заменить `test-prod`; проверить поиском, что старые утверждения и имя команды не остались

## 4. Быстрая проверка изменения

- [x] 4.1 Выполнить `just format`, проверить состав индекса и убедиться, что команда не создала коммит и не добавила файлы вне API, оператора, общего Go-кода и web
- [x] 4.2 Выполнить корневой `just lint`, дождаться результатов всех трёх приложений и исправить найденные ошибки
- [x] 4.3 Выполнить только корневой `just test`, подтвердить параллельный запуск быстрых наборов, отсутствие k3s и e2e и ориентир до пяти минут при установленных зависимостях
- [x] 4.4 Проверить `just --list` в корне и каждом приложении, тесты маршрутизации обвязки и `openspec validate unify-just-commands --strict`; `just test-full`, `just test-e2e`, `just test-e2e-headed` и `just test-e2e-prod` не запускать
