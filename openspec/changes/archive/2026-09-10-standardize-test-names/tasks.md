## 1. Правило и координация

- [x] 1.1 Перед реализацией прочитать `temp/execution.md`, записать время, область и пересекающиеся файлы изменения `standardize-test-names`; не изменять и не восстанавливать файлы параллельного `unify-just-commands`, а спорные изменения накладывать только на его актуальную версию
- [x] 1.2 Добавить в корневой `AGENTS.md` правило запуска изменённых тестов и соглашение для Go-идентификаторов, русских строковых сценариев, групп, табличных вариантов и бывших самостоятельных функций; проверить примеры и отсутствие противоречия с разрешённой для этой миграции облегчённой проверкой
- [x] 1.3 Снять окончательный список тестовых объявлений и точных ссылок перед переименованием; подтвердить, что учтены `api`, общий `internal`, `operator`, `web`, `tests/e2e`, каталог оператора, фильтры и shell-проверки запуска

## 2. API и общий Go-код

- [x] 2.1 Параллельным агентом прочитать тела всех тестов в `api`, переименовать самостоятельные функции в формат `Test_<WhatWeDo>_<WhichConditions>_<WhatWeExpect>` и дать самостоятельным `t.Run` понятные русские названия; проверить поиском отсутствие старого формата и кратких поведенческих строк
- [x] 2.2 Тем же агентом переименовать тесты и самостоятельные подтесты в общем `internal`, сохранив короткими только значения табличных вариантов; проверить поиском все объявления в этой области

## 3. Unit- и envtest-тесты оператора

- [x] 3.1 Параллельным агентом переименовать тесты в `operator/api` и `operator/internal/config`, `operator/internal/valkey`, читая проверки перед выбором имени; проверить соответствие имён фактическим утверждениям
- [x] 3.2 Тем же агентом переименовать unit- и envtest-тесты в `operator/internal/operator`, сохранить идентификаторы матрицы отдельным сегментом после `Test_` или обязательного префикса `Test_Envtest_` и дать самостоятельным `t.Run` русские сценарные названия; проверить поиском всю область

## 4. Integration и запуск оператора

- [x] 4.1 Отдельным параллельным агентом переименовать 62 функции в `operator/integration`, сохранив идентификаторы обязательной матрицы и явно назвав действие, условия и результат; проверить число обнаруженных integration-тестов без их выполнения
- [x] 4.2 В той же области владения синхронно обновить точные выражения в `scripts/operator_test_scenarios.tsv`; выполнить `scripts/validate_operator_test_scenarios.sh` и подтвердить единственное назначение каждого из 62 тестов
- [x] 4.3 Обновить активные фильтры envtest, шаблоны отчётов и искусственные имена Go-тестов в shell-проверках запуска; проверить затронутые скрипты через `bash -n` и убедиться поиском, что активные файлы не закрепляют старые имена

## 5. Frontend и e2e

- [x] 5.1 Параллельно с Go-направлениями переименовать все прямые и параметрические сценарии `it`, `test`, `it.each` и `test.each` в `web` длинными русскими формулировками, оставить `describe` понятными названиями групп, а подставляемые значения короткими только при наличии полного родительского контекста; проверить все 100 объявлений Vitest, включая четыре вызова `.each`
- [x] 5.2 Переименовать Playwright-сценарий в полную русскую формулировку и перенести `@prod` в `TestDetails.tag`; проверить типы e2e и сохранение выбора через `--grep @prod` без запуска браузера

## 6. Объединение и облегчённая проверка

- [x] 6.1 Объединить результаты параллельных направлений поверх актуального рабочего дерева, свериться с записями `temp/execution.md` и проверить diff на отсутствие изменений подготовки, действий и утверждений тестов
- [x] 6.2 Проверить обычные Go-пакеты через `go test -run '^$' ./api/... ./internal/... ./operator/...`, envtest-вариант через `go test -run '^$' -tags=envtest ./operator/... ./internal/...` и integration-вариант через `go test -run '^$' -tags=integration ./operator/integration`; тестовые сценарии при этом не запускать
- [x] 6.3 Выполнить проверку типов `web` и `tests/e2e`, синтаксическую проверку изменённых shell-файлов и повторную проверку каталога оператора; исправить ошибки без запуска Vitest, Playwright, API с PostgreSQL или k3s-матрицы
- [x] 6.4 Отдельным поиском прямых вызовов и `.each` проверить все 331 Go-функцию, 69 мест `t.Run`, 100 объявлений Vitest и один Playwright-сценарий, затем выполнить `openspec validate standardize-test-names --strict` и в итоговом отчёте перечислить выполненные проверки и явно назвать полные наборы, которые не запускались
