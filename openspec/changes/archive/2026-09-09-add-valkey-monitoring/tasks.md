## 1. Таблица и чтение метрик

- [x] 1.1 Создать аддитивную goose-миграцию `valkey_node_metrics` и модель `store.ValkeyNodeMetric`; проверить внешнюю ссылку, ограничения, `null` CPU, уникальность снимка и оба индекса после применения миграций к тестовой базе.
- [x] 1.2 Реализовать запрос PostgreSQL для временных корзин, средних показателей, приростов счётчиков, смены `run_id` и пропусков; проверить сортировку, предыдущий снимок на границе окна и предел семи дней интеграционными тестами.

## 2. HTTP API

- [x] 2.1 Добавить модель окна и разбор `range` либо пары `from`, `to`; проверить готовые диапазоны, значение `1h` по умолчанию, неизвестные и несовместимые параметры, RFC 3339, обратные и слишком широкие окна модульными тестами.
- [x] 2.2 Добавить сервисный метод и DTO рядов по нодам с именем `<slug>-<ordinal>`, последней ролью и полями ответа из design; проверить пустой массив, `null` в корзинах и отсутствие `run_id` и демонстрационных даных в JSON.
- [x] 2.3 Зарегистрировать `GET /v1/managed/valkey/instances/:id/metrics` в существующей группе `owned`; проверить через настоящий TCP HTTP успешный запрос владельца, `401` без JWT и одинаковый `404` для чужой, удалённой и отсутствующей базы.
- [x] 2.4 Дополнить очистку данных тестов новой таблицей и прогнать `just test` в `api`; все тесты и применение миграций должны завершиться с нулевым кодом.

## 3. Фронтенд мониторинга

- [x] 3.1 Установить зависимости через `pnpm install`, восстановить модель метрик и клиент в `pages/valkey`, подключить `apiRequest` и преобразование серверного `snake_case`; проверить URL с каждым `range`, DTO, отмену запроса и отсутствие `ownerId` и `source: demo` тестами клиента.
- [x] 3.2 Восстановить `MetricChart`, стили и полный `ValkeyMonitoringPage` из истории, адаптировать роли `primary`, `replica`, `unknown` и пустое состояние «Метрик пока нет»; проверить четыре графика, единицы, последние значения, выбор нод, разрывы и клавиатурную навигацию компонентными тестами.
- [x] 3.3 Подключить последовательное обновление раз в пять секунд, смену окна и базы, загрузку, ошибку и повтор; проверить поддельными таймерами отсутствие параллельных запросов, сохранение выбранных нод и игнорирование позднего ответа прежнего запроса.
- [x] 3.4 Обновить тестовый HTTP-стенд и проверки маршрута мониторинга; проверить прямую ссылку, общий каркас базы, запрос `range=5m`, пустой ответ и отсутствие демонстрационных метрик.

## 4. Документация и итоговые проверки

- [x] 4.1 Обновить описание текущего этапа и точный ответ API метрик в `SYSTEM.md`, не добавляя фоновый сбор, импорт или очистку; проверить согласованность документа со спецификациями change.
- [x] 4.2 Запустить `pnpm check` в `web`; форматирование, границы FSD, типы, тесты и сборка должны завершиться с нулевым кодом.
- [x] 4.3 Запустить корневые проверки Go для затронутого API и `openspec validate --all --strict`; линтер, сборка и строгая проверка спецификаций должны завершиться с нулевым кодом.
