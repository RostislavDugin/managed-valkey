## 1. Подготовка

- [x] 1.1 Добавить строку `temp/` в корневой `.gitignore` и проверить, что `git status` больше не показывает файлы из `temp`
- [x] 1.2 Перенести `temp/favicon.ico` в `web/public/favicon.ico`, заменить в `web/index.html` заглушку `href="data:,"` на ссылку на файл и проверить, что после `pnpm dev` во вкладке виден значок
- [x] 1.3 Собрать `web/src/shared/ui/logo-wide.tsx` из `temp/logo.svg` с заливкой `currentColor` и размером через свойство, проверить читаемость в обеих схемах

## 2. Клиент api и заглушка входа

- [x] 2.1 Завести `web/src/shared/api/client` поверх `fetch`: базовый адрес, заголовок `Authorization`, разбор `{error: {code}}` в исключение с кодом; проверить разбор ответа с кодом на вымышленном ответе
- [x] 2.2 Завести `web/src/shared/api/auth` с `checkEmail`, `register`, `login`, `logout`, `getSession` поверх `localStorage`: ключи `mv_users` и `mv_token`, поддельный токен по форме JWT со сроком 7 дней, задержка ответа около 400мс; проверить полный цикл регистрации и входа вручную
- [x] 2.3 Реализовать коды ошибок заглушки (`VALIDATION_FAILED`, `UNAUTHORIZED`, `CONFLICT`) и проверить, что регистрация на занятую почту отвечает `CONFLICT`

## 3. Экран входа

- [x] 3.1 Создать `web/src/pages/auth` с маршрутом `/auth`, добавить путь в `web/src/shared/config/routes.ts`; проверить открытие страницы
- [x] 3.2 Собрать первый шаг: поле почты, проверка формата, вызов `checkEmail`, обработка `RATE_LIMITED` с показом `retry_after`; проверить все три исхода
- [x] 3.3 Собрать второй шаг для известной почты: одно поле пароля, вход, очистка поля и сохранение почты при ошибке; проверить вход верным и неверным паролем
- [x] 3.4 Собрать второй шаг для новой почты: два поля пароля, минимум 8 символов, совпадение значений, переход на форму входа при ответе `CONFLICT`; проверить каждую проверку
- [x] 3.5 Добавить подсказки CapsLock и кириллицы у полей пароля, состояние CapsLock слушать на уровне окна; проверить, что подсказка видна при включённом заранее CapsLock, пропадает при выключении мимо поля, и что подсказка о раскладке появляется и исчезает
- [x] 3.6 Добавить действие возврата к вводу почты со стиранием паролей; проверить возврат и повторный проход
- [x] 3.7 Разметить поля для менеджеров паролей (почта только для чтения и как имя пользователя, пароль как текущий или новый); проверить подстановку и предложение сохранить в браузере
- [x] 3.8 Оформить экран по образцу из `temp`: логотип над карточкой, карточка по центру, пометка о демонстрационном режиме под формой; проверить в обеих схемах

## 4. Сессия и защита маршрутов

- [x] 4.1 Перестроить `web/src/app/routes`: `/auth` с загрузчиком, уводящим вошедшего, и маршрут оболочки с загрузчиком, уводящим невошедшего; проверить оба перенаправления
- [x] 4.2 Сохранять адрес запрошенной страницы при перенаправлении и возвращать на него после входа; проверить входом с адреса вложенной страницы
- [x] 4.3 Добавить мятный индикатор на время первой проверки сессии, при котором ни консоль, ни форма не показываются; проверить, что защищённая страница не мелькает
- [x] 4.4 Добавить автоматический выход по истёкшему сроку токена, по ответу `UNAUTHORIZED` и по удалению `mv_token` в соседней вкладке, каждый с уведомлением; проверить все три пути
- [x] 4.5 Добавить мятную полосу загрузки на переходах внутри оболочки, оставляющую прежнюю страницу видимой; проверить на переходе с задержкой

## 5. Оболочка консоли

- [x] 5.1 Собрать оболочку с навигацией слева и содержимым справа по образцу `temp/sidebar.png`, ширина по токенам каркаса; проверить в обеих схемах
- [x] 5.2 Добавить логотип вверху навигации и пункт текущего раздела с заливкой, отличной от наведения; проверить три состояния пункта
- [x] 5.3 Собрать нижний блок: кнопка выхода во всю ширину, строка с почтой и обрезкой длинного адреса, ряд иконочных кнопок; проверить выход и обрезку
- [x] 5.4 Перенести переключатель цветовой схемы с `HomePage` в нижний блок, сохранить выбор между сессиями и на экране входа; проверить перезагрузкой
- [x] 5.5 Сделать навигацию выдвижной и закрытой по умолчанию ниже точки `tablet`, с закрытием после выбора раздела; проверить на узком окне

## 6. Документы

- [x] 6.1 Дополнить `web/DESIGN.md`: заливка текущего пункта навигации и состав нижнего блока с кнопкой выхода

## 7. Проверка

- [x] 7.1 Прогнать `pnpm check` в `web` и добиться нулевого кода
- [x] 7.2 Пройти сценарий целиком вручную в обеих схемах и на узком окне: регистрация, выход, вход, истечение токена, возврат на запрошенную страницу
