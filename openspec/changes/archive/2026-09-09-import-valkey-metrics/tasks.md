## 1. Контракт `status.metrics`

- [x] 1.1 Сверить параллельные изменения публичного типа `ValkeyInstanceStatus`, добавить или объединить тип снимка с `metav1.MicroTime`, обязательными идентификаторами, ролью, показателями и допускающим `null` CPU; проверить типы командой `go test ./operator/api/v1alpha1`.
- [x] 1.2 Добавить `Metrics` как map-list по `ordinal` с пределом три элемента, сгенерировать DeepCopy и CRD из Go-типа и проверить отсутствие ручных расхождений командой `scripts/check_operator_generated.sh`.

## 2. Запись и хранение метрик в API

- [x] 2.1 Загрузить положительную длительность `VALKEY_METRICS_RETENTION` со значением по умолчанию `168h` в `api/internal/config`; проверить значение по умолчанию, допустимую длительность, ноль, отрицательное и повреждённое значение модульными тестами.
- [x] 2.2 Добавить пакетную вставку не более трёх `ValkeyNodeMetric` в короткой транзакции с проверкой активного инстанса, границей по времени PostgreSQL и `ON CONFLICT DO NOTHING`; проверить полный снимок, `null` CPU, точность времени, повтор и одновременное удаление интеграционными тестами PostgreSQL.
- [x] 2.3 Добавить удаление строк строго старше заданной границы и подключить независимый проход при запуске и раз в 24 часа без перекрытия; проверить границу, повтор после ошибки, остановку по контексту и неизменную HTTP-readiness тестами хранилища и runner.

## 3. Импорт снимков из CR

- [x] 3.1 Разобрать `status.metrics` отдельно от основного наблюдения, сопоставить каждый снимок с `status.nodes` по `ordinal`, Pod UID, container ID и `runId`, сохранить роль снимка и не отбрасывать соседние допустимые элементы; проверить совпадение, неизвестный ordinal, прежний процесс, смену роли и смешанный список модульными тестами `api/internal/sync`.
- [x] 3.2 Подключить вставку допустимых снимков к каждому проходу импорта после проверки принадлежности CR, сохранив ранний выход для удаляемого инстанса; проверить импорт при неизменившемся, отсутствующем и устаревшем основном наблюдении, а также продолжение после ошибки отдельного инстанса.
- [x] 3.3 Добавить безопасные сообщения об отброшенных снимках и ошибках записи без Secret и лишних идентификаторов процесса; проверить содержимое журнала тестом отказа импорта.

## 4. Проверка рабочего пути без оператора

- [x] 4.1 Расширить HTTP-тест синхронизации: создать инстанс через TCP HTTP, дождаться CR, записать согласованные `status.nodes` и `status.metrics` административным клиентом k3s, дождаться фонового импорта и получить точку через существующий `GET /metrics`; проверить исходные значения, `null` CPU и отсутствие дубля при повторном опросе.
- [x] 4.2 Дополнить сценарий k3s новым `collectedAt` при прежнем `observedAt`, чужой идентичностью процесса, соседним допустимым снимком и просроченным снимком; проверить строки PostgreSQL и ответ HTTP без запуска оператора.
- [x] 4.3 Прогнать существующие тесты клиента и экрана мониторинга в `web`; при обнаруженной несовместимости исправить только преобразование действующего DTO или состояние данных и подтвердить результат `pnpm check`.

## 5. Документация и итоговые проверки

- [x] 5.1 Обновить `SYSTEM.md`, `SYSTEM-DETAIL.md` и `operator/TODO.md`: отметить готовый импорт и очистку, оставить сбор метрик оператором незавершённым, разделить настраиваемый срок хранения и семидневный предел чтения HTTP API и проверить согласованность с delta specs.
- [x] 5.2 Запустить `just test` в `api`, проверки общего Go-кода и сгенерированного CRD без интеграционного запуска оператора; все команды должны завершиться с нулевым кодом.
- [x] 5.3 Запустить `openspec validate import-valkey-metrics --type change --strict --no-interactive` и устранить все ошибки артефактов перед завершением реализации.
