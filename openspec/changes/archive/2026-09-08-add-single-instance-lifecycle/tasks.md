## 1. Проверки зависимостей на dev-стенде

- [x] 1.1 Проверить ContainerRestartRules на закреплённом k3s: StatefulSet принимает контейнерный restartPolicy Never, завершившийся Valkey не перезапускается при выключенном операторе; сохранить результат в отчёте изменения, при провале остановить реализацию запуска.
- [x] 1.2 Получить обезличенные примеры config_dump и certs закреплённого Envoy для TLS/SNI, TCPRoute, whitelist и idle timeout; зафиксировать поля active/warming и проверить различие конфигураций allow/deny, результаты сохранить рядом с изменением.
- [x] 1.3 Проверить доступ host-оператора к Pod IP, доступ к admin API обеих реплик и различимость двух внешних тестовых клиентов в Envoy; зафиксировать воспроизводимые команды без секретов и устранить недостатки dev-маршрутизации/RBAC в пределах принятой изоляции.

## 2. Публичный контракт

- [x] 2.1 Расширить типы spec/status полями создания single, снимком принятой конфигурации, nodes, network и deletion; сгенерировать CRD/DeepCopy и проверить envtest для неполных старых CR, идентичностей, размеров и разных metadata.generation/desiredGeneration.
- [x] 2.2 Описать и реализовать публичный формат Secret с версионными ключами хеша; проверить разбор корректных/повреждённых значений и отсутствие зависимости API от внутренних пакетов оператора.
- [x] 2.3 Добавить фиксацию первоначального снимка и диагностику UnsupportedChange/неподдерживаемого ha; проверить изменение spec во время создания, после running, возврат исходных настроек и приоритет удаления.

## 3. Учётные данные

- [x] 3.1 Реализовать атомарную инициализацию служебных паролей и users.acl с app off, ownerReference и последующей записью credentialsInitialized; envtest проверяет повтор, рестарт между Secret/status и отсутствие преждевременного initialized.
- [x] 3.2 Реализовать обновление Secret с resourceVersion и сохранением чужих полей; envtest воспроизводит конкурентную доставку хеша и подтверждает, что версия ACL остаётся принятой, а новый хеш не теряется.
- [x] 3.3 Добавить диагностику отсутствующего Secret/хеша и повреждённого комплекта; проверить RECOVERY_REQUIRED после инициализации и отсутствие регенерации паролей или секретных значений в Events/логах.

## 4. Клиент Valkey и управление

- [x] 4.1 Добавить закреплённый valkey-go и обёртку с контекстом, таймаутами, отключёнными повторами изменяющих команд и фильтрацией секретов в ошибках; проверить отмену и отсутствие повторной команды при потере ответа.
- [x] 4.2 Реализовать приём управления процессом: завершённый AUTH, CLIENT KILL прежних operator-соединений, затем чтение роли/ACL/идентичности; тест подтверждает порядок и отсутствие AUTH вместе с изменением в pipeline.
- [x] 4.3 Связать lifecycle соединений и фоновых проверок с Lease; проверить прекращение изменений и закрытие соединений при потере лидерства, включая задержанный ответ команды.

## 5. Ресурсы single

- [x] 5.1 Реализовать неизменяемую ConfigMap, расчёты памяти и конфигурацию запуска с секретами только в памяти/Secret; проверить граничные размеры, digest, readiness и отсутствие persistence/liveness.
- [x] 5.2 Реализовать StatefulSet, headless/primary Services, NetworkPolicy и ownership; envtest проверяет идемпотентность, OnDelete, requests=limits и отсутствие -ro/PDB, k3s проверяет запуск с app off.
- [x] 5.3 Добавить наблюдение Pod/Secret/дочерних ресурсов и запись идентичности процесса с Pod finalizer до допуска app; проверить смену Pod UID/container ID/run_id и отсутствие клиентского доступа до сохранения истории.
- [x] 5.4 Добавить защиту от смены VALKEY_IMAGE; проверить RECOVERY_REQUIRED без изменения шаблона и безопасное восстановление с исходным образом после возврата настройки.

## 6. Маршруты и проверка Envoy

- [x] 6.1 Добавить закреплённые типы Gateway API/Envoy, scheme и генерируемые права; проверить сборку и соответствие минимально необходимым операциям, включая port-forward и Certificate.
- [x] 6.2 Реализовать listener, TCPRoute и первоначальную SecurityPolicy; envtest проверяет whitelist off/allow/deny-all, namespace selector, отсутствие -ro и конфликт одновременного обновления общего Gateway без потери listener соседа.
- [x] 6.3 Реализовать проверку актуальных conditions, сертификата и общей ClientTrafficPolicy; тесты покрывают неверный домен, истечение TLS, устаревшее поколение и разные правила dev/prod.
- [x] 6.4 Реализовать чтение config_dump/certs двух Envoy и сравнение активных полей по примерам этапа 1; тесты покрывают warming, неизвестный формат, один ответ, смену состава и отсутствие чувствительных дампов в логах/status.
- [x] 6.5 Добавить отпечаток и периодическую сверку с переиспользованием снимка Envoy; проверить, что соседний listener не сбрасывает собственное подтверждение, а смена процесса Envoy требует новой проверки.

## 7. Готовность и наблюдение

- [x] 7.1 Реализовать допуск app после проверки сети, назначение primary и проверку EndpointSlice текущего Pod UID/IP; интеграционный тест подтверждает запрет раннего доступа и running только после полного успеха.
- [x] 7.2 Добавить applied, appliedPasswordVersion, initialized и observedGeneration; проверить подтверждение принятой версии и отсутствие подтверждения нового неподдерживаемого spec.
- [x] 7.3 Реализовать Clock, provisioning/error и running/unavailable, heartbeat и причины; unit-тесты проверяют 10 минут от создания CR через рестарт, восстановление доступности и независимость heartbeat от unknown Envoy.
- [x] 7.4 Проверить отсутствие лишних записей неизменного status между сроками heartbeat и сохранение времени verified при ошибках; envtest проверяет resourceVersion и конфликты обновлений status.

## 8. Остановка, замена и удаление

- [x] 8.1 Реализовать сохранение терминального состояния конкретного процесса до снятия Pod finalizer; проверить обычное завершение при выключенном операторе и продолжение после рестарта между сохранением доказательства и заменой Pod.
- [x] 8.2 Добавить запрет допуска замены при утрате истории и проверку удаления Node по сохранённому UID согласно правилу v1; тесты покрывают A/B/C, NotReady, Pod NotFound, ошибку Kubernetes и повторное использование имени Node.
- [x] 8.3 Реализовать стадии удаления: собственные маршруты, отключение app/соединений, StatefulSet в ноль; проверить повторные проходы и отсутствие восстановления ресурсов при deletionTimestamp.
- [x] 8.4 Реализовать ожидание доказательств всех известных процессов и завершение finalizers с сохранением Secret до остановки; проверить недоступный процесс, потерю ответа на удаление Pod и рестарт на каждой стадии.
- [x] 8.5 Проверить удаление до создания StatefulSet, в provisioning/error и при UnsupportedChange; CR и дочерние ресурсы исчезают, namespace остаётся ответственностью вызывающей стороны.

## 9. Самостоятельные интеграционные тесты

- [x] 9.1 Заменить заглушку test-full тестовым запуском на k3s с одной копией оператора, изолированными входными объектами и штатной очисткой; отсутствие стенда даёт ошибку, тесты не требуют API/PostgreSQL и не снимают finalizers принудительно.
- [x] 9.2 Проверить создание single, TLS/SNI с проверкой CA, корректный/неверный AUTH и SET/GET снаружи; проверить ACL разрешённых клиентских и запрещённых административных команд на закреплённом Valkey.
- [x] 9.3 Проверить два инстанса и клиентов с различимыми адресами: whitelist off/allow/deny-all, сетевую изоляцию, неверный SNI и отсутствие доступа до применения начального whitelist.
- [x] 9.4 Проверить создание/удаление соседнего инстанса при установленных TCP и pub/sub-соединениях через каждую реплику Envoy; соединения и данные соседа сохраняются.
- [x] 9.5 Проверить рестарты оператора во время создания и удаления, потерянный ответ ACL, обычное завершение Valkey и восстановление single с пустым кэшем; неподтверждённая остановка блокирует допуск нового процесса.
- [x] 9.6 Прогнать just test-fast и just test-full, генерацию CRD/RBAC/DeepCopy, линтер, сборку и go mod tidy -diff; сохранить отчёт с командами, версиями и явными границами покрытия, обновить operator/TODO.md для оставшихся сценариев v1.
