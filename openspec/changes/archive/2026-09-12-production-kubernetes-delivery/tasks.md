`valkey-pod-lifecycle` реализован, проверен и синхронизирован с основными спецификациями. Прежний трёхнодовый кластер заменён однонодовым кластером 16 vCPU / 64 GiB. Новый административный kubeconfig сохранён в `PRODUCTION_KUBECONFIG`; нода готова, а Cilium сообщает `1/1 reachable`. Kubernetes-инфраструктура, DNS и TLS готовы. CI/CD `34683372158` завершил все проверки, заменил оператор на SHA `776b31f1218e8855662f25882091058cd546c8f6` и выпустил рабочий API.

## 1. Разделение изменений OpenSpec

- [x] 1.1 Оставить в `cluster-capacity-placement` только завершённый расчёт вместимости и настройки API, проверить синхронизацию его основных спецификаций, выполнить строгую проверку и перенести изменение в архив без незавершённого раздела `ci-cd`.

## 2. Условия первого выпуска

- [x] 2.1 Проверить на новой рабочей ноде завершение `Pod` с `restartPolicy: Never` и скачивание общедоступного `ghcr.io/rostislavdugin/managed-valkey-operator` без `imagePullSecrets`; зафиксировать, что отказ единственной ноды не покрывается HA.
- [x] 2.2 Настроить суммарную физическую ёмкость 16 vCPU / 64 GiB в `Docker Compose`, проверить расчёт фиксированного резерва 15% CPU / 10% RAM и публичный учётный лимит 13 vCPU / 57 GiB.
- [x] 2.3 Проверить сохранённые намерения в рабочей PostgreSQL без вывода секретов; зафиксировать 0 активных инстансов, 0 незавершённых удалений и нулевой резерв на 11 сентября 2026 года.
- [x] 2.4 Сохранить новый административный kubeconfig и `CLOUDFLARE_API_TOKEN` в окружении GitHub `production`, проверить наличие обязательных секретов без вывода значений и описать их замену в эксплуатационной документации.
- [x] 2.5 После реализации и проверки `valkey-pod-lifecycle` заменить проверку `ContainerRestartRules` в `scripts/deploy_kubernetes.sh` и её тестах на `spec.restartPolicy: Never` самого `Pod`; проверить на всех рабочих нодах завершение с нулевым и ненулевым кодом без повторного запуска и очистку служебных ресурсов, а также отсутствие пользовательских `StatefulSet` прежнего оператора. До выполнения зависимости не снимать блокировку выпуска.

## 3. Сборка и подготовка Kubernetes

- [x] 3.1 Завершить сборку образа оператора для `linux/amd64`, публикацию с тегом SHA и обязательную проверку результата; подтвердить метаданные образа и скачивание из общедоступного пакета без учётных данных.
- [x] 3.2 Подготовить CRD, RBAC, сетевые ресурсы, TLS и закреплённые версии `Gateway API`, `Envoy Gateway` и `cert-manager`; проверить результат обработки `Kustomize` и отсутствие секретов в нём.
- [x] 3.3 Применить компоненты к новому однонодовому кластеру и проверить управляемую замену `Pod` при `OnDelete`; подтвердить готовность оператора и точный SHA его образа, не создавая пользовательский `ValkeyInstance`.
- [x] 3.4 Создать отдельный файл подключения Kubernetes для API, проверить ограниченные права, режим `0600`, монтирование только для чтения и удаление временных файлов при успешном и прерванном выпуске.

## 4. Включение рабочего сервиса

- [x] 4.1 Направить `cert-manager-webhook.valkey.h3llo-demo.com` на `InternalIP` новой ноды, получить внешний адрес Envoy, направить `*.valkey.h3llo-demo.com` на него в режиме `DNS only` и повторить выпуск; проверить совпадение DNS, готовность TLS-сертификата и состояние шлюза.
- [x] 4.2 Передать выпуск на сервер, применить миграции и запустить API с `KUBERNETES_BACKGROUND_SYNC_ENABLED=true`; проверить доступ API к Kubernetes и внешний `/readyz` до записи `/opt/managed-valkey/.deployed-sha`.
- [x] 4.3 Повторить выпуск после искусственного отказа между подготовкой Kubernetes и обновлением API; подтвердить идемпотентность и сохранность пользовательских ресурсов средствами сценарных тестов выпуска, не расширяя общую интеграцию или сквозные тесты.

## 5. Документация и итоговая проверка

- [x] 5.1 Обновить `SYSTEM.md` и руководство по рабочему окружению для одной ноды 16 vCPU / 64 GiB: описать мягкое размещение Envoy и HA, секреты, ручную настройку DNS, замену учётных данных и откат; сверить текст с фактическими командами.
- [x] 5.2 Сверить эксплуатационную документацию после перехода на `Pod`; запустить проверки сценариев выпуска и синтаксиса командных файлов, строгую проверку OpenSpec и просмотр итоговых изменений. Выполнить полный CI/CD от начала до конца со всеми обязательными тестами и успешным выпуском; подтвердить отсутствие секретов, тестовых пользовательских CR в сценариях выпуска и новых проверок в общей интеграции или сквозных тестах.
