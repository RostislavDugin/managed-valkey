## 1. Однонодовый стенд и процессы

- [x] 1.1 Добавить настраиваемый адрес health probe оператора с прежним `:8081` по умолчанию и проверить production-значение и тестовый loopback-адрес в unit-тестах конфигурации.
- [x] 1.2 Задать профилю `integration` одну k3s server-ноду, одну реплику Envoy и резерв памяти для четырёх минимальных Valkey; обновить `scripts/test_environment_test.sh` и убедиться, что профили API, оператора и e2e не изменились.
- [x] 1.3 Добавить `scripts/test_integrations.sh`, который применяет миграции, запускает по одному API и оператору с отдельными kubeconfig и случайными портами, ждёт их готовности и адресно завершает группы процессов; проверить штатное завершение и прерывание в shell-тестах.
- [x] 1.4 Добавить измерение полной длительности от начала подготовки до конца штатной очистки и отчёт о сравнении с ориентиром в пять минут; shell-тестом проверить, что успешный долгий запуск остаётся успешным, а длительность и превышение видны в отчёте.

## 2. Общая тестовая обвязка

- [x] 2.1 Создать пакет `tests/integrations` с загрузкой адреса API, административного kubeconfig, CA и опубликованного адреса Valkey из окружения; проверить его компиляцию командой `go test -tags integration ./tests/integrations -run '^$'`.
- [x] 2.2 Реализовать HTTP-клиент регистрации, чтения аккаунта и полного набора операций Valkey без импорта внутренних пакетов API; unit-тестами через `httptest.Server` проверить заголовки, JSON, ошибки и отсутствие паролей в диагностических сообщениях.
- [x] 2.3 Реализовать только читающий Kubernetes-клиент и TLS-клиент Valkey с подключением к NodePort и SNI из ответа API; проверить чтение CR и Pod и команды `AUTH`, `PING`, `SET` и `GET` на интеграционном стенде.
- [x] 2.4 Добавить отдельные конечные сроки ожидания HTTP и Kubernetes условий, немедленную проверку завершения процессов и сохранение последнего наблюдения; unit-тестами проверить успех, отмену и истечение срока операции без фиксированных пауз и общего пятиминутного timeout.
- [x] 2.5 Добавить каталог четырёх обязательных `Test_`-функций, запуск с `-parallel=4` и отменяемый барьер перед четырьмя запросами создания; проверить, что удалённое или переименованное имя останавливает команду до подготовки k3s и что все четыре участника проходят барьер одновременно.

## 3. Параллельные сценарии

- [x] 3.1 Реализовать `Test_CreateSingleValkey_WithRealApiAndOperator_BecomesReachableAndRunning`: отдельный аккаунт и `single` 1 CPU / 1 GB, согласованное состояние API, Namespace, CR, готовый Pod и TLS-команды Valkey; проверить точечный сценарий на общем стенде.
- [x] 3.2 Реализовать `Test_ResizeSingleValkey_WithRealApiAndOperator_AppliesRequestedResources`: отдельный аккаунт, изменение 1 CPU / 1 GB на 1 CPU / 2 GB, новый UID Pod, новые resource requests, применённые поля API и рабочее TLS-подключение; проверить точечный сценарий на общем стенде.
- [x] 3.3 Реализовать `Test_RotateSingleValkeyPassword_WithRealApiAndOperator_ReplacesApplicationCredential`: отдельный аккаунт, запись значения, ротация, совпавшие версии API, отказ старого пароля и чтение значения новым; проверить точечный сценарий на общем стенде.
- [x] 3.4 Реализовать `Test_DeleteSingleValkey_WithRealApiAndOperator_RemovesKubernetesResourcesAndApiState`: отдельный аккаунт, удаление готового инстанса, исчезновение Namespace, `404` карточки, отсутствие в списке и нулевой расход квоты; проверить точечный сценарий на общем стенде.
- [x] 3.5 Добавить HTTP-cleanup для первых трёх сценариев и дождаться подтверждённого удаления до их завершения; намеренно оборвать один сценарий и проверить, что очистка не удаляет ресурсы остальных аккаунтов.

## 4. Команды, документация и диагностика

- [x] 4.1 Добавить `tests/integrations/Justfile` и корневой `just test-integrations`, затем подключить integration как четвёртый параллельный набор `test-full` вместо заглушки; обновить `scripts/run_all_tests_test.sh` и проверить маршрутизацию всех трёх корневых тестовых команд.
- [x] 4.2 Дописать в корневой `AGENTS.md` назначение, границы, запреты, четыре параллельных аккаунта и Valkey-инстанса, общий однонодовый стенд и мягкий ориентир длительности около пяти минут; проверить, что текст не делает превышение ориентира ошибкой и не относит HA, аварии и Playwright к этому набору.
- [x] 4.3 Расширить проверку диагностических артефактов на логи API и оператора, Kubernetes-состояния и Events; тестовыми файлами проверить блокировку публикации при наличии поля `password`, точного тестового пароля, JWT аккаунта, kubeconfig или содержимого Secret.
- [x] 4.4 Сохранять безопасные журналы процессов, последнее HTTP-состояние и длительности сценариев при ошибке; shell-тестом проверить наличие файлов и отсутствие файлов Secret и тел запросов.

## 5. CI и итоговая проверка

- [x] 5.1 Добавить обязательный job `test-integrations` после lint jobs, запускать в нём `just test-integrations`, проверять артефакты перед выгрузкой и добавить job в зависимости `build-api` и `build-web`; задать технический timeout job значительно больше пяти минут, обновить проверки workflow и убедиться, что функциональная ошибка integration блокирует build и deploy.
- [x] 5.2 Запустить `just test-integrations` на чистом однонодовом стенде и подтвердить в отчёте одновременный старт четырёх сценариев, четыре разных аккаунта, Valkey-инстанса и Namespace, прохождение всех операций, штатную очистку и фактическую полную длительность.
- [x] 5.3 Повторить `just test-integrations` не менее трёх раз, сравнить длительности этапов с ориентиром в пять минут и проверить стабильное прохождение без автоматического снижения `-parallel=4`; превышение ориентира само по себе не считать ошибкой.
- [x] 5.4 До, во время и после полного интеграционного запуска проверить RAM, CPU, процессы и контейнеры; убедиться, что после очистки не осталось ресурсов этого запуска и не изменились dev и соседние тестовые окружения.
- [x] 5.5 Запустить shell-тесты изменённых сценариев, `go test` для затронутых пакетов конфигурации и обвязки, затем корневые `just lint` и быстрый `just test`; отдельно отметить, что сорокаминутная матрица оператора не запускалась в CI-проверке этого изменения.
