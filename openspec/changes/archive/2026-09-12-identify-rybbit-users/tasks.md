## 1. Обёртка Rybbit

- [x] 1.1 Добавить в `shared/lib` типизированную синхронизацию `{ userId, email } | null` с вызовами `identify` и `clearUserId`; проверить модульными тестами точные аргументы обоих вызовов.
- [x] 1.2 Добавить проверку рабочего тега Rybbit, ожидание раз в 100 мс до 10 секунд и отмену устаревшей ревизии; проверить тестами отсутствие таймера в локальной сборке, позднюю готовность, последнее состояние и завершение ожидания по сроку.
- [x] 1.3 Экспортировать обёртку через публичный API `shared/lib` и проверить границы импортов командой `pnpm lint:fsd`.

## 2. Связь с пользовательской сессией

- [x] 2.1 Синхронизировать UUID и почту из `ConsoleShell` при изменении действующей сессии без очистки в завершении эффекта; проверить тестом маршрута вызов `identify(userId, { email })`.
- [x] 2.2 Очищать пользователя Rybbit при открытии `AuthPage`; проверить тестами прямое открытие страницы входа, ручной выход, удаление токена в другой вкладке, ответ `401` и истечение JWT.
- [x] 2.3 Проверить, что отсутствие или отказ Rybbit не меняет переходы и состояние авторизации, запустив затронутые тесты обёртки и маршрутов Vitest.

## 3. Итоговая проверка

- [x] 3.1 Сохранить существующие условия подключения Rybbit только в рабочей сборке и проверить тестом сборки наличие `data-site-id` при `RYBBIT_SITE_ID` и отсутствие скрипта без него.
- [x] 3.2 Запустить `pnpm check` в `web` и `openspec validate identify-rybbit-users --strict --no-interactive`; обе команды должны завершиться без ошибок.
- [ ] 3.3 После выпуска войти под двумя тестовыми аккаунтами и проверить в Rybbit, что у каждого профиля показаны его UUID, почта, переходы и запись сессии, а действия после выхода не относятся к прежнему аккаунту.
