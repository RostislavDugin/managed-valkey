## 1. Рабочие образы и маршрутизация

- [x] 1.1 Добавить в `caddy/Dockerfile` воспроизводимую сборку `web` через `pnpm` и копирование `web/dist` в конечный образ; проверить сборку образа для `linux/amd64` и наличие `index.html` внутри него.
- [x] 1.2 Изменить `caddy/Caddyfile`: передавать `/v1/*`, `/livez` и `/readyz` в API, обслуживать SPA с запасным `index.html`, открыть разрешённые маршруты VictoriaLogs без проверки имени и пароля и сохранить 404 для остальных путей; проверить конфигурацию командой `caddy validate` в собранном образе.
- [x] 1.3 Перевести `docker-compose.prod.yml` на готовые образы API, миграций и Caddy, добавить безопасные рабочие значения по умолчанию и оставить обязательными только `POSTGRES_PASSWORD` и `JWT_SECRET`; проверить итог через `docker compose config` с минимальным временным файлом переменных.
- [x] 1.4 Удалить из `deploy/prod` учётные данные и объект Kubernetes `Secret` для VictoriaLogs, сохранив экспорт оператора по HTTPS без авторизации; проверить результат через `kustomize build --load-restrictor LoadRestrictionsNone deploy/prod`.
- [x] 1.5 Привести `.env.example` к разделению локальных значений и минимального рабочего `.env`; проверить, что файл не содержит настоящих секретов и согласован с обязательными переменными Docker Compose.

## 2. Развёртывание версии

- [x] 2.1 Добавить сценарий установки версии в `/opt/managed-valkey/releases/<sha>` с проверкой входных файлов, загрузкой Docker-образов, двумя файлами переменных и постоянным именем проекта Docker Compose; проверить сценарий на временном каталоге и через разрешённую конфигурацию Docker Compose.
- [x] 2.2 Добавить запуск Docker Compose с ожиданием миграций и готовности сервисов, внешний запрос к `/readyz` и смену ссылки `current` только после успеха; проверить, что искусственная ошибка до проверки готовности оставляет предыдущую ссылку `current` без изменений.
- [x] 2.3 Добавить повторный запуск выбранной версии для отката без `goose down`; проверить, что сценарий использует прежний SHA и общие тома рабочего проекта.

## 3. GitHub Actions

- [x] 3.1 Создать `.github/workflows/ci-cd.yml` с закреплёнными ревизиями внешних действий и параллельными заданиями `lint-api`, `lint-operator`, `lint-web`; проверить синтаксис файла и соответствие команд локальным линтерам, форматтерам и генерации оператора.
- [x] 3.2 Добавить следующий барьером этап из параллельных `test-api`, `test-operator`, `test-web`, включая отдельную PostgreSQL для API; проверить команды локально и убедиться, что `test-web` не запускает рабочую сборку.
- [x] 3.3 Добавить параллельные сборки API с миграциями и Caddy с клиентским приложением для `linux/amd64`, теги с SHA и закрытые результаты с архивами Docker; проверить платформу загруженных образов через `docker image inspect`.
- [x] 3.4 Добавить задание `deploy` только для успешного `push` в `main` через окружение GitHub `production`, группу последовательного выполнения и передачу версии по SSH; проверить условия процесса для запроса на слияние и `push` в `main` без подключения к настоящему серверу.
- [x] 3.5 Добавить проверку `PRODUCTION_HOST`, `PRODUCTION_SSH_USER`, `PRODUCTION_SSH_PORT`, `PRODUCTION_SSH_KNOWN_HOSTS`, `PRODUCTION_SSH_PRIVATE_KEY`, `POSTGRES_PASSWORD` и `JWT_SECRET`, безопасное создание `.env` с режимом `0600` и проверку пароля PostgreSQL на допустимые в URL символы; проверить, что значения секретов и содержимое `.env` не появляются в выводе процесса.

## 4. Документация

- [x] 4.1 Обновить `SYSTEM.md`: Caddy хранит и отдаёт SPA, API не содержит `web/dist`, рабочий VictoriaLogs временно доступен без пароля; проверить отсутствие старых утверждений о Go `embed` и обязательной проверке имени и пароля.
- [x] 4.2 Обновить `SYSTEM-DETAIL.md`: описать граф CI/CD, каталоги версий, доставку по SSH, окружение GitHub, минимальный `.env`, маршруты Caddy и временно публичный `vmui` с OTLP; сверить описание с `.github/workflows/ci-cd.yml` и `docker-compose.prod.yml`.
- [x] 4.3 Обновить описание порядка рабочей выкладки с учётом того, что этот процесс разворачивает VM, а оператор остаётся отдельной частью управляемого Kubernetes; проверить согласованность разделов об окружениях, структуре репозитория и порядке выкладки.

## 5. Итоговая проверка

- [x] 5.1 Запустить Go-форматирование с выводом различий, линтеры API и оператора, проверку генерации, `go mod tidy -diff`, `api just test` и `operator just test-fast`; все команды должны завершиться с нулевым кодом.
- [x] 5.2 Запустить `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` и `pnpm build` в `web`; все команды должны завершиться с нулевым кодом.
- [x] 5.3 Собрать рабочие образы для `linux/amd64`, проверить `docker-compose.prod.yml`, маршруты SPA, API и VictoriaLogs в локальном стенде и убедиться, что рабочему серверу не нужна компиляция исходников.
- [x] 5.4 Проверить итоговые изменения на секреты и незакреплённые внешние версии, затем выполнить `openspec validate add-ci-cd --strict`.
