## 1. Общий e2e-стенд

- [x] 1.1 Настроить для профиля `e2e` стабильный публичный NodePort через `k3s-server`, не меняя сетевое поведение профилей integration и operator; проверить профиль мок-тестом `scripts/test_environment_test.sh` и подключением через server при готовом Envoy на другой ноде.
- [x] 1.2 Добавить предварительную загрузку закреплённого образа Valkey на три ноды e2e и проверить по журналу подготовки, что первый `single` и `ha` не выполняют сетевой pull.
- [x] 1.3 Расширить `scripts/test_e2e.sh` запуском одного оператора с e2e-kubeconfig, отдельным readiness endpoint, регистрацией API, оператора и web в состоянии запуска и ограниченным завершением групп процессов; проверить успешный запуск и принудительное завершение через отдельный shell-тест runner.
- [x] 1.4 Разделить журналы host-оператора и Kubernetes-диагностики для e2e, затем проверить `scripts/test_environment_test.sh`, что сбор диагностики не перезаписывает `operator.log`.
- [x] 1.5 Добавить измерение длительности миграций, подготовки процессов, Playwright и полного e2e-запуска; проверить формат `durations.tsv` автоматическим тестом runner.

## 2. Фикстуры и защитные проверки Playwright

- [x] 2.1 Добавить в `tests/e2e` закреплённый TypeScript-клиент Valkey и обновить lockfile; проверить `pnpm install --frozen-lockfile` и `pnpm typecheck`.
- [x] 2.2 Реализовать общую фикстуру пользовательских действий с интервалом 0 мс для headless и 3000 мс для headed и production; модульным тестом с подменяемыми часами проверить минимальный интервал и отсутствие задержки перед проверками.
- [x] 2.3 Показать в карточке Valkey применённую конфигурацию и отдельный RO-адрес для HA; выделить помощники регистрации, входа, чтения каталога размеров, создания, ожидания состояния, изменения тарифа, смены пароля и удаления через доступные роли и подписи; проверить поля карточки и локаторы на текущем сценарии регистрации.
- [x] 2.4 Реализовать TLS-помощник Valkey, который использует host, port и пароль из интерфейса, SNI, пользователя `app` и локальный CA при его наличии; проверить `SET`, `GET`, `INFO memory`, отказ старого пароля и ошибку `READONLY` в целевых e2e-сценариях.
- [x] 2.5 Реализовать фоновые пробы записи и RO-чтения с периодом 100 мс, учётом длительности отказа и гарантированным завершением; модульным тестом проверить отмену, фиксацию хотя бы одной ошибки записи и расчёт максимального промежутка между успешными чтениями.
- [x] 2.6 Реализовать Kubernetes-помощник чтения primary, удаления Pod, адресной остановки и восстановления agent с проверкой Compose project, container ID и Node UID; проверить ошибку при попытке затронуть server, неизвестный agent или чужой контейнер.
- [x] 2.7 Реализовать реестр аккаунтов и UI-teardown, который в новом browser context удаляет все видимые инстансы каждого аккаунта и ждёт пустого списка и нулевой квоты; проверить очистку после намеренной ошибки тестовым сценарием.
- [x] 2.8 Создать закрытый файл контрольных секретов, записывать в него пароль аккаунта, JWT-секрет процесса и одноразовые пароли Valkey, расширить очистку и сканирование отчётов; проверить обычные файлы, ZIP, встроенный ZIP и отказ публикации при оставшемся секрете.
- [x] 2.9 Отключить автоматические снимки и добавить снимок при ошибке с маскированием полей одноразового пароля; проверить на намеренном падении при открытом окне пароля, что изображение и HTML не раскрывают значение.
- [x] 2.10 Добавить проверку обязательного каталога Playwright по русским названиям и тегам до bootstrap; проверить, что удалённый сценарий, неверный тег и пустой список завершают команду ошибкой.

## 3. Пользовательские сценарии

- [x] 3.1 Перевести существующую регистрацию на общие фикстуры с метаданными `@prod`; запустить этот сценарий в headless и убедиться, что новый аккаунт попадает в `/valkey/management`.
- [x] 3.2 Реализовать `@prod` lifecycle `single`: создание, блокировку действий до `running`, основной адрес, `SET`/`GET`/`INFO`, увеличение и уменьшение размера, ротацию пароля и удаление; выполнить сценарий на локальном e2e-стенде и проверить освобождение квоты и недоступность прежнего адреса.
- [x] 3.3 Реализовать `@prod` lifecycle `ha`: основной и RO-адреса, `READONLY`, ресурсы трёх процессов, изменение размера, ротацию пароля и удаление; выполнить сценарий на локальном e2e-стенде и проверить оба TLS-маршрута после каждой операции.
- [x] 3.4 Реализовать `@local` проверку личной квоты с динамическим выбором тарифов, двумя `single` и повтором допустимого resize после удаления вспомогательного инстанса; выполнить сценарий и проверить отсутствие HTTP-запроса для запрещённых действий.
- [x] 3.5 Реализовать `@local` исчерпание общей ёмкости через разные аккаунты, проверку HTTP `422`, `NOT_ENOUGH_RESOURCES`, `reason=cluster_quota` и доступности ранее созданных инстансов; выполнить сценарий и проверить UI-очистку всех аккаунтов.
- [x] 3.6 Реализовать `@local-k8s` потерю primary Pod с двумя пробами, временным отказом записи, пределом RO-перерыва пять секунд и восстановлением записи за две минуты; выполнить сценарий и проверить удаление инстанса через личный кабинет.
- [x] 3.7 Реализовать `@local-k8s` потерю worker-ноды с primary, соблюдая порядок остановки контейнера и удаления Node, восстановление прежнего публичного адреса, `degraded` на двух нодах и возврат к `running` на трёх; выполнить сценарий и проверить восстановление agent до пользовательской очистки.

## 4. Команды, CI/CD и документация

- [x] 4.1 Передавать режим задержки и путь контрольных секретов через `scripts/run_playwright_e2e.sh` для `test`, `headed` и `prod`; проверить маршрутизацию режимов shell-тестом без запуска браузера.
- [x] 4.2 Обновить e2e job: полное название, проверка каталога, безопасное сканирование динамических секретов и публикация только разрешённых файлов; проверить структуру `.github/workflows/ci-cd.yml` скриптом `scripts/ci_cd_workflow_test.sh`.
- [x] 4.3 Удалить `needs` между десятью независимыми lint, test, integration, e2e и build jobs, добавить итоговый job с `if: always()` и проверкой результата каждого, перевести deploy на этот барьер; автоматическим тестом workflow проверить fan-out, полный fan-in и запрет deploy при failure, cancelled или skipped.
- [x] 4.4 Заменить выполненный `tests/e2e/TODO.md` актуальным описанием команд, тегов, каталога, очистки и диагностики, удалить устаревший `e2e/TODO.md` и обновить `SYSTEM.md`, `SYSTEM-DETAIL.md` и применимые инструкции; поиском проверить отсутствие утверждений, что e2e ограничивается регистрацией, оператор не запускается или CI ждёт lint перед тестами и сборкой.

## 5. Итоговая проверка

- [x] 5.1 Запустить typecheck e2e, shell syntax и затронутые тесты сценариев окружения, runner, каталога, артефактов и workflow; убедиться, что быстрые проверки проходят без создания k3s и не меняют индекс Git.
- [x] 5.2 Перед полным headless e2e проверить RAM, CPU, процессы и контейнеры, во время запуска контролировать потребление ресурсов, после завершения убедиться в отсутствии процессов, контейнеров, сетей, томов, маршрутов и базы этого запуска; выполнить `just test-e2e` без пропущенных сценариев и сохранить измеренные длительности.
- [x] 5.3 По измерению установить обоснованные Playwright и CI timeouts с запасом, повторить упавшие или затронутые сценарии и подтвердить, что превышение ориентира длительности само по себе не меняет результат.
- [x] 5.4 Запустить headed-проверку с доступным дисплеем и подтвердить наблюдением и автоматическим измерением интервал 3000 мс между пользовательскими действиями при отсутствии пауз у backend-проверок; production-команду не выполнять автоматически.
- [x] 5.5 Выполнить `openspec validate add-console-e2e-and-parallel-ci --strict` и `openspec validate --specs`, проверить итоговый diff и согласованность обязательного каталога, документации и CI-графа.
