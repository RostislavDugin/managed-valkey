## 1. Канонические проектные навыки

- [x] 1.1 Перенести `humanizer` и текущие навыки OpenSpec в `.skills`, объединить различающиеся ссылки на команды Codex и Claude и проверить, что остальное содержимое исходных копий сохранено.
- [x] 1.2 Заменить общие каталоги в `.agents/skills` и `.claude/skills` относительными символическими ссылками на `.skills` и проверить все ссылки через `find -L` и чтение каждого `SKILL.md`.
- [x] 1.3 Скачать полный `feature-sliced-design` с ревизии `e7eac044ee7405b0e82abe8e54cab7a1e9e8bc0c` и три навыка Mantine с ревизии `222191c6b15d1ae2c02051a285f47230777be513`, записать источники в `.skills/SOURCES.md` и проверить наличие всех упомянутых reference-файлов.
- [x] 1.4 Дополнить корневой `AGENTS.md` правилом канонического хранения и проверить, что обязательная ссылка на `humanizer` ведёт в `.skills`.

## 2. Основа web

- [x] 2.1 Создать каркас командой `pnpm create vite web --template react-ts --no-interactive`, удалить настройку и зависимости ESLint, демонстрационные изображения и стили; проверить, что остались рабочие `index.html`, точка входа и настройки Vite и TypeScript.
- [x] 2.2 Добавить в `web/package.json` согласованные runtime- и dev-зависимости, выполнить `pnpm install` и проверить повторную установку командой `pnpm install --frozen-lockfile`.
- [x] 2.3 Настроить TypeScript, Vite, React, React Router, псевдоним `@/*` и прокси `/v1`; запустить `pnpm dev` и проверить корневой маршрут и передачу запроса через прокси.
- [x] 2.4 Настроить Oxfmt, Oxlint с `oxc-config-mantine` и Steiger; проверить, что `pnpm format:check`, `pnpm lint` и `pnpm typecheck` завершаются успешно.
- [x] 2.5 Создать минимальную pages-first структуру FSD с тонкой точкой входа и используемым срезом главной страницы; проверить структуру Steiger и отсутствие пустых слоёв.

## 3. Дизайн-система

- [x] 3.1 Перенести палитры, семантические токены, размеры, типографику и точки изменения раскладки в `web/src/app/styles`; проверить доступность одинаковых значений из Mantine и Tailwind.
- [x] 3.2 Адаптировать тему и варианты компонентов к Mantine 9.6, подключить шрифты и провайдер темы; проверить светлую и тёмную схемы на начальной странице.
- [x] 3.3 Подключить Tailwind 4 через Vite без Preflight и проверить, что штатные кнопка и поле Mantine сохраняют фон, границы и состояния взаимодействия.
- [x] 3.4 Применить исправления контраста для акцентного текста и интерактивных границ; пересчитать контрольные пары и проверить пороги 4.5:1 и 3:1.
- [x] 3.5 Собрать `web/DESIGN.md` из применимых материалов `design` и проверить в нём правила токенов, раскладки, компонентов, текста и доступности.
- [x] 3.6 Сверить каждый файл исходного `design` с таблицей миграции, удалить ненужные и перенесённые файлы и проверить отсутствие корневого каталога `design` и битых ссылок в `web/DESIGN.md`.

## 4. Инструкции и итоговая проверка

- [x] 4.1 Создать `web/AGENTS.md` со ссылками на корневые инструкции, `DESIGN.md`, FSD и навыки Mantine; создать `web/CLAUDE.md` со ссылкой на локальный `AGENTS.md` и проверить разрешение всех путей.
- [x] 4.2 Выполнить `pnpm check` и проверить, что форматирование, Oxlint, Steiger, TypeScript и производственная сборка завершаются с нулевым кодом.
- [x] 4.3 Проверить, что в `.agents/skills` и `.claude/skills` нет независимых каталогов общих навыков, а все символические ссылки указывают на существующие каталоги в `.skills`.
- [x] 4.4 Проверить итоговый `git diff`, подтвердить отсутствие изменений в пользовательском `SYSTEM.md` и выполнить `openspec validate setup-web-frontend --strict`.
