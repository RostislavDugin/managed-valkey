## 1. Снимок email в PostgreSQL

- [x] 1.1 Создать через `api/Justfile` SQL-миграцию, которая добавляет обязательный `valkey_instances.user_email`, заполняет существующие строки и поддерживает INSERT предыдущей версии API через триггер; проверить применение к новой базе, базе с существующими активными и удалёнными строками и откат миграции.
- [x] 1.2 Добавить снимок email в модель `store.ValkeyInstance` и сохранять `Actor.Email` при создании; проверить интеграционным HTTP-тестом, что значение записывается в общей транзакции, не попадает в DTO и не меняется последующими мутациями.
- [x] 1.3 Добавить проверку совместимости старого INSERT без `user_email`; убедиться тестом PostgreSQL, что триггер подставляет email по `user_id`, а неизвестный пользователь не создаёт строку.

## 2. Доставка метаданных из API

- [x] 2.1 Добавить общие константы для `valkey.h3llo-demo.com/user-email` и `valkey.h3llo-demo.com/role`; проверить компиляцией API и оператора, что строковые ключи не дублируются в пакетах.
- [x] 2.2 Разделить проверку идентичности и построение описательных метаданных, чтобы API после проверки меток и UID добавлял email-аннотацию на `Namespace` и `ValkeyInstance`, сохраняя посторонние метаданные, `status` и `finalizers`; проверить модульными тестами создание, исправление и совпадающее состояние без записи.
- [x] 2.3 Покрыть интеграционными тестами API первоначальную доставку, дополнение существующих объектов без аннотации, исправление неверного снимка и отказ изменять объект с чужим ID или UID; выполнить затронутые тесты через настоящий TCP HTTP и Kubernetes-клиент стенда.

## 3. Метаданные ресурсов оператора

- [x] 3.1 Разделить селекторные и описательные метки: оставить прежний набор во всех селекторах, добавить `instance-id`, `user-id` и email-аннотацию в `metadata` объектов `StatefulSet`, `ConfigMap`, Service, `NetworkPolicy`, `PodDisruptionBudget`, `TCPRoute` и `SecurityPolicy`; проверить модульными тестами каждый тип ресурса и неизменность селекторов.
- [x] 3.2 Расширить шаблон Pod описательными метаданными без изменения `StatefulSet.spec.selector`; проверить сценарием `envtest` обновление существующего StatefulSet без ошибки неизменяемого поля, замены Pod и потери адресов Service.
- [x] 3.3 Обобщить сверку роли Pod так, чтобы одним изменением сохранялись посторонние метаданные, добавлялись ID и email, а `role` и `valkey.h3llo-demo.com/role` назначались и удалялись вместе; проверить модульными тестами primary, допущенную реплику, неподтверждённый процесс и отсутствие email от предыдущей версии API.
- [x] 3.4 Проверить сценарием `envtest`, что появление email-аннотации на `ValkeyInstance` запускает сверку и дополняет уже работающие Pod без рестарта, а Kubernetes `Node` остаётся неизменным.
- [x] 3.5 Дополнить интеграционный HA-сценарий проверкой смены обеих меток роли после failover и выдачи `replica` прежнему primary только после подтверждения синхронизации и допуска к чтению.

## 4. Общая проверка

- [x] 4.1 Запустить `format`, `lint` и быстрый `test` отдельно в `api` и `operator`, проверить состав Git index после форматирования и устранить все ошибки.
- [x] 4.2 Запустить корневой `just test` для совместной быстрой проверки API и оператора; до и после запуска проверить ресурсы системы и отсутствие оставшихся процессов или контейнеров конкретного тестового запуска.
- [x] 4.3 На тестовом k3s проверить вывод `kubectl get pods -A`: для каждого процесса видны `instance-id`, `user-id`, email-аннотация и согласованная роль, а фильтры по обеим меткам роли выбирают ожидаемые Pod.
