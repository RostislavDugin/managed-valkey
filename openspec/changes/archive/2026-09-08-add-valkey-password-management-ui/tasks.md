## 1. Модель и временный клиент

- [x] 1.1 Добавить модель учётных данных и генерацию 32-символьного пароля из 24 случайных байт Web Crypto; проверить формат, длину и отсутствие смещения при base64url-кодировании модульными тестами.
- [x] 1.2 Перевести `mv_valkey_instances` с версии 4 на 5 с маской, текущей и применённой версиями пароля и временем демонстрационного применения; проверить миграции версий 1–4 и отсутствие полного пароля в сериализованном состоянии.
- [x] 1.3 Передавать пароль в `createInstance`, сохранять только маску и возвращать базу без секрета; проверить успешное создание, неверный формат и постоянное хранилище тестами временного клиента.
- [x] 1.4 Добавить получение учётных данных и смену пароля с проверкой владельца, формата, ожидаемой версии и незавершённой операции; проверить переход от равных версий к применению и обратно, конфликты и отсутствие записи при ошибке.

## 2. Первичная выдача пароля

- [x] 2.1 Добавить в контекст `ValkeyLayout` временный результат создания с установкой, получением для совпадающей базы и очисткой; проверить, что значение не попадает в состояние маршрута или браузерные хранилища.
- [x] 2.2 Генерировать пароль при отправке формы создания, передавать его временному клиенту и после успеха открывать вкладку «Управление» с одноразовым результатом; проверить новый пароль после отклонённой и повторно отправленной формы.
- [x] 2.3 Собрать окно одноразового показа с полным паролем, предупреждением и явной кнопкой копирования; проверить копирование, закрытие и невозможность повторного открытия.

## 3. Смена пароля во вкладке «Управление»

- [x] 3.1 Добавить блок учётных данных с пользователем `app`, маской, статусом незавершённого применения, загрузкой и локальной ошибкой с повтором; проверить отсутствие статуса после применения и состояние `Применяется`, не скрывая адрес, тариф и примеры подключения.
- [x] 3.2 Добавить окно смены с предупреждением о закрытии подключений и отдельным текстом для `degraded`; проверить, что до подтверждения пароль не генерируется и запрос не отправляется.
- [x] 3.3 После принятия смены показать новый пароль в том же окне, запустить обновление метаданных до совпадения версий и заблокировать повторную смену на время применения; проверить успех, конфликт версии и ошибку запроса компонентными тестами.
- [x] 3.4 Очищать полный пароль при закрытии, размонтировании, смене маршрута и `pagehide`, а поздний ответ не должен открывать окно снова; проверить обычный переход, перезагрузку и возврат из bfcache тестами событий браузера.

## 4. Аудит и итоговая проверка

- [x] 4.1 Добавить `instance.password.rotate` в тип, русские подписи и проверку хранилища; проверить одно событие после успешной смены, отсутствие события после ошибки и отсутствие секретных значений в журнале.
- [x] 4.2 Проверить клавиатурный фокус, подписи кнопок копирования, области нажатия, светлую и тёмную схемы и ширины 375, 768, 1024 и 1440 пикселей; выровнять обычное начертание значений и зелёных действий, оставить значения серыми, а адрес моноширинным.
- [x] 4.3 Запустить тесты раздела Valkey, затем `pnpm check` в `web` и `openspec validate add-valkey-password-management-ui --strict`; все команды должны завершиться с нулевым кодом.
