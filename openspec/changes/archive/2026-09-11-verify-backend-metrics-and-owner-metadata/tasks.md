## 1. Тестовый клиент HTTP

- [x] 1.1 Добавить в `tests/integrations/http_client_test.go` структуры ответа метрик и чтение `GET /v1/managed/valkey/instances/{id}/metrics?range=5m`; проверить путь, авторизацию и разбор ответа в `Test_APIClient_WithLifecycleRequests_SendsHTTPHeadersAndDecodesResponses`.

## 2. Проверки общего сценария

- [x] 2.1 Дополнить `kubernetesReady` проверкой меток с идентификаторами на готовых `Namespace`, `ValkeyInstance` и `Pod`, а также роли `Pod` и аннотации с адресом электронной почты; добавить локальный тест, который проходит только при полном наборе метаданных.
- [x] 2.2 Добавить ожидание непустой точки метрик с загрузкой CPU и обязательными показателями Valkey, сохранять последний ответ при истечении срока и проверить помощник на ответах с пустыми и заполненными интервалами.
- [x] 2.3 Вызвать ожидание метрик из `Test_CreateSingleValkey_WithRealApiAndOperator_BecomesReachableAndRunning` без изменения каталога обязательных сценариев; проверить каталог командой `scripts/check_integration_test_catalog.sh`.

## 3. Проверка реализации

- [x] 3.1 Отформатировать изменённые файлы Go и выполнить линтер для затронутых пакетов; убедиться, что форматирование не добавило в индекс посторонние изменения.
- [x] 3.2 Проверить доступные ресурсы, запустить `just test-integrations`, затем убедиться, что процессы и контейнеры запуска завершены и ресурсы освобождены.
- [x] 3.3 Выполнить `openspec validate verify-backend-metrics-and-owner-metadata --strict` и устранить ошибки согласованности артефактов.
