## 1. Чтение показателей

- [x] 1.1 Добавить совместимую версию `k8s.io/metrics` и узкий клиент `PodMetrics`; проверить сборку `go test ./operator/internal/operator ./operator/internal/valkey`.
- [x] 1.2 Добавить чтение полного `INFO` и повторную проверку `run_id` в клиент Valkey; модульным тестом проверить все поля успешного ответа, отсутствие обязательного поля, отрицательное значение и смену процесса.
- [x] 1.3 Преобразовать свежую загрузку CPU контейнера `valkey` в целые милликоры, а ошибку, устаревшую отметку и значение прежнего контейнера в `nil`; проверить варианты одним табличным тестом.

## 2. Контроллер метрик

- [x] 2.1 Реализовать отдельный цикл согласования метрик под общим `LeaseScope`: запускать его раз в `config.MetricsInterval`, собирать до трёх узлов параллельно и не возвращать ошибки данных в очередь основного контроллера; проверить период, обычное обновление `status.nodes` до истечения периода и утрату права активного оператора модульным тестом.
- [x] 2.2 Перед сбором и после него сверять `podUID`, `containerID` и `runId`, а перед записью повторно сверять свежий `status.nodes`; проверить отказ от снимка при каждой смене идентичности.
- [x] 2.3 Обновлять `status.metrics` одной записью с проверкой `resourceVersion`, сохранять последний успешный снимок текущего процесса, удалять снимок прежнего процесса и не менять соседние `ordinal` или другие поля `status`; проверить успешный снимок, ошибку `INFO`, `cpuMillicores=null` и конфликт записи компактным набором тестов.
- [x] 2.4 Зарегистрировать контроллер в `ctrl.Manager` с фильтром, который не запускает немедленный повтор после собственной записи; проверить настройки контроллера и отсутствие изменений сгенерированных CRD и RBAC командой `scripts/check_operator_generated.sh`.
- [x] 2.5 Сделать повтор записи стадии замены устойчивым к одновременной записи метрик: копировать идентификатор заменяемого процесса до изменения `status` и проверить конфликт одним модульным тестом.

## 3. Рабочий сценарий и документация

- [x] 3.1 Расширить существующий интеграционный сценарий `single`: дождаться снимка с загрузкой CPU после готовности, затем после штатной замены Pod проверить `podUID`, `containerID` и `runId` нового процесса; выполнить сценарий в составе `operator just test-full`, не добавляя отдельный стенд k3s.
- [x] 3.2 Обновить `SYSTEM.md`, `SYSTEM-DETAIL.md` и `operator/TODO.md`: описать работающий источник `status.metrics`, отдельный контроллер, правила ошибок и свежести CPU; сверить текст с уточнениями требований OpenSpec.

## 4. Итоговые проверки

- [x] 4.1 Проверить доступную память, CPU, активные тестовые процессы и контейнеры, затем выполнить `operator just format`, `operator just lint` и `operator just test`; после запуска проверить индекс Git и отсутствие оставшихся ресурсов тестов.
- [x] 4.2 С учётом доступных ресурсов запустить `operator just test-full`, дождаться результата `single-lifecycle`, затем остановить оставшуюся медленную матрицу первой версии и адресно очистить её ресурсы; ошибку при параллельном запуске перепроверить одним сценарием k3s и сохранить результаты в итоговом отчёте.
- [x] 4.3 Выполнить `openspec validate collect-operator-valkey-metrics --type change --strict --no-interactive` и устранить ошибки артефактов.
