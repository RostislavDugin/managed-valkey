## 1. Схема данных и конфигурация

- [x] 1.1 Добавить миграцию `users`, `user_quotas`, `audit_logs` и `auth_registration_keys` с UUIDv7, внешними ключами, ограничениями и индексами из дизайна.
- [x] 1.2 Проверить работу авторизации интеграционными HTTP-тестами на тестовой PostgreSQL после применения миграций; отдельные тесты SQL-миграций не добавлять.
- [x] 1.3 Подключить прямые зависимости bcrypt и JWT v5, добавить обязательный `JWT_SECRET` в конфигурацию API и проверить загрузку допустимой конфигурации и отказ без секрета модульными тестами.
- [x] 1.4 Обновить разделы авторизации, идемпотентности, схемы PostgreSQL и матрицы тестов в `SYSTEM.md` и `SYSTEM-DETAIL.md`; проверить, что контракт регистрации с `Idempotency-Key` и новая таблица описаны одинаково в документах и спецификациях.

## 2. Общие ошибки API

- [x] 2.1 Создать `api/internal/apierr` с кодами, HTTP-статусами и единым JSON-представлением ошибок; модульными тестами проверить известные коды, `details` и безопасное сообщение для внутренней ошибки.
- [x] 2.2 Подключить общий обработчик ошибок и перевести Recovery на `500 INTERNAL`; HTTP-тестами проверить формат всех 4xx/5xx и отсутствие значения паники, тела авторизации и секретных заголовков в ответах и логах.

## 3. Предметная авторизация

- [x] 3.1 Реализовать нормализацию почты, проверку её формата и границ пароля, bcrypt cost 12 и фиктивное сравнение для неизвестного пользователя; модульными тестами проверить регистр, пробелы, 8 символов, 72 байта UTF-8 и неверный пароль.
- [x] 3.2 Реализовать выпуск и проверку JWT HS256 через отдельный сервис с `Clock`; модульными тестами проверить UUIDv7 в `sub`, срок 10 лет, истечение, неверную подпись, недостающие поля и запрет другого алгоритма.
- [x] 3.3 Добавить репозитории пользователей, квот, ключей регистрации и аудита с явными транзакциями и очисткой ключей старше 24 часов; интеграционными HTTP-тестами проверить атомарную регистрацию, конфликт почты, успешный и истёкший ключ, `IDEMPOTENCY_MISMATCH` и две конкурентные регистрации.
- [x] 3.4 Реализовать вход и получение текущего пользователя с проверкой `is_blocked`; интеграционными HTTP-тестами подтвердить одинаковый `UNAUTHORIZED` для неизвестной почты и неверного пароля, отказ заблокированному пользователю, запись аудита без секретов и нулевое использование в `/v1/me`.
- [x] 3.5 Реализовать общий скользящий лимит 10 запросов за 60 секунд на прямой IP с внедряемыми часами и очисткой пустых записей; модульными тестами проверить общий предел трёх маршрутов, точный `retry_after`, освобождение окна и игнорирование forwarded-заголовков.

## 4. HTTP-маршруты и защита API

- [x] 4.1 Зарегистрировать `POST /v1/auth/check-email`, `POST /v1/auth/register` и `POST /v1/auth/login`; HTTP-тестами проверить успешные ответы, валидацию, конфликт, обязательный `Idempotency-Key`, ограничение частоты и заголовок `Retry-After`.
- [x] 4.2 Добавить middleware Bearer JWT, который загружает пользователя, проверяет `is_blocked`, передаёт `user_id` обработчику и добавляет его в логгер; HTTP-тестами проверить отсутствующий, повреждённый, просроченный и действующий токены, а также блокировку после выпуска JWT.
- [x] 4.3 Добавить защищённый `GET /v1/me` с пользователем, квотой и использованием; HTTP-тестами проверить DTO, нормализованную почту и отказ без авторизации.
- [x] 4.4 Собрать зависимости авторизации в точке входа API и сохранить работу `/livez` и `/readyz`; тестами маршрутизатора и сервера проверить, что технические проверки не требуют JWT и готовность по-прежнему зависит только от PostgreSQL.

## 5. Повторы в HTTP-клиенте фронтенда

- [x] 5.1 Разделить публичный `apiRequest` и внутренний исполнитель попыток с подменяемыми `fetch`, временем, ожиданием и случайным числом; тестами проверить успех с первой попытки и прежнее добавление базового URL, JSON-заголовков и JWT.
- [x] 5.2 Реализовать максимум три попытки с растущей случайной задержкой для сетевых ошибок и статусов `408`, `425`, `500`, `502`, `503`, `504`; тестами с управляемым временем проверить успешный повтор, исчерпание попыток и отсутствие повтора остальных статусов.
- [x] 5.3 Ограничить повторы безопасными методами, явной политикой и `Idempotency-Key`; тестами проверить запрет небезопасного повтора, воспроизведение JSON-тела и сохранение метода, пути, `X-Request-Id` и ключа между попытками.
- [x] 5.4 Связать `AbortSignal` с активной попыткой и задержкой; тестами проверить отмену до запроса, во время `fetch` и между попытками без преобразования `AbortError` в предметную ошибку.
- [x] 5.5 Расширить разбор ошибок: получать срок ожидания `429` из JSON или `Retry-After`, использовать 60 секунд при пустом ответе Caddy и завершать сессию при `401`; тестами проверить каждый источник срока, недействительный JSON и событие удаления токена.

## 6. Подключение авторизации фронтенда

- [x] 6.1 Заменить реализацию `checkEmail`, `register` и `login` в `shared/api/auth.ts` вызовами API, создавать один `Idempotency-Key` на отправку регистрации и сохранять серверный JWT; тестами проверить тела, маршруты, повтор одного ключа, `CONFLICT` и `UNAUTHORIZED`.
- [x] 6.2 Реализовать `getSession` через локальную проверку `exp` и защищённый `/v1/me`, сохранив интерфейс `Session`; тестами проверить действующий токен, истечение, блокировку, временную ошибку после повторов и отсутствие токена.
- [x] 6.3 Удалить `mv_users`, генерацию демонстрационных JWT, искусственную задержку, миграцию старого токена и пометку демонстрационного режима; тестами страницы проверить двухшаговый вход, регистрацию, менеджер паролей и прежние сообщения об ошибках с настоящим клиентом.
- [x] 6.4 Подключить обработчик кнопки «Выйти» к `logout`: удалить `mv_token`, перейти на `/auth` и завершить сессию в других вкладках через событие `storage`; тестами оболочки проверить кнопку, выход по `401`, таймер `exp` и межвкладочное событие.

## 7. Общая проверка

- [x] 7.1 Запустить `just test` в `api` с тестовой PostgreSQL и исправить все ошибки; отдельно убедиться, что тесты авторизации очищают созданные строки и не зависят от порядка запуска.
- [x] 7.2 Запустить `pnpm check` в `web` и исправить форматирование, границы FSD, типы, тесты и production-сборку.
- [x] 7.3 Запустить Go-форматирование, линтер, сборку и `go mod tidy -diff` по правилам pre-commit hook; проверить чистый результат без добавления посторонних файлов в индекс.
- [x] 7.4 Проверить вручную путь `check-email -> register -> /v1/me -> logout -> login` через Vite и локальный API, затем подтвердить автоматический повтор временного `503` и показ `429` без скрытого ожидания.

## 8. Интеграционные тесты через HTTP

- [x] 8.1 Добавить общее тестовое окружение с `httptest.Server`, настоящими `auth.Service`, `store.Store` и PostgreSQL, а также помощник регистрации уникального аккаунта через HTTP с автоматической очисткой данных.
- [x] 8.2 Переписать интеграционные сценарии авторизации, маршрутизатора, middleware, health и ошибок на запросы через `http.Client`; удалить подмену сервиса авторизации и прямые интеграционные вызовы сервиса.
- [x] 8.3 Запустить `api just test`, Go-форматирование, линтер, сборку, `go mod tidy -diff` и строгую проверку OpenSpec.
