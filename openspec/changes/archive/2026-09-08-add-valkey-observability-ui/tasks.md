## 1. Модель времени и временные клиенты

- [x] 1.1 Установить зависимости текущего worktree командой `pnpm install` в `web` и проверить, что версии из lock-файла установились без его изменения.
- [x] 1.2 Вынести настройку русской локали, точное форматирование и `dayjs(value).fromNow()` в `shared/lib/date-time.ts`, перевести карточку базы на общий модуль и проверить точную и относительную подпись тестами с поддельными часами.
- [x] 1.3 Добавить в `pages/valkey/model` типы метрик, нод, окон, действий аудита и форматирование значений; проверить окна, подписи действий, единицы и группировку местных дат модульными тестами.
- [x] 1.4 Добавить `auditLogs` и автора мутации в схему `mv_valkey_instances`; в совместной сборке с `add-valkey-password-management-ui` сохранить итоговую версию 5, миграции версий 1–4, метаданные учётных данных, снимок почты и запись событий только после успешного создания, переименования, изменения тарифа, смены пароля и удаления.
- [x] 1.5 Реализовать клиент аудита с сортировкой, лимитом 50 и непрозрачным base64url-курсором; проверить одинаковое время событий, отсутствие пропусков и повторов на нескольких страницах.
- [x] 1.6 Реализовать отменяемый клиент метрик с 30, 60, 288 и 336 точками, плавными детерминированными рядами по базе и временной корзине и разрывами `null`; проверить границы ресурсов, состав нод `single` и `ha`, стабильный повтор и отмену запроса.

## 2. Экран мониторинга

- [x] 2.1 Добавить семантические токены трёх рядов и стили графиков для светлой и тёмной схемы; проверить контраст сплошных линий и отсутствие цвета как единственного признака выбранного состояния.
- [x] 2.2 Собрать сводку и четыре площадных графика памяти, CPU, подключений и операций через `AreaChart` с кривой `monotone`, не выводя отдельный график «Ключи»; проверить `connectNulls={false}`, градиентную заливку, ось памяти от 0 до RAM одной ноды, CPU от 0 до 100% и подписи внутри карточек.
- [x] 2.3 Реализовать окно 5 минут по умолчанию, выбор окна и видимости нод, последовательное обновление каждые 5 секунд без параллельных запросов, подсказки с точным временем и `fromNow()`, доступные с мыши и через настоящие SVG-точки с клавиатуры, и ручную повторную загрузку; проверить таймер, стрелки, `Home`, `End`, сохранение выбранных нод при смене окна, сброс выбора при смене базы и число точек каждого окна компонентными тестами.
- [x] 2.4 Добавить состояния первой загрузки, пустых данных и ошибки с повтором; проверить, что смена окна не убирает весь экран и поздний ответ не заменяет данные другого окна.

## 3. Журнал аудита

- [x] 3.1 Собрать строки журнала с русской подписью, техническим именем действия, почтой, точным местным временем и `fromNow()`; проверить пересчёт относительных подписей через минуту поддельного времени.
- [x] 3.2 Сгруппировать события под заголовками «Сегодня», «Вчера» и календарными датами; проверить границу местного дня и показ года только вне текущего года.
- [x] 3.3 Подключить кнопку «Показать ещё», добавление событий по курсору и состояния загрузки, пустого ответа и ошибки; проверить несколько страниц, слияние соседних групп дат и защиту от повторного нажатия во время запроса.

## 4. Маршруты и итоговая проверка

- [x] 4.1 Заменить `ValkeyPlaceholderPage` страницами мониторинга и аудита, переименовать вкладку в «Аудит» и обновить публичный API среза; проверить прямые ссылки, перезагрузку, кнопки браузера и сохранение общего каркаса базы.
- [x] 4.2 Обновить тестовый роутер и проверки вкладок, затем прогнать тесты раздела Valkey и убедиться, что прежние сценарии управления базой продолжают работать.
- [x] 4.3 Проверить оба экрана в светлой и тёмной схемах на ширинах 375, 768, 1024 и 1440 пикселей, включая фокус настоящих точек графика, подписи внутри карточек, области нажатия и отсутствие горизонтальной прокрутки.
- [x] 4.4 Запустить `pnpm check` в `web` и `openspec validate --all --strict`; обе команды должны завершиться с нулевым кодом.
