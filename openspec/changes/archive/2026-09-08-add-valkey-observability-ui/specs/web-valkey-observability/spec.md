## Purpose

Экран наблюдаемости показывает состояние выбранной базы по временным рядам и
сохраняет понятную историю действий до подключения серверного API.

## ADDED Requirements

### Requirement: Мониторинг показывает метрики выбранной базы

Экран мониторинга SHALL показывать последние значения и плавные площадные
временные ряды использованной памяти, CPU, подключений и операций в секунду.
Память SHALL отображаться в гигабайтах, а CPU SHALL отображаться в процентах от
выделенных базе vCPU в диапазоне от 0 до 100%. Экран SHALL показывать только эти
четыре основных графика и MUST NOT показывать отдельный график попаданий,
промахов и вытеснений ключей. Значения разных баз смешиваться MUST NOT.

#### Scenario: Единицы и форма основных графиков

- **WHEN** экран получает память в байтах и CPU в миллиядрах
- **THEN** память в сводке, на оси и в подсказке показана в гигабайтах, а её ось идёт от 0 до объёма RAM одной ноды
- **AND** CPU переведён в процент от выделенных базе vCPU и ограничен шкалой от 0 до 100%
- **AND** четыре основных временных ряда показаны областями с полупрозрачной заливкой и сплошными умеренно сглаженными линиями без дополнительных пиков между измерениями

#### Scenario: База с одной нодой

- **WHEN** пользователь открывает мониторинг базы в режиме `single`
- **THEN** сводные значения и графики показывают ряд единственной ноды
- **AND** у ряда видны имя ноды и роль `primary`

#### Scenario: Отказоустойчивая база

- **WHEN** пользователь открывает мониторинг базы в режиме `ha`
- **THEN** графики показывают отдельные ряды primary и двух реплик
- **AND** пользователь может включать и выключать каждый ряд

#### Scenario: Значение в точке графика

- **WHEN** пользователь наводит указатель или переводит фокус на точку графика
- **THEN** подсказка показывает точное местное время, относительное время и значения видимых нод
- **AND** последние значения доступны в тексте вне подсказки

#### Scenario: Навигация по точкам с клавиатуры

- **WHEN** пользователь переводит фокус на график
- **THEN** последняя доступная точка ряда принимает фокус
- **AND** стрелки переводят фокус между соседними точками
- **AND** `Home` и `End` переводят фокус в начало и конец ряда
- **AND** подсказка фокуса показывает точное и относительное время и значения видимых нод

#### Scenario: Пропуск измерения

- **WHEN** у ноды нет измерения или значение CPU равно `null`
- **THEN** график оставляет разрыв в ряду
- **AND** не подставляет ноль и не соединяет соседние точки через пропуск

### Requirement: Пользователь выбирает окно метрик

Мониторинг SHALL предоставлять окна 5 минут, 1 час, 24 часа и 7 дней. При первом
открытии SHALL быть выбрано окно 5 минут. Выбранное окно SHALL определять
детализацию оси времени и объём загружаемых данных. Экран SHALL обновлять метрики
каждые 5 секунд и MUST NOT запускать следующий запрос до завершения предыдущего.

#### Scenario: Первое открытие

- **WHEN** пользователь впервые открывает мониторинг базы
- **THEN** выбрано окно 5 минут
- **AND** загружены 30 точек на ноду

#### Scenario: Последовательное обновление

- **WHEN** экран мониторинга остаётся открытым
- **THEN** новый запрос запускается через 5 секунд от начала предыдущего запроса
- **AND** если предыдущий запрос длится дольше 5 секунд, новый начинается только после его завершения
- **AND** два запроса метрик не выполняются параллельно

#### Scenario: Переключение окна

- **WHEN** пользователь выбирает другое окно
- **THEN** экран загружает ряды для этого окна
- **AND** показывает состояние загрузки, не сбрасывая выбранные ноды

#### Scenario: Переход к другой базе

- **WHEN** пользователь без перезагрузки открывает мониторинг другой базы
- **THEN** экран выбирает все ноды новой базы
- **AND** значения и идентификаторы нод прежней базы не появляются на экране

#### Scenario: Окно в пределах суток

- **WHEN** выбрано окно 5 минут, 1 час или 24 часа
- **THEN** ось времени показывает подходящие для ширины окна часы и минуты

#### Scenario: Недельное окно

- **WHEN** выбрано окно 7 дней
- **THEN** ось времени разбита по календарным дням
- **AND** точное время остаётся доступно в подсказке точки

### Requirement: Временный клиент метрик повторяет будущий API

До появления серверного API мониторинг SHALL получать асинхронный ответ от
клиента с теми же окнами и рядами по нодам, которые описаны для
`GET /v1/managed/valkey/instances/{id}/metrics`. Компоненты страницы MUST NOT
создавать демонстрационные измерения сами.

#### Scenario: Число точек в готовом окне

- **WHEN** клиент получает запрос для окна `5m`, `1h`, `24h` или `7d`
- **THEN** он возвращает соответственно 30, 60, 288 или 336 временных точек на ноду

#### Scenario: Стабильные демонстрационные данные

- **WHEN** клиент повторно получает одинаковый запрос для той же базы и временной корзины
- **THEN** значения и пропуски в уже существующих точках не меняются случайным образом

#### Scenario: Замена клиента метрик

- **WHEN** появляется серверный API метрик
- **THEN** источник данных меняется внутри клиента
- **AND** экран и его элементы управления не требуют изменения

### Requirement: Журнал аудита сгруппирован по дням

Экран аудита SHALL показывать время, понятное название действия и почту автора.
События SHALL идти от новых к старым и группироваться по календарной дате в
местном часовом поясе браузера.

#### Scenario: События сегодня и вчера

- **WHEN** журнал содержит события за текущий и предыдущий местный день
- **THEN** группы называются «Сегодня» и «Вчера»

#### Scenario: Более старое событие

- **WHEN** событие относится к более ранней дате
- **THEN** заголовок группы содержит день, название месяца и год, когда он отличается от текущего

#### Scenario: Время события

- **WHEN** пользователь читает строку журнала
- **THEN** рядом показаны точное местное время и относительная подпись вида «5 минут назад»
- **AND** относительная подпись пересчитывается, пока страница открыта

#### Scenario: Известное действие

- **WHEN** журнал содержит действие из фиксированного списка аудита Valkey
- **THEN** экран показывает его русское название и подходящую иконку
- **AND** техническое имя действия остаётся доступно в строке

### Requirement: Журнал загружается страницами

Первый запрос журнала SHALL возвращать до 50 новых событий. При наличии более
старых событий экран SHALL показывать кнопку «Показать ещё» и передавать
полученный курсор следующему запросу без разбора.

#### Scenario: Есть следующая страница

- **WHEN** ответ содержит курсор продолжения
- **THEN** пользователь видит кнопку «Показать ещё»
- **AND** нажатие добавляет старые события после уже показанных
- **AND** события остаются в правильных группах дат без повторов

#### Scenario: Журнал загружен полностью

- **WHEN** ответ не содержит курсор продолжения
- **THEN** кнопка «Показать ещё» не показывается

### Requirement: Временный журнал отражает действия пользователя

До появления серверного API временное хранилище SHALL записывать аудит при
создании, переименовании, изменении тарифа и удалении базы. Запись SHALL
содержать идентификатор события, техническое имя действия, почту пользователя
на момент действия и время в UTC.

#### Scenario: Создание базы

- **WHEN** временный клиент успешно создаёт базу
- **THEN** журнал получает одно событие `instance.create`

#### Scenario: Изменение базы

- **WHEN** временный клиент успешно переименовывает базу или меняет её тариф
- **THEN** журнал получает одно соответствующее событие `instance.update` или `instance.resize`

#### Scenario: Отклонённое действие

- **WHEN** временный клиент отклоняет действие из-за проверки данных, конфликта или квоты
- **THEN** журнал не получает событие

#### Scenario: Замена клиента аудита

- **WHEN** появляется серверный API аудита
- **THEN** источник данных меняется внутри клиента
- **AND** экран, группировка и постраничная загрузка не требуют изменения

### Requirement: Экраны обрабатывают состояния данных

Мониторинг и аудит SHALL иметь отдельные состояния загрузки, данных, пустого
ответа и ошибки. Ошибка одного экрана MUST NOT скрывать каркас страницы базы.

#### Scenario: Первая загрузка

- **WHEN** экран ожидает первый ответ временного клиента
- **THEN** он показывает скелетон с зарезервированным местом под содержимое

#### Scenario: Пустые метрики

- **WHEN** за выбранное окно нет ни одного измерения
- **THEN** мониторинг сообщает, что измерений пока нет
- **AND** выбранное окно остаётся доступно

#### Scenario: Пустой журнал

- **WHEN** клиент возвращает пустой список событий
- **THEN** аудит сообщает, что действий с базой пока не было

#### Scenario: Ошибка загрузки

- **WHEN** клиент не может вернуть метрики или аудит
- **THEN** соответствующий экран показывает причину и кнопку повторной загрузки

### Requirement: Представление работает в обеих схемах и с клавиатуры

Цвета рядов SHALL следовать семантическим токенам светлой и тёмной схемы.
Состояние видимости ряда MUST NOT передаваться только цветом. Экраны SHALL
сохранять видимый фокус, подписи элементов управления и отсутствие
горизонтальной прокрутки страницы на поддерживаемых ширинах.

#### Scenario: Выбор ряда с клавиатуры

- **WHEN** пользователь переходит к списку нод с клавиатуры
- **THEN** он может изменить видимость каждого ряда
- **AND** видит выбранное состояние без опоры только на цвет

#### Scenario: Узкий экран

- **WHEN** ширина экрана составляет 375 пикселей
- **THEN** сводные значения, графики и строки аудита укладываются по вертикали
- **AND** страница не получает горизонтальную прокрутку
