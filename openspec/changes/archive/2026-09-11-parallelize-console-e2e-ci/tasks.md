## 1. Выбор группы e2e

- [x] 1.1 Добавить runner-параметр `all`, `lifecycle`, `quotas` или `failures`, передавать соответствующий Playwright-фильтр до bootstrap и проверить маршрутизацию, неизвестное имя и пустую группу в `scripts/run_playwright_e2e_test.sh`.
- [x] 1.2 Расширить проверку обязательного каталога контролем непересекающихся групп и полного покрытия семи сценариев; проверить отсутствие сценария, неверный тег, пересечение и пустую группу в `scripts/check_e2e_catalog_test.sh`.
- [x] 1.3 Сохранить значение `all` для `just test-e2e` и `just test-e2e-headed`, добавить явные групповые команды для CI и проверить команды без создания k3s.

## 2. Параллельный CI

- [x] 2.1 Перевести `test-e2e` на matrix `lifecycle`, `quotas`, `failures` с `fail-fast: false`, отдельными артефактами и общим обязательным результатом; проверить структуру и отсутствие зависимостей от lint в `scripts/ci_cd_workflow_test.sh`.
- [x] 2.2 Обновить документацию e2e и CI: локальный запуск использует один общий стенд, а GitHub Actions создаёт три изолированных стенда и выполняет группы параллельно; проверить согласованность поиском устаревших утверждений.

## 3. Проверка и спецификации

- [x] 3.1 Запустить typecheck e2e, shell syntax, тесты runner, каталога и workflow, затем выполнить `openspec validate parallelize-console-e2e-ci --strict` и `openspec validate --specs`.
- [x] 3.2 Отправить изменение в GitHub Actions и убедиться, что три e2e-группы стартуют независимо, вместе покрывают семь сценариев, очищают каждый стенд и разрешают итоговое задание и deploy только после общего успеха.
- [x] 3.3 Синхронизировать дельты `ci-cd` и `test-orchestration` в основные спецификации после успешной проверки, повторить `openspec validate --specs` и проверить итоговый diff.
