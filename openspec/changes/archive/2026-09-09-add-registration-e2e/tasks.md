## 1. Пакет Playwright

- [x] 1.1 Создать отдельный пакет `tests/e2e` с закреплёнными зависимостями TypeScript и `@playwright/test`; проверить установку через `pnpm install --frozen-lockfile`.
- [x] 1.2 Добавить конфигурацию одного Chromium без повторов, с базовым URL из окружения и артефактами при ошибке; проверить разбор конфигурации командой Playwright со списком тестов.

## 2. Сценарий регистрации

- [x] 2.1 Реализовать Playwright-сценарий `@prod`, который создаёт уникальную почту, проходит двухэтапную форму с паролем `testpassword` и проверяет `/valkey/management` и почту в авторизованной навигации; проверить сценарий на локальном стенде.
- [x] 2.2 Убедиться, что тест не вызывает API напрямую, не создаёт Valkey и не выводит пароль в название или журналы; проверить исходный код теста и отчёт успешного запуска.

## 3. Локальный запуск

- [x] 3.1 Добавить сценарий, который в выделенном e2e-окружении применяет миграции, запускает API и Vite на свободных loopback-портах, ждёт готовности и всегда завершает дочерние процессы; проверить успешный `just test-e2e` и отсутствие оставшихся процессов после него.
- [x] 3.2 Добавить `tests/e2e/Justfile` и корневые команды `test-e2e`, `test-e2e-headed` и `test-prod`; проверить список команд `just` и сухой запуск production-команды с точным адресом `https://app.h3llo-demo.com`.
- [x] 3.3 Подключить `e2e` к `scripts/run_all_tests.sh` через `tests/e2e/Justfile`, оставив `integration` будущим набором; проверить, что корневой `just test` запускает e2e параллельно с API, оператором и `web` и учитывает его ошибку в результате.

## 4. CI и диагностика

- [x] 4.1 Добавить обязательное задание `test-e2e`, которое после всех lint-заданий устанавливает Go, Node.js, pnpm, `just`, Chromium и вызывает `just test-e2e`; проверить, что оно не зависит от других тестовых заданий.
- [x] 4.2 Отключить небезопасную трассировку авторизации, сохранять при ошибке отчёт, снимки экрана и диагностику e2e без kubeconfig и файлов с секретами; намеренно уронить тест и проверить артефакты на отсутствие пароля и тела запроса регистрации.
- [x] 4.3 Добавить `test-e2e` в зависимости отключённых заданий сборки, чтобы после их включения ошибка e2e блокировала сборку; проверить оба списка `needs` в `.github/workflows/ci-cd.yml`.

## 5. Документация и общая проверка

- [x] 5.1 Обновить `tests/e2e/TODO.md`: описать реализованный сценарий и оставить жизненный цикл, квоты и отказы Valkey незавершёнными; проверить, что документ не называет будущие сценарии подключёнными.
- [x] 5.2 Выполнить `just test-e2e`, корневой `just test` и `git diff --check`; проверить успешную регистрацию, удаление временной базы и отсутствие изменений dev-данных.
