## 1. Идентификатор пользователя, модель и временный клиент

- [x] 1.1 Перевести временный auth на `userId`: добавить UUIDv7 в `mv_users`, хранить его в JWT `sub`, вернуть `userId` и почту в `Session`; проверить тестами формат UUIDv7, регистрацию, вход, восстановление сессии, миграцию старой записи и токена с почтой в `sub`.
- [x] 1.2 Добавить в `pages/valkey/model` типы базы, полную тарифную сетку из `SYSTEM.md`, расчёт цены по периодам и форматирование ресурсов; проверить тестами все допустимые сочетания vCPU и RAM, отбрасывание сочетаний вне правил и цены `single` и `ha` на граничных размерах.
- [x] 1.3 Реализовать расчёт занятой квоты и доступности кандидата при создании и resize; проверить тестами суммирование по числу нод, граничные значения 4 vCPU / 16 ГБ и исключение текущего размера изменяемой базы.
- [x] 1.4 Реализовать в `pages/valkey/api` проверяемое версионированное хранение `mv_valkey_instances`, разделение по `ownerId` из сессии и задержку 400 мс; проверить тестами пустое хранилище, повторное чтение, другой `userId`, повреждённую запись и неизвестную версию.
- [x] 1.5 Добавить операции списка, чтения, создания, переименования, resize и удаления с кодами `ApiError`; проверить тестами конфликт имени, превышение квоты, неизвестный идентификатор и сохранение результата каждой операции.

## 2. Маршруты и каркас раздела

- [x] 2.1 Добавить публичный API среза `pages/valkey`, маршруты `/valkey/*` и перенаправления с `/` и `/valkey`; проверить прямое открытие управления, создания, карточки, мониторинга и журнала аудита.
- [x] 2.2 Собрать `ValkeyLayout` с хлебными крошками «Базы данных», «Базы данных / Создание БД» и «Базы данных / <имя базы>»; проверить состав крошек на списке, создании и всех вкладках базы.
- [x] 2.3 Собрать `ValkeyInstanceLayout` со вкладками-ссылками открытой базы и заглушками «Мониторинг» и «Аудит логи» с текстом «Раздел появится позже»; проверить, что вкладки есть только у базы, активная следует за адресом и переходы работают мышью, клавиатурой и кнопками браузера.

## 3. Список и пустое состояние

- [x] 3.1 Реализовать загрузку баз текущего пользователя с состояниями загрузки, ошибки и повтора; проверить, что данные появляются после задержки и страница не скачет при замене скелетона.
- [x] 3.2 Собрать пустое состояние по `/temp/h3llo-databases-empty.png` с кратким описанием и кнопкой «Создать базу»; проверить, что таблица и квота при пустом списке не показываются.
- [x] 3.3 Собрать заголовок, поиск и таблицу по `/temp/h3llo-databases-list.png`; проверить столбцы, переход по имени, меню строки, фильтрацию без учёта регистра и отдельное состояние «Ничего не найдено».
- [x] 3.4 Добавить правую панель использования с занятыми и доступными vCPU и RAM; проверить значения на наборах `single`, `ha` и их сочетании.

## 4. Создание базы

- [x] 4.1 Собрать форму на `useForm` с радио-карточками режима, отдельными дискретными слайдерами vCPU и RAM и генерируемым именем; проверить всю тарифную сетку, приведение RAM к ближайшему допустимому значению после смены vCPU, рабочие значения по умолчанию и проверку имени при потере фокуса.
- [x] 4.2 Добавить поле префикса с правилами из `SYSTEM.md`, предпросмотром будущего DNS-имени и сборкой `<префикс>-<6 случайных символов>` во временном клиенте; проверить значение по умолчанию, отказ на неверном префиксе, уникальность DNS-имени между владельцами и его неизменность при переименовании.
- [x] 4.3 Добавить панель стоимости с периодами «Месяц», «День» и «Час»; проверить немедленный пересчёт при смене режима, тарифа и периода.
- [x] 4.4 Оставить размеры сверх квоты доступными для просмотра цены, заблокировать отправку, показать причину и ссылку `https://t.me/rostislav_dugin`; проверить частично занятую и полностью исчерпанную квоту.
- [x] 4.5 Подключить отправку формы, состояние загрузки, ошибки поля и уведомление об успехе; проверить создание, защиту от повторной отправки, конфликт имени, сохранение после перезагрузки и переход на карточку.

## 5. Карточка, изменение и удаление

- [x] 5.1 Собрать карточку по `/temp/h3llo-database-detail.png` с именем, статусом, режимом, тарифом, ресурсами, временем создания и текущей стоимостью; проверить прямую ссылку и состояние неизвестной базы.
- [x] 5.2 Добавить переименование в коротком модальном окне; проверить допустимое имя, конфликт, ошибку у поля и обновление карточки, хлебных крошек и списка.
- [x] 5.3 Добавить изменение тарифа с неизменяемым режимом, новой ценой, проверкой квоты без текущего размера базы и предупреждением о последствиях ресайза; проверить простой и очистку кеша для `single`, очистку кеша при уменьшении RAM в `ha`, риск потери последних записей при остальных изменениях `ha`, отсутствие отмены и отказ при превышении остатка.
- [x] 5.4 Добавить подтверждение «Удалить базу `<имя>`?» и кнопку «Удалить базу»; проверить отмену, успешное удаление, освобождение квоты и переход к пустому состоянию после удаления последней базы.

## 6. Адаптивность и итоговая проверка

- [x] 6.1 Настроить таблицу и правые панели для ширин 375, 768, 1024 и 1440px; проверить отсутствие горизонтальной прокрутки страницы, прокрутку только внутри таблицы и открытие панели из выдвижного блока на узком экране.
- [x] 6.2 Проверить светлую и тёмную схемы, видимый фокус, подписи полей, `aria-label` и области нажатия не меньше 44×44px; исправить найденные расхождения с `web/DESIGN.md`.
- [x] 6.3 Запустить `pnpm check` в `web` и `openspec validate add-valkey-management --strict`; обе команды должны завершиться с нулевым кодом.

## 7. Свод композиции с оригиналом консоли

- [x] 7.1 Добавить в каркас консоли слоты шапки и правой колонки, перенести в них хлебные крошки и панели раздела; проверить, что крошки стоят вне содержимого страницы, панель попадает в правую колонку и пустая колонка ширину не занимает.
- [x] 7.2 Свести сетку формы с оригиналом: равные колонки подписи и контрола, иконки-подсказки у подписей, кнопка отправки на ширину колонки контролов, метки слайдера RAM с единицей; проверить доступные имена контролов и подсказок.
- [x] 7.3 Добавить во временный клиент `getValkeyDomain`, показать адрес с доменом в подсказке префикса и на карточке базы; проверить значения по умолчанию, переопределение переменными окружения и работу формы при недоступном домене.
- [x] 7.4 Свести оформление с оригиналом по замеру `temp/index.html`: начертания Mantine, подписи `Input.Label`, поля размера `md`, слайдер `xl` с радиусом 8px, подсветка выбранной карточки, крошки весом 600, строки расчёта 10px на 12px; проверить, что тесты видят прежние значения расчёта.
- [x] 7.5 Довести замер: колонка контента 784px с боковыми полями сверх неё, шапка без разделителя, крошки начертания 500; проверить, что подписи и контролы формы совпадают по вертикалям с оригиналом.

## 8. Уточнение карточки базы по действующей консоли

- [x] 8.1 Перенести статус, меню удаления и панель текущей стоимости в `ValkeyInstanceLayout`; проверить их на всех вкладках и отсутствие горизонтального сдвига содержимого.
- [x] 8.2 Пересобрать управление с именем и кнопкой переименования, копируемым идентификатором, примерами JavaScript, TypeScript, Python и Go и плотной карточкой свойств.
- [x] 8.3 Заменить слайдеры изменения тарифа на радио-карточки, сохранить текущую нестандартную конфигурацию в списке и удалить пояснение о ресурсах на ноду и кластер.
- [x] 8.4 Обновить тесты, проверить страницу в браузере на широком и узком экране, затем запустить `pnpm check` и строгую проверку OpenSpec.
